---
phase: 10-source-vetting
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [VET-19-01, VET-19-02, VET-19-03, VET-19-04]
estimate:
  tokens: 18000
  raw_tokens: 9000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "VET-19-01..04 checkboxes remain `- [ ]` (unchecked). Verify, not execute, checks them."
    - "Each VET-19 line has a parenthetical annotation recording the 10-RESEARCH / 10-01 verdict (retry failed / dated tier / unused / Excluded-pending)"
    - "IO-01..06 lines carry Phase 10 handoff notes (remap / more VV&A / GO 8719 / GO 200N / AAF deferred) but stay unchecked"
    - "STATE.md Deviations/Notes has a dated Phase 10 verdicts bullet; frontmatter progress fields are untouched"
    - "ROADMAP.md Phase 10 **Plans** is no longer TBD; Phase 11 Goal/SC parentheticals name what Phase 10 cleared vs deferred"
    - "Unclassified coverage probes (VET-19-02, VET-19-03, VET-19-04) stay unresolved assumptions — do not auto-resolve with a backstop; do not invent check_kind / check_target"
    - "Idempotency (explicit): re-running 10-02 updates the same parentheticals / bullets in place; it does not duplicate the Phase 10 deviation bullet or append a second annotation to a VET-19 line"
    - "Concurrency (explicit): 10-02 starts only after 10-01 is committed; single writer per file; interrupted run re-reads and applies only missing notes"
    - "must-NOT put URLs in SOURCE-VETTING (re-assert grep -c http = 0); must-NOT treat unreachable as Tier 1 in annotations; must-NOT use AAF; must-NOT create packs/; must-NOT invent a pack; must-NOT check VET-19 boxes"
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: "VET-19 + IO handoff annotations (boxes still open)"
      contains: "retry failed; deferred, no in-source; not a build-clear"
    - path: .planning/STATE.md
      provides: "Phase 10 deviations bullet with GO / NO-GO"
      contains: "Phase 10 (2026-08-17)"
    - path: .planning/ROADMAP.md
      provides: "Phase 10 Plans list + Phase 11 consumes-vetting note"
      contains: "10-01-PLAN.md"
  key_links:
    - from: .planning/REQUIREMENTS.md
      to: docs/SOURCE-VETTING.md
      via: "annotation text must match 10-01 verdicts"
      pattern: "IS-GPS-200N"
    - from: .planning/STATE.md
      to: .planning/phases/10-source-vetting/10-RESEARCH.md
      via: "deviations bullet names 10-RESEARCH.md as store"
      pattern: "10-RESEARCH.md"
---

<objective>
Annotate REQUIREMENTS.md, STATE.md, and ROADMAP.md so Phase 11 consumes the Phase 10 decision table without re-guessing. Do not check VET-19 boxes.

Purpose: Close the planning-surface loop after 10-01 wrote the published register.
Output: Three scoped planning-file edits + a read-only consistency sweep (including Link Policy re-check on SOURCE-VETTING.md). No packs.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@docs/SOURCE-VETTING.md
@.planning/phases/10-source-vetting/10-RESEARCH.md
@.planning/phases/10-source-vetting/10-01-PLAN.md
</context>

<plan_notes>
10-01 must be committed first (depends_on). This plan does not re-edit SOURCE-VETTING.md except to fix Link Policy / verdict drift found in Task 2 sweep.

Do not touch STATE.md YAML frontmatter (current_phase, progress, completed_plans). Execute workflow owns those.

If a required parenthetical already exists (re-run), leave it or refresh wording — do not stack duplicates.
</plan_notes>

<claim_verification>
Live commands this planner session (2026-08-17). 10-01 has not executed yet; these are the pre-execute planning-file baselines.

| claim | command | observed | status |
|---|---|---|---|
| VET-19 boxes are open unchecked one-liners | `grep -n "VET-19" .planning/REQUIREMENTS.md` | lines 14–17 all `- [ ] **VET-19-0N**:` with no parenthetical verdict yet | VERIFIED |
| IO-01 still offers Army CBA pack as first option | `grep -n "IO-01" .planning/REQUIREMENTS.md` | `FUT-04 Army CBA pack **or** remap existing A-94 / VV&A` | VERIFIED |
| IO-02 still names \`dodm-5000-102\` first | `grep -n "IO-02" .planning/REQUIREMENTS.md` | `\`dodm-5000-102\` (or additional VV&A RPG chapters` | VERIFIED |
| IO-04 still says IS-200/300 | `grep -n "IO-04" .planning/REQUIREMENTS.md` | `select IS-200/300` | VERIFIED |
| STATE expects AAF deferral; no Phase 10 verdicts bullet yet | `grep -n "Phase 10\\|IO-05" .planning/STATE.md` | `IO-05/IO-06 are conditional on AAF clearing VET-19-03 — expect honest deferral`; no `Phase 10 (2026-08-17)` | VERIFIED |
| ROADMAP Phase 10 Plans is TBD | `grep -n "\\*\\*Plans\\*\\*" .planning/ROADMAP.md` | first hit under Phase 10: `**Plans**: TBD` | VERIFIED |
| STATE frontmatter current_phase 10 / status planning | `sed -n '1,16p' .planning/STATE.md` | `current_phase: 10`, `status: planning`, `completed_plans: 14` | VERIFIED |
| 10-01 plan exists as wave 1 owner of SOURCE-VETTING | `ls .planning/phases/10-source-vetting/10-01-PLAN.md` | file present | VERIFIED |
</claim_verification>

<tasks>

<task type="auto">
  <name>Task 1: Annotate REQUIREMENTS VET-19 + IO handoff notes (boxes stay open)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md, docs/SOURCE-VETTING.md, .planning/phases/10-source-vetting/10-RESEARCH.md</read_first>
  <action>Edit-only, scoped replacements of existing bullet lines. Do **not** change `- [ ]` to `- [x]` on any VET-19 or IO row. Do **not** add SOURCE-VETTING URLs. Do **not** create packs/. If a line already contains the exact parenthetical below, skip it.

Replace the four VET-19 bullets with these exact lines (checkbox stays open):

`- [ ] **VET-19-01**: Vet FUT-04 Army CBA Guide (ASAFM PDF retry) — build-or-exclude with in-source licence evidence — *Phase 10 (2026-08-17): retry failed; deferred, no in-source; not a build-clear. Official host 403, archive playback 503. FUT-04 remains. ROADMAP SC-1 satisfied by fresh evidence. Do not tick as built.*`

`- [ ] **VET-19-02**: Vet DoDM 5000.102, NASA-STD-8719.14, GPS ICD-IS-200/300 (select), NASA SP-7084 — Tier 1/2/Excluded with dated rationale — *Phase 10 (2026-08-17): NASA-STD-8719.14C = Tier 1 leaning; GPS select = IS-GPS-200N Tier 1 leaning (there is no public IS-300); NASA SP-7084 = Tier 1 RECONFIRMED; DoDM 5000.102 = UNVERIFIED / deferred-excluded (no PDF). Dated rows in docs/SOURCE-VETTING.md v1.19 section.*`

`- [ ] **VET-19-03**: AAF Product Support + Software pathway guidebooks — licence spot-check BEFORE any use (Phase 6 deferral still in force); record Tier or keep Excluded-pending — *Phase 10 (2026-08-17): still NOT yet vetted — do not use. Excluded-pending row added. No in-source guidebook grant. IO-05/IO-06 must record deferred.*`

`- [ ] **VET-19-04**: Add any newly ruled-out sources to docs/SOURCE-VETTING.md Excluded table — *Phase 10 (2026-08-17): AAF Product Support + Software pathway recorded as Excluded-pending (not a hard kill). Army CBA and DoDM 5000.102 stay deferred / UNVERIFIED — not Excluded-table hard-stops. No other newly ruled-out sources.*`

Append a single italic parenthetical to each of IO-01..06 (do not rewrite the requirement stem; do not touch IO-07 except the note below):

- After the IO-01 line, append: ` — *Phase 10 handoff: Army CBA is NO-GO; take the remap existing A-94 / VV&A decision chapters path. Do not invent a CBA pack.*`
- After the IO-02 line, append: ` — *Phase 10 handoff: \`dodm-5000-102\` is NO-GO; take additional VV&A RPG chapters in existing \`dod-vva-rpg\`.*`
- After the IO-03 line, append: ` — *Phase 10 handoff: GO on NASA-STD-8719.14C (build-time third-party scan still required).*`
- After the IO-04 line, append: ` — *Phase 10 handoff: GO on IS-GPS-200N only (not IS-300). Optional +705J/+800J. Skip ICD-GPS-153.*`
- After the IO-05 line, append: ` — *Phase 10 handoff: AAF Software pathway not cleared — record deferred. No AAF pack.*`
- After the IO-06 line, append: ` — *Phase 10 handoff: AAF Product Support not cleared — record deferred. No AAF pack.*`

On IO-07, append only: ` — *Phase 10: no new source cleared; outcome unchanged.*`

Do not add Out of Scope table rows for Army CBA / DoDM / AAF (they remain deferred, not descoped from the milestone the way GP-08 was). Do not edit MAP-19 / HYG / REL rows.
</action>
  <verify>
    <automated>grep -n "retry failed; deferred, no in-source" .planning/REQUIREMENTS.md && grep -n "IS-GPS-200N" .planning/REQUIREMENTS.md && grep -n "still NOT yet vetted — do not use" .planning/REQUIREMENTS.md && grep -n "Excluded-pending" .planning/REQUIREMENTS.md && grep -n "Army CBA is NO-GO" .planning/REQUIREMENTS.md && grep -n "Phase 10 handoff" .planning/REQUIREMENTS.md && test "$(grep -c '^- \\[x\\] \\*\\*VET-19' .planning/REQUIREMENTS.md)" = "0" && test "$(grep -c '^- \\[ \\] \\*\\*VET-19' .planning/REQUIREMENTS.md)" = "4"</automated>
  </verify>
  <acceptance_criteria>
    - all four VET-19 lines remain `- [ ]` and each contains `Phase 10 (2026-08-17)`
    - VET-19-01 contains `retry failed; deferred, no in-source; not a build-clear`
    - VET-19-02 names `NASA-STD-8719.14C`, `IS-GPS-200N`, `Tier 1 RECONFIRMED`, and `DoDM 5000.102 = UNVERIFIED`
    - VET-19-03 contains `NOT yet vetted — do not use`
    - VET-19-04 contains `Excluded-pending` and says Army CBA / DoDM are not hard-stops
    - IO-01..06 each contain `Phase 10 handoff`
    - no `- [x] **VET-19` line exists
  </acceptance_criteria>
  <done>REQUIREMENTS annotated with Phase 10 verdicts; VET-19 and IO boxes still open; no packs invented; AAF unused.</done>
</task>

<task type="auto">
  <name>Task 2: STATE + ROADMAP notes and consistency sweep</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>.planning/STATE.md, .planning/ROADMAP.md, docs/SOURCE-VETTING.md, .planning/REQUIREMENTS.md</read_first>
  <action>Edit-only. Do not touch STATE.md YAML frontmatter keys (`gsd_state_version`, `milestone`, `current_phase`, `status`, `stopped_at`, `last_updated`, `progress.*`). Do not create packs/. Do not check ROADMAP Phase 10 checkbox (`- [ ] **Phase 10` stays unchecked until verify/complete).

STATE.md under `## Deviations / Notes`, append this single bullet if it is not already present:

`- Phase 10 (2026-08-17): SOURCE-VETTING v1.19 recorded. GO: NASA-STD-8719.14C, IS-GPS-200N, SP-7084 optional. NO-GO: FUT-04 Army CBA (403/503 deferred), DoDM 5000.102 (UNVERIFIED), AAF (NOT yet vetted — do not use). Verdicts + URLs in 10-RESEARCH.md. Phase 11 builds only GO names.`

Leave the existing bullet `IO-05/IO-06 are conditional on AAF clearing VET-19-03 — expect honest deferral` in place (it is now confirmed, not contradicted).

ROADMAP.md scoped edits:

1. Phase 10 block: change `**Plans**: TBD` to `**Plans**: [10-01](phases/10-source-vetting/10-01-PLAN.md) (register), [10-02](phases/10-source-vetting/10-02-PLAN.md) (planning-surface annotations)`

2. Phase 10 overview bullet (the `- [ ] **Phase 10: Source vetting**` line) — keep the checkbox unchecked; append ` — 10-01/10-02 docs-only` after the existing parenthetical. Do not change the Goal or Success Criteria numbered list (SC-1 already allows FUT-04 deferred-with-fresh-evidence).

3. Phase 11 Goal line currently `**Goal**: Poorest competency primaries move; no silent ticks` — keep that sentence and append ` (consumes Phase 10: build 8719.14C + IS-GPS-200N; remap/defer Army CBA, DoDM 5000.102, AAF; SP-7084 optional)`

4. Phase 11 Success Criteria item 3 currently `Integration + Logistics built only if AAF cleared; otherwise deferred-recorded` — leave the text; it already matches the NO-GO. No pack-count change (Phase 10 built zero packs).

Then a read-only sweep; fix drift with scoped Edits only if a gate fails:

1. `test "$(grep -c 'http' docs/SOURCE-VETTING.md)" = "0"` — if non-zero, strip the leaking string from SOURCE-VETTING.md (allowed exception to files_modified).
2. Confirm SOURCE-VETTING contains `Vetted candidates (v1.19.0)`, `Phase 11 handoff`, `IS-GPS-200N`, `NASA-STD-8719.14C`, `NOT yet vetted — do not use`, `DoDM 5000.102 — UNVERIFIED`. If 10-01 missed one, stop and record a Deviations ledger entry rather than inventing a new verdict.
3. Confirm no `packs/` in `git diff --name-only -- packs/`.
4. Confirm VET-19 boxes still unchecked.
5. Confirm STATE frontmatter `progress:` block is byte-stable vs start of this task (do not "helpfully" bump completed_plans).

Do not run extract.py, catalog updates, or vet_source.py.
</action>
  <verify>
    <automated>grep -n "Phase 10 (2026-08-17)" .planning/STATE.md && grep -n "10-01-PLAN.md" .planning/ROADMAP.md && grep -n "10-02-PLAN.md" .planning/ROADMAP.md && grep -n "consumes Phase 10" .planning/ROADMAP.md && test "$(grep -c 'http' docs/SOURCE-VETTING.md)" = "0" && grep -q "Vetted candidates (v1.19.0)" docs/SOURCE-VETTING.md && grep -q "Phase 11 handoff" docs/SOURCE-VETTING.md && test "$(grep -c '^- \\[x\\] \\*\\*VET-19' .planning/REQUIREMENTS.md)" = "0" && test "$(git diff --name-only -- packs/ | wc -l)" = "0" && test "$(grep -c '\\*\\*Plans\\*\\*: TBD' .planning/ROADMAP.md)" = "3"</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md contains `Phase 10 (2026-08-17):` with GO and NO-GO names
    - STATE.md frontmatter `progress:` / `completed_plans` unchanged by this task
    - ROADMAP Phase 10 Plans links 10-01-PLAN.md and 10-02-PLAN.md (not TBD)
    - ROADMAP Phase 11 Goal contains `consumes Phase 10`
    - `grep -c http docs/SOURCE-VETTING.md` equals 0
    - SOURCE-VETTING still has v1.19 Vetted heading + Phase 11 handoff (10-01 artifacts)
    - no VET-19 box is checked
    - `git diff --name-only -- packs/` is empty
  </acceptance_criteria>
  <done>STATE + ROADMAP record Phase 10 verdicts and plan files; Link Policy still 0; VET-19 boxes open; no pack tree changes; frontmatter progress untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Published register → planning checkboxes | Annotations must not silently tick VET-19 (verify owns that) |
| Planning notes → Phase 11 execute | Wrong GO/NO-GO here causes pack-before-clear |

## ASVS L1 / STRIDE

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-10-01 | Information Disclosure | docs/SOURCE-VETTING.md (re-check) | high | mitigate | Task 2 sweep `grep -c http` = 0 |
| T-10-06 | Tampering / process bypass | REQUIREMENTS checkboxes | high | mitigate | Task 1 verify: zero `- [x] **VET-19` lines |
| T-10-07 | Spoofing | IO annotations claiming AAF/Army/DoDM cleared | high | mitigate | Exact NO-GO / deferred strings prescribed |
| T-10-03 | Elevation | packs/ | high | mitigate | files_modified are planning files only; packs/ diff empty |

No unmitigated high.
</threat_model>

## Artifacts this phase produces

New notes only (no code symbols expected):

- REQUIREMENTS.md: four VET-19 parentheticals; six IO handoff notes; IO-07 unchanged-outcome note
- STATE.md: one Deviations/Notes bullet `Phase 10 (2026-08-17): …`
- ROADMAP.md: Phase 10 Plans links; Phase 10 overview suffix; Phase 11 Goal consumes-vetting clause

No packs/, no catalog, no extract.py.

## Deviations ledger (executor)

If execute must diverge, append a bullet under **Deviations** in `.planning/phases/10-source-vetting/10-02-SUMMARY.md`:

- What changed
- Why
- Which VET-19 ID is affected

If no divergence: write `None.` Empty/None is valid.

If 10-01 artifacts are missing, do not invent SOURCE-VETTING verdicts here — stop and record the gap.

<verification>
- VET-19-01..04 annotated and still unchecked
- IO-01..06 carry Phase 10 handoff; AAF unused
- STATE deviations bullet names GO / NO-GO
- ROADMAP Plans no longer TBD on Phase 10
- Link Policy still 0; no packs/
</verification>

<success_criteria>
- Planning surfaces match 10-01 / 10-RESEARCH decisions
- Phase 11 can start without re-vetting
- VET-19 boxes left for verify
</success_criteria>

<output>
Create .planning/phases/10-source-vetting/10-02-SUMMARY.md when done
</output>
