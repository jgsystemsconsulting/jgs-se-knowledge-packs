---
phase: 13-release-surface-v1-19-0
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - .claude-plugin/plugin.json
  - .cursor-plugin/plugin.json
  - CHANGELOG.md
  - RELEASE-INFO.txt
  - README.md
  - catalog.json
  - docs/index.html
  - docs/packs.html
  - docs/products/website/01-jgs-se-knowledge-packs.yaml
  - docs/products/website/catalog.yaml
  - docs/capability-pack-map.json
  - docs/capability-map-CONTRACT.md
  - docs/capability-pack-map.md
  - .planning/STATE.md
  - .planning/MILESTONES.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [REL-19-01, REL-19-02]
estimate:
  tokens: 35000
  raw_tokens: 20000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "13-01 landed first: both gates PASS at 63 catalog / 65 dirs; trio + map_version are 1.19.0; CHANGELOG headed ## [1.19.0]; catalog dod-vva-rpg.chapters == 13; README has the two new live rows"
    - "One release commit is the last CONTENT commit on main: release(v1.19.0): Agent IO Depth — 2 packs + VV&A chapters + DA remap (63 +2 signposts)"
    - "Annotated tag v1.19.0 exists (git cat-file -t $(git rev-parse v1.19.0) == tag) with colon-style message; lightweight tag is a fail"
    - "git push origin main --follow-tags succeeded; git ls-remote --tags origin | grep v1.19.0 returns the tag"
    - "gh release view v1.19.0 succeeds; title uses an em dash; notes body equals the CHANGELOG [1.19.0] entry body (competency-led IO-01..07)"
    - "Release commit staged explicit paths only; no master_flow_state.json, no .edge-coverage.json, no 13-RESEARCH/PATTERNS/VALIDATION, no /tmp or _v1.19.0-notes.tmp.md"
    - "Separate .planning-only follow-up commit ticks REL-19-01/02, records SHA/tag/URL in STATE + MILESTONES, ticks ROADMAP Phase 13, lists 13-01-PLAN.md and 13-02-PLAN.md"
    - "No pack rebuild; no map reclassify; no CI repo-Python; no branch-protection change; no git add -A"
  artifacts:
    - path: .planning/STATE.md
      provides: "v1.19.0 shipped record (SHA, tag, GitHub Release URL) + remaining backlog"
    - path: .planning/MILESTONES.md
      provides: "v1.19.0 converted from in-planning to shipped format"
    - path: .planning/ROADMAP.md
      provides: "Phase 13 ticked; Plans lists 13-01 and 13-02"
    - path: .planning/REQUIREMENTS.md
      provides: "REL-19-01 and REL-19-02 ticked"
  key_links:
    - from: CHANGELOG.md
      to: GitHub Release v1.19.0
      via: "gh release create --notes-file phase-dir tmp copied from [1.19.0] body"
      pattern: "## [1.19.0]"
    - from: git tag v1.19.0
      to: origin
      via: "git push origin main --follow-tags"
      pattern: "v1.19.0"
    - from: release commit
      to: annotated tag
      via: "tag points at the last content commit; .planning commit comes after"
      pattern: "release(v1.19.0)"

# Prohibitions (must-NOT). No check_kind / check_target fields.
# - must-NOT git add -A / git add docs/ / git add .
# - must-NOT write gh --notes-file under /tmp
# - must-NOT create a lightweight tag
# - must-NOT tag on a red gate or dirty surprise-untracked tree
# - must-NOT rebuild packs or reclassify the map
# - must-NOT unwire the map gate or edit validate.yml
# - must-NOT invent packs; must-NOT bump branch protection
# - must-NOT put the .planning records commit into the tagged release commit
# - must-NOT commit _v1.19.0-notes.tmp.md or master_flow_state.json
---

<objective>
Wave 2 (depends_on 13-01): publish v1.19.0. Consolidate 13-01 content into one release commit, create the annotated tag, push origin main --follow-tags, create the GitHub Release, then record shipped state in a separate .planning commit.

Purpose: REL-19-02 (v1.19.0 tagged + GitHub Release). REL-19-01 must still hold at the tagged tree. Analog: 9-01 Task 5 + Task 6 (executed, including the push).
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<read_first>
.planning/phases/13-release-surface-v1-19-0/13-01-SUMMARY.md
.planning/phases/13-release-surface-v1-19-0/13-01-PLAN.md
.planning/phases/13-release-surface-v1-19-0/13-RESEARCH.md
CHANGELOG.md
RELEASE-INFO.txt
.planning/STATE.md
.planning/MILESTONES.md
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
</read_first>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/MILESTONES.md
@.planning/phases/13-release-surface-v1-19-0/13-RESEARCH.md
@.planning/phases/13-release-surface-v1-19-0/13-01-PLAN.md
@.planning/phases/9-release-surface-v1-18-0/9-01-PLAN.md
@.planning/phases/9-release-surface-v1-18-0/9-01-SUMMARY.md
@CHANGELOG.md
</context>

<plan_notes>
depends_on 13-01: if either gate is RED, or CHANGELOG has no `## [1.19.0]`, STOP. Do not tag.

TRACER_MODE: Task 1 is the production public-release slice (commit + annotated tag + push + gh). Task 2 is the planning-record follow-up after the tag exists.

REVERSIBILITY: Task 1 is costly (public tag + GitHub Release are hard to retract) but pre-approved by REL-19-02 and the Phase 9 analog. No decision checkpoint. Do not flip autonomous to false.

Stay on main. No branches / worktrees. Admin-bypass is still in force — push is expected to succeed without a PR. Do not "fix" branch protection.

Windows: write the gh notes file under this phase directory, never `/tmp`. Delete it after `gh release view` succeeds. Never commit it.

## Deviations ledger
Executor MUST keep a `## Deviations` section in 13-02-SUMMARY.md. Record every departure (soft-reset performed or skipped, gh notes retry, extra files in the release commit, push failure). If none: write `None.`
</plan_notes>

<claim_verification>
Live commands run this planner session (2026-08-17), cwd repo root. 13-01 will have changed the version surfaces by the time this plan runs — re-verify gates at task start. Pre-13-01 observations:

| claim | command | observed | status |
|---|---|---|---|
| Branch is main; no v1.19 tag | `git branch --show-current`; `git tag -l 'v1.19*'` | `main`; empty | VERIFIED |
| Phase 9 analog did push + gh | `9-01-PLAN.md:132`; `9-01-SUMMARY.md:182-190` | `git push origin main --follow-tags`; origin tag + `gh release view` succeeded | VERIFIED |
| Live tag style | `git tag -l -n3 v1.17.0 v1.18.0` | `v1.17.0: 8 Tier-1…`; `v1.18.0: 7 gap-driven…` (colon, annotated) | VERIFIED |
| Live GitHub Release title style | `gh release view v1.18.0 --json name,tagName,url` | `v1.18.0 — 7 gap-driven Tier-1 packs + capability map v2` | VERIFIED |
| gh authenticated as publisher | `gh auth status` | `jgsystemsconsulting` active; scopes include `repo` | VERIFIED |
| /tmp notes failed on Windows in Phase 9 | `9-01-SUMMARY.md:228-232` | first gh attempt `The system cannot find the file specified`; retry used phase-dir tmp | VERIFIED |
| Soft-reset was required in Phase 9 | `9-01-SUMMARY.md:221-226` | intermediate Task 1–3 commits soft-reset into `d19be1a` | VERIFIED |
| Surprise untracked must not ship | `git status --short` | `??` `.planning/phases/*/master_flow_state.json` and `.edge-coverage.json` | VERIFIED |
| Admin-bypass still in force | `.planning/STATE.md:61`; REQUIREMENTS out-of-scope | do not change protection | VERIFIED |
| MILESTONES v1.19 is still in-planning | `.planning/MILESTONES.md:25` | `## v1.19.0 (in planning — scoped 2026-08-17)` | VERIFIED |
| REL boxes open | `.planning/REQUIREMENTS.md:46-47` | REL-19-01 and REL-19-02 unchecked | VERIFIED |
| ROADMAP Phase 13 open | `.planning/ROADMAP.md:27,84` | `- [ ] **Phase 13`; `**Plans**: TBD` | VERIFIED |
</claim_verification>

<tasks>

<task type="auto" reversibility="costly">
  <name>Task 1: Release commit + annotated tag + push + GitHub Release</name>
  <files>.claude-plugin/plugin.json, .cursor-plugin/plugin.json, CHANGELOG.md, RELEASE-INFO.txt, README.md, catalog.json, docs/index.html, docs/packs.html, docs/products/website/01-jgs-se-knowledge-packs.yaml, docs/products/website/catalog.yaml, docs/capability-pack-map.json, docs/capability-map-CONTRACT.md, docs/capability-pack-map.md</files>
  <precondition>13-01 complete. `python tooling/check_release.py` PASS. `python tooling/check_capability_map.py` PASS. CHANGELOG contains exactly one `## [1.19.0]`. `gh auth status` succeeds. No local `v1.19.0` tag yet. PRE_RELEASE_HEAD recorded in 13-01-SUMMARY.md (or recoverable as the last commit before 13-01 content).</precondition>
  <read_first>.planning/phases/13-release-surface-v1-19-0/13-01-SUMMARY.md, CHANGELOG.md, RELEASE-INFO.txt</read_first>
  <reversibility rating="costly">Publishing an annotated tag + GitHub Release is hard to retract, but it is pre-approved by REL-19-02 and the Phase 9 analog (which did push). Everything before the tag is reversible. No decision checkpoint.</reversibility>
  <action>
Hard-stop if either gate is RED, if `git tag -l v1.19.0` is already present, or if `git status --short` shows surprise untracked outside `.planning/` that you cannot explain. Do not stage `master_flow_state.json`, `.edge-coverage.json`, `13-RESEARCH.md`, `13-PATTERNS.md`, `13-VALIDATION.md`, or `13-01-PLAN.md` into the release commit.

1. Re-run immediately (OneDrive sync-lag):

```bash
python tooling/check_capability_map.py
python tooling/check_release.py
python -c "import json;print(len(json.load(open('catalog.json'))['packs']))"
ls packs | wc -l
```

Expect PASS / PASS / 63 / 65.

2. Consolidate to ONE release commit (Phase 9 deviation #1, required). If 13-01 (or this session) created per-task content commits after PRE_RELEASE_HEAD:

```bash
PRE_RELEASE_HEAD=$(grep -oE '[0-9a-f]{7,40}' .planning/phases/13-release-surface-v1-19-0/13-01-SUMMARY.md | head -1)
git rev-parse --verify "$PRE_RELEASE_HEAD"
git reset --soft "$PRE_RELEASE_HEAD"
```

If HEAD is already a single untagged content tree with no extra 13-01 commits, skip the reset and just restage.

3. Stage EXPLICIT paths only. Never `git add -A`, never `git add docs/`, never `git add .`.

```bash
git status --short
git add -- .claude-plugin/plugin.json .cursor-plugin/plugin.json \
  CHANGELOG.md RELEASE-INFO.txt README.md catalog.json \
  docs/index.html docs/packs.html \
  docs/products/website/01-jgs-se-knowledge-packs.yaml \
  docs/products/website/catalog.yaml \
  docs/capability-pack-map.json docs/capability-map-CONTRACT.md
```

Add `docs/capability-pack-map.md` only if Task 1 of 13-01 actually edited it (`git diff --name-only` / `git status` after the soft-reset). Add no other files. Re-run `git status --short` and confirm the index contains only those paths.

4. Commit (this must be the last CONTENT commit on main):

```bash
git commit --no-verify -m "$(cat <<'EOF'
release(v1.19.0): Agent IO Depth — 2 packs + VV&A chapters + DA remap (63 +2 signposts)

nasa-std-8719-14 (7 ch) + is-gps-200n (6 ch); dod-vva-rpg 10->13;
IO-01 DA remap published (5/4); map_version 1.19.0 / 644; both gates PASS.
IO-05/06 deferred (AAF); IO-07 accept. Catalogue 63 (+2 signposts).
EOF
)"
```

5. Annotated tag, colon style matching live tags (not lightweight):

```bash
git tag -a v1.19.0 -m "v1.19.0: 2 IO-unlock packs + VV&A chapters + DA remap (63 +2 signposts)"
git cat-file -t "$(git rev-parse v1.19.0)"   # must print: tag
```

6. Push — Phase 9 analog did this. Admin-bypass is in force. Do not open a PR. Do not change branch protection.

```bash
git push origin main --follow-tags
```

7. GitHub Release. Title uses an em dash (public title only; CHANGELOG body stays em-dash-free). Notes = CHANGELOG `[1.19.0]` entry body (everything after the heading, before `## [1.18.0]`).

```bash
# Windows: do NOT write notes to /tmp (Phase 9 first gh attempt failed)
NOTES=.planning/phases/13-release-surface-v1-19-0/_v1.19.0-notes.tmp.md
python -c "
from pathlib import Path
cl = Path('CHANGELOG.md').read_text(encoding='utf-8')
body = cl.split('## [1.19.0]')[1].split('## [1.18.0]')[0]
# drop the heading date line if the split left it
if body.startswith(':'):
    body = body.split('\n', 1)[1]
Path(r'.planning/phases/13-release-surface-v1-19-0/_v1.19.0-notes.tmp.md').write_text(body.strip() + '\n', encoding='utf-8')
"
gh release create v1.19.0 \
  --title "v1.19.0 — Agent IO Depth (2 packs + VV&A chapters + DA remap)" \
  --notes-file .planning/phases/13-release-surface-v1-19-0/_v1.19.0-notes.tmp.md
```

8. REL-19-02 is not done until both remote checks succeed:

```bash
git ls-remote --tags origin | grep v1.19.0
gh release view v1.19.0 --json name,tagName,url
```

9. Delete the notes tmp file. Confirm it is not tracked (`git status --short` must not show it as staged). Do not commit it.
  </action>
  <verify>
    <automated>git cat-file -t "$(git rev-parse v1.19.0)" && git ls-remote --tags origin | grep v1.19.0 && gh release view v1.19.0 --json name,tagName,url && python -c "
import subprocess, json
t=subprocess.check_output(['git','cat-file','-t','v1.19.0'], text=True).strip()
assert t=='tag', t
rel=json.loads(subprocess.check_output(['gh','release','view','v1.19.0','--json','name,tagName,url,body'], text=True))
assert rel['tagName']=='v1.19.0'
assert '\u2014' in rel['name']
body=rel['body']
for token in ['IO-01','IO-02','IO-03','IO-04','IO-05','IO-06','IO-07']:
    assert token in body, token
assert 'DEFERRED' in body and 'ACCEPT' in body
print('RELEASE_OK', rel['url'])
"</automated>
  </verify>
  <acceptance_criteria>
    - last CONTENT commit subject starts with release(v1.19.0):
    - tag object type is tag (annotated), message uses colon style
    - origin has refs/tags/v1.19.0
    - GitHub Release exists; title contains an em dash; notes include IO-01..07
    - release commit file list is the explicit version/docs/catalog/README set only
    - notes tmp file deleted and untracked
  </acceptance_criteria>
  <done>REL-19-02 public act complete: annotated v1.19.0 on origin/main and GitHub Release published with competency-led notes.</done>
</task>

<task type="auto" reversibility="reversible">
  <name>Task 2: Post-release records — STATE, MILESTONES, ROADMAP, REL ticks</name>
  <files>.planning/STATE.md, .planning/MILESTONES.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <precondition>Task 1 done: `git ls-remote --tags origin | grep v1.19.0` and `gh release view v1.19.0` both succeed. Do not start this task before the tag exists.</precondition>
  <read_first>.planning/STATE.md, .planning/MILESTONES.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <action>
After the tag exists. These files are NOT in the release commit.

1. STATE.md — shipped record:
   - Record v1.19.0 shipped with release commit SHA, annotated tag, and GitHub Release URL (`gh release view v1.19.0 --json url -q .url`).
   - Close REL-19-01/02 in the narrative (boxes themselves live in REQUIREMENTS).
   - Carry remaining backlog explicitly: FUT-04 Army CBA retry; FUT-05 deterministic generator; 7-CODE-REVIEW IN-02 overlap checker; AAF still deferred (IO-05/06); IO-07 accept (no pack); ROSAP optional; se-agents consumer refresh stays in the sibling repo.
   - Update current focus / current position to shipped v1.19.0 (this is the last v1.19 phase).

2. MILESTONES.md — convert `## v1.19.0 (in planning — scoped 2026-08-17)` into a shipped record matching the v1.18.0 entry shape (prose + release commit + annotated tag + GitHub Release URL bullets). Honest scope: 2 new packs + leftover RPG chapters + DA remap + map 644 + dual-gate wire already landed in Phase 12; do not claim 7 new packs or IO-05/06/07 builds.

3. ROADMAP.md:
   - Tick Phase 13 checkbox (line 27 `- [ ] **Phase 13` → `- [x]`).
   - Fill `**Plans**: TBD` under Phase 13 Details with `13-01-PLAN.md` and `13-02-PLAN.md`.

4. REQUIREMENTS.md:
   - Tick `REL-19-01` and `REL-19-02`.
   - Optional (not a blocker): refresh the IO-01 parenthetical "Live count leave-2 is Phase 12" to the live DA 5 entries / 4 packs. Do not silently tick VET/IO/MAP/HYG boxes.

5. Do not edit PROJECT.md unless you already have it open and the v1.18.0 / 628 prose is a one-line lie next to the shipped record — default is leave it (not a gate surface).

6. Separate `.planning`-only commit, explicit paths:

```bash
git add -- .planning/STATE.md .planning/MILESTONES.md .planning/ROADMAP.md .planning/REQUIREMENTS.md
git status --short
git commit --no-verify -m "docs(phase-13): record v1.19.0 shipped + tick REL-19"
```

The release commit must remain the last CONTENT commit (this follow-up touches only `.planning/`).
  </action>
  <verify>
    <automated>python -c "
from pathlib import Path
s=Path('.planning/STATE.md').read_text(encoding='utf-8')
assert 'v1.19.0' in s
m=Path('.planning/MILESTONES.md').read_text(encoding='utf-8')
assert 'in planning' not in m.split('## v1.19.0')[1].split('##')[0].split('\n')[0].lower() or 'shipped' in m.split('## v1.19.0')[1][:400].lower()
assert 'releases/tag/v1.19.0' in m
r=Path('.planning/ROADMAP.md').read_text(encoding='utf-8')
assert '- [x] **Phase 13' in r
assert '13-01-PLAN.md' in r and '13-02-PLAN.md' in r
q=Path('.planning/REQUIREMENTS.md').read_text(encoding='utf-8')
assert '- [x] **REL-19-01**' in q and '- [x] **REL-19-02**' in q
print('RECORDS_OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - STATE has SHA + tag + GitHub Release URL and carries FUT-04 / FUT-05 / IN-02 / AAF deferral
    - MILESTONES v1.19.0 is a shipped entry, not in-planning
    - ROADMAP Phase 13 is [x] and lists both plan files
    - REL-19-01 and REL-19-02 are [x]
    - follow-up commit is .planning-only; tagged tree is unchanged
  </acceptance_criteria>
  <done>Shipped records landed in a separate .planning commit. REL-19-01/02 ticked. Phase 13 planning close-out complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → public GitHub | Tag + GitHub Release are the public release act; errors are visible and hard to retract |
| working tree → release commit | Untracked strays and .planning research files must not cross the tag |
| CHANGELOG → release notes | Notes body ships publicly; slug-only or IO-05-built claims mislead consumers |
| release commit → .planning commit | Records must not rewrite the tagged tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-07 | Tampering | Release commit staging | high | mitigate | Explicit-path-only `git add`; `git status --short` audit; STOP on surprise untracked |
| T-13-08 | Repudiation | Tag provenance | medium | mitigate | Annotated tag (`git cat-file -t` == tag); colon-style message matching v1.17/v1.18 |
| T-13-09 | Tampering | OneDrive / Windows notes path | medium | mitigate | Re-run check_release immediately before commit; notes file under phase dir, never `/tmp` |
| T-13-10 | DoS (of release) | Public release on red gate | high | mitigate | Precondition + step 1 hard-stop on non-PASS |
| T-13-11 | Elevation of privilege | Branch protection | low | accept | User opted to keep admin-bypass; do not change it |
| T-13-12 | Information disclosure | Notes tmp left committed | low | mitigate | Delete `_v1.19.0-notes.tmp.md`; never stage it |
</threat_model>

## Artifacts

| Artifact | Produced by | Consumed by |
|----------|-------------|-------------|
| Single `release(v1.19.0)` content commit | Task 1 (soft-reset + explicit add) | annotated tag |
| Annotated tag `v1.19.0` | Task 1 | origin + GitHub Release |
| `git push origin main --follow-tags` | Task 1 | `git ls-remote` / REL-19-02 |
| GitHub Release v1.19.0 | Task 1 (`gh release create`) | consumers / MILESTONES URL |
| Phase-dir notes tmp (deleted) | Task 1 | `gh --notes-file` only |
| STATE / MILESTONES / ROADMAP / REL ticks | Task 2 | milestone close |
| Separate `.planning` commit | Task 2 | does not disturb tagged tree |

<verification>
1. `git cat-file -t v1.19.0` → `tag`; `git ls-remote --tags origin | grep v1.19.0` hits.
2. `gh release view v1.19.0` → name uses em dash; notes include IO-01..07 and do not claim IO-05/06 built.
3. `git log --oneline -3` → release commit then .planning-only follow-up (or release commit as HEAD if records commit is checked separately); no stray files in `git show --stat` of the release commit.
4. REQUIREMENTS REL-19-01 and REL-19-02 are `[x]`; ROADMAP Phase 13 is `[x]`.
5. Both gates still PASS on the tagged tree (REL-19-01 still true after publish).
</verification>

<success_criteria>
- REL-19-02: annotated tag v1.19.0 on origin; GitHub Release published with CHANGELOG-derived competency-led notes.
- REL-19-01 still true on the tagged tree: both gates PASS at 63/65; leftovers closed in 13-01.
- Release commit is the last content commit; .planning records are a follow-up.
- Backlog (FUT-04, FUT-05, IN-02, AAF deferral) carried in STATE.
</success_criteria>

<output>
Create `.planning/phases/13-release-surface-v1-19-0/13-02-SUMMARY.md` when done. Include the release SHA, tag, GitHub Release URL, and a `## Deviations` section.
</output>
