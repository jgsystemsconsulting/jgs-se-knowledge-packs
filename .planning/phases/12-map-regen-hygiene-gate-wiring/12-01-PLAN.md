---
phase: 12-map-regen-hygiene-gate-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/capability-pack-map.json
  - docs/capability-pack-map.md
  - docs/capability-map-CONTRACT.md
  - tooling/check_capability_map.py
autonomous: true
requirements: [MAP-19-01, MAP-19-02, MAP-19-03, MAP-19-05]
estimate:
  tokens: 80000
  raw_tokens: 40000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "check_capability_map.py exits 0 against the regenerated map (staleness 0, uniqueness, envelope, floors)"
    - "docs/capability-pack-map.json keeps schema_version==2 and map_version=='1.18.0'; generated_on is the execute-day ISO date"
    - "All 63 chapter-bearing packs appear in at least one map entry; on_disk_only=0 and map_only=0"
    - "MAP-19-03 is a MOVE: federal-bca ch04+ch06 and dod-vva-rpg ch06 sit only in Decision Analysis & Trade Studies (cluster 16); Opportunity still has federal-bca ch01–ch03, ch05 + 3 support files; dod-vva-rpg ch08 stays Validation; ch10 stays Risk"
    - "Decision Analysis & Trade Studies = 5 entries / 4 packs after the three-row move (nasa-ceh + nasa-se-handbook + federal-bca + dod-vva-rpg)"
    - "MAP-19-02: name-keyed THRESHOLDS >=4 for Decision Analysis, Validation, Integration, Interface Management & ICIDs (raise 3->4), Operations/Maintenance & Disposal; existing Training/Traceability/Opportunity floors not weakened"
    - "None of the five listed primaries is (count < 4 AND n_packs == 1). Integration stays 4/4 — do not invent an Integration pack or raid other clusters"
    - "CONTRACT.md has one paragraph: live snapshot 628+ (post-regen 644 if 16 adds / 0 support); 502 is residue; Cybersecurity (69/10) + Digital Engineering (25/4) remain unbound (se-agents-side)"
    - "capability-pack-map.md summary table + a v1.19 changelog bullet synced to JSON counts; json+md committed together"
    - "No generator script; no plugin/CHANGELOG/RELEASE-INFO/tag bump; no catalog dod-vva-rpg.chapters 10->13; no README new-slug rows; no AAF/CBA/DoDM/stakeholder packs; no Cyber/DE bindings; no URLs in SOURCE-VETTING"
    - "EDGE_ABSENT=1: no SPEC. Unclassified stay unresolved assumptions. 16 new-chapter placements are agent judgment (hints, not locked). Remap rows are locked — do not re-guess"
    - "Idempotency: re-run updates the same JSON/md/THRESHOLDS/CONTRACT in place; does not duplicate (pack, chapter) rows"
    - "Concurrency: stay on main; exclusive ownership of map json/md + CONTRACT + check_capability_map.py. 12-02 must not start until this plan's map gate is GREEN"
  artifacts:
    - path: docs/capability-pack-map.json
      provides: "v2 envelope regen + 16 classified chapters + 3-row MOVE"
      contains: '"map_version": "1.18.0"'
    - path: docs/capability-pack-map.md
      provides: "summary counts + v1.19 changelog bullet (new slugs + remap + leftover RPG)"
    - path: docs/capability-map-CONTRACT.md
      provides: "MAP-19-05 paragraph (628+ / 502 residue / Cyber+DE unbound)"
      contains: "502"
    - path: tooling/check_capability_map.py
      provides: "MAP-19-02 name-keyed THRESHOLDS >=4 for listed primaries"
      contains: '"Decision Analysis & Trade Studies"'
  key_links:
    - from: tooling/check_capability_map.py
      to: docs/capability-pack-map.json
      via: "gate reads committed JSON + packs/ filesystem — staleness is live"
      pattern: "MAP_PATH"
    - from: docs/capability-pack-map.json
      to: packs/nasa-std-8719-14/chapters/
      via: "every on-disk chapter is exactly one cluster entry"
      pattern: "nasa-std-8719-14"
    - from: docs/capability-map-CONTRACT.md
      to: docs/capability-pack-map.md
      via: "existing consumption-contract link; MAP-19-05 lives in CONTRACT"
      pattern: "capability-map-CONTRACT.md"

# Prohibitions (must-NOT). No check_kind / check_target fields.
# - must-NOT bump version to 1.19 (plugin / CHANGELOG [1.19.0] / RELEASE-INFO / map_version / tag)
# - must-NOT invent packs (AAF / Army CBA / stakeholder / dodm-5000-102 / SP-7084 / IS-300)
# - must-NOT leave remap as copy (must MOVE — delete old cluster membership)
# - must-NOT put URLs in SOURCE-VETTING
# - must-NOT steal REL-19 (catalog dod-vva-rpg.chapters, README live-pack rows, GitHub Release)
# - must-NOT write a map generator (FUT-05 stays deferred)
# - must-NOT bind Cyber/DE clusters
# - must-NOT raid Integration or collapse Opportunity below floor
# - must-NOT wire check_release.py in this plan (12-02)
# - must-NOT check MAP-19 boxes (verify does that)
---

<objective>
Wave A (12-RESEARCH recommended shape): regenerate the capability-pack map by classifying the 16 unmapped Phase 11 chapters, apply the locked MAP-19-03 three-row MOVE into Decision Analysis, encode MAP-19-02 name-keyed floors, and add the MAP-19-05 CONTRACT paragraph.

Purpose: make `check_capability_map.py` GREEN on the post-Phase-11 tree without stealing Phase 13 version/catalog work or inventing packs. Output: committed JSON+md, extra THRESHOLDS, CONTRACT paragraph. Gate stays standalone until 12-02.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-map-regen-hygiene-gate-wiring/12-RESEARCH.md
@.planning/phases/12-map-regen-hygiene-gate-wiring/12-PATTERNS.md
@.planning/phases/11-io-unlocking-packs-decision-analysis-remap/11-02-SUMMARY.md
@.planning/phases/8-agent-enablement-surface/8-01-PLAN.md
@docs/capability-pack-map.json
@docs/capability-pack-map.md
@docs/capability-map-CONTRACT.md
@tooling/check_capability_map.py
@packs/nasa-std-8719-14/SKILL.md
@packs/is-gps-200n/SKILL.md
@packs/dod-vva-rpg/SKILL.md
</context>

<plan_notes>
Discuss was skipped (no CONTEXT.md). Locked decisions come from 12-RESEARCH user_constraints + Phase 11 remap table. Do not re-guess MAP-19-03 chapters. Do not re-vet Phase 10.

TRACER_MODE: Task 1 is the production-quality end-to-end slice — classify 16 + MOVE 3 + sync md + run the existing gate until GREEN. Tasks 2–3 encode the floor and the consumer paragraph against that already-green map.

REVERSIBILITY: revert the scoped commits; schema stays v2; no version surfaces touched.

EDGE_ABSENT=1: no SPEC. The 16 new-chapter cluster assignments are unclassified judgment (hints in 12-RESEARCH). Remap rows are locked. Unclassified stay unresolved assumptions — do not invent check_kind / check_target.

Stay on main. No branches / worktrees. Use `python`.

## Deviations ledger
Executor MUST keep a `## Deviations` section in 12-01-SUMMARY.md. Record every departure from this plan (classification that disagrees with a hint, extra support-file rows, count != 644, extra files touched). If none: write `None.` Do not invent silent scope.
</plan_notes>

<claim_verification>
Live commands run this planner session (2026-08-17), cwd repo root. Do not invent replacements.

| claim | command | observed | status |
|---|---|---|---|
| Branch is main | `git branch --show-current` | `main` | VERIFIED |
| Map gate RED, 19 issues | `python tooling/check_capability_map.py`; echo exit | exit 1; `FAIL: 19 issue(s)`; packs `is-gps-200n` + `nasa-std-8719-14` not in map; `on_disk_only=16, map_only=0`; 16 named chapter files listed | VERIFIED |
| Release gate PASS and does not import the map gate | `python tooling/check_release.py`; python `'check_capability_map' in check_release.py` | `RELEASE CHECK: PASS — repo is release-ready against the mechanical gate.`; `imports_map False` | VERIFIED |
| Envelope still Phase 8 | python json load | `schema_version` 2, `map_version` `1.18.0`, `generated_on` `2026-08-17`; 32 clusters; 628 entries; 61 mapped packs; 63 disk chapter-bearing packs | VERIFIED |
| Disk not mapped | python set-diff packs-with-chapters vs map packs | `['is-gps-200n', 'nasa-std-8719-14']`; map-not-disk `[]` | VERIFIED |
| Chapter files on disk | `ls packs/{nasa-std-8719-14,is-gps-200n,dod-vva-rpg}/chapters \| wc -l` | 7 / 6 / 13 | VERIFIED |
| Listed-primary live counts (pre-regen) | python (count, n_packs) | DA 2/2; Validation 5/4; Integration 4/4; Interfaces 4/3; Ops/Maint 6/4; Opportunity 10/2; Assurance 9/4 | VERIFIED |
| MAP-19-03 current membership | python walk of (pack, chapter) | federal-bca ch04+ch06 → Opportunity; dod-vva-rpg ch06 → Assurance; ch08 → Validation; ch10 → Risk; federal-bca ch01–ch03, ch05 + 3 support files still Opportunity | VERIFIED |
| DA today is only two rows | python print DA chapters | `nasa-ceh`/`ch06-nasa-ceh-decision-support-analyses.md`; `nasa-se-handbook`/`ch34-6-8-decision-analysis.md` | VERIFIED |
| Cyber/DE unbound counts (do not bind) | python | Cybersecurity & Security Engineering 69/10; Digital Engineering & Digital Twins 25/4 | VERIFIED |
| Version trio still 1.18.0; only v1.18.0 tag | plugin/CHANGELOG/RELEASE-INFO + `git tag -l` | plugin 1.18.0; CHANGELOG `## [1.18.0]`; RELEASE-INFO 1.18.0; tag `v1.18.0` only (no v1.19*) | VERIFIED |
| Catalog leftover is Phase 13 | python catalog slugs | `dod-vva-rpg chapters 10`; nasa-std-8719-14 + is-gps-200n already thin-registered; n_catalog 63 | VERIFIED |
| THRESHOLDS today | `tooling/check_capability_map.py:34-39` | Training 1; Traceability 3; Interfaces 3; Opportunity 2. No DA/Validation/Integration/Ops keys | VERIFIED |
| CONTRACT still says standalone | `docs/capability-map-CONTRACT.md:81-82` | `The gate is standalone (not wired into check_release.py in this milestone;` | VERIFIED |
| Link policy | `grep -c http docs/SOURCE-VETTING.md` | `0` | VERIFIED |
| CI never execs repo Python | `.github/workflows/validate.yml:4-6` | `never executes checked-out repository code` | VERIFIED |
</claim_verification>

<tasks>

<task type="auto" reversibility="easy">
  <name>Task 1: Agent-classify 16 chapters + MAP-19-03 MOVE + sync md (MAP-19-01/03)</name>
  <files>docs/capability-pack-map.json, docs/capability-pack-map.md</files>
  <read_first>.planning/phases/12-map-regen-hygiene-gate-wiring/12-RESEARCH.md, .planning/phases/11-io-unlocking-packs-decision-analysis-remap/11-02-SUMMARY.md, docs/capability-pack-map.md, docs/capability-pack-map.json, packs/nasa-std-8719-14/SKILL.md, packs/is-gps-200n/SKILL.md, packs/dod-vva-rpg/SKILL.md</read_first>
  <action>
Agent pass — not a script. Do not create a generator. Analog: Phase 8 Task 2 (8-01-PLAN.md) plus 12-RESEARCH Pattern 1 + Pattern 2.

1. Read each of the 16 unmapped chapter files (titles/leads suffice) and the pack SKILL.md When-to-use. Assign every chapter to exactly one of the 32 existing cluster names in `docs/capability-pack-map.json`. Follow `docs/capability-pack-map.md` rules of construction: process definitions → "Standards, Tailoring & Process Models"; performing the capability → that capability cluster; cross-cutting chapters keep a secondary-fit remark in `note`. Every new entry is `{pack, chapter, note}` like existing rows.

   Hints (judgment, not locked — spot-check the chapter before assigning; do not force-fit):

   `nasa-std-8719-14` (IO-03 → Ops/Maint/Disposal primary). Omit support files (multi-cluster if ch04 leans Safety or ch07 leans Governance).
   - ch01-scope-and-applicability.md → Operations, Maintenance & Disposal (or Standards if read as process-definition)
   - ch02-assessment-overview.md → Operations, Maintenance & Disposal
   - ch03-debris-released-normal-operations.md → Operations, Maintenance & Disposal
   - ch04-explosions-breakups-collisions.md → Operations, Maintenance & Disposal; note Safety secondary
   - ch05-postmission-disposal.md → Operations, Maintenance & Disposal (primary gold)
   - ch06-reentry-surviving-debris.md → Operations, Maintenance & Disposal
   - ch07-special-classes-odar-eomp.md → Operations, Maintenance & Disposal; note Governance secondary

   `is-gps-200n` (IO-04 → Interface Management & ICIDs primary). Omit support files (ch01 is CM/change-control). Complementary to `faa-std-025`.
   - ch01-is-scope-and-change-control.md → Configuration Management & Baselines (IRN/CCB) **or** Interface Management & ICIDs if read as "what an ICD is"
   - ch02-interface-definition-and-identification.md → Interface Management & ICIDs
   - ch03-interface-criteria-pattern.md → Interface Management & ICIDs
   - ch04-nav-data-as-payload.md → Interface Management & ICIDs
   - ch05-time-and-definition-hygiene.md → Interface Management & ICIDs
   - ch06-appendices-as-a-map.md → Interface Management & ICIDs (or Standards if "how the IS is structured")
   Do not dump into Requirements Traceability just because the Topic Index mentions it.

   `dod-vva-rpg` leftovers (IO-02 chapters-not-a-pack):
   - ch11-te-vv-checklist.md → Test & Evaluation **or** Verification. Do **not** force Integration (IO-05 is AAF, not this checklist).
   - ch12-developing-the-referent.md → Validation
   - ch13-conceptual-model-development-and-validation.md → Validation

2. Apply the locked MAP-19-03 MOVE (11-02-SUMMARY table — AUTHORITATIVE, do not re-pick). Uniqueness is on `(pack, chapter)` across all clusters. Copying without deleting the old row fails the gate.

   | Pack | Chapter | Delete from (live today) | Insert into |
   |---|---|---|---|
   | federal-bca | ch04-uncertainty-and-sensitivity.md | Opportunity/Benefit Management (json ~1105-1107) | Decision Analysis & Trade Studies |
   | federal-bca | ch06-reporting-and-decision-use.md | Opportunity/Benefit Management (json ~1115-1117) | Decision Analysis & Trade Studies |
   | dod-vva-rpg | ch06-accreditation-agent-role.md | Assurance & System Assurance (json ~3260-3262) | Decision Analysis & Trade Studies |

   Leave in place: federal-bca ch01–ch03, ch05 + `cheatsheet.md (support file)` / `glossary.md (support file)` / `patterns.md (support file)` in Opportunity (10→8). Leave dod-vva-rpg ch08-validation-fundamentals.md in Validation. Leave ch10-risk-and-vva.md in Risk. Do not move ch01–ch03/ch05 or any support file.

   After the move Decision Analysis must be exactly these four packs / five chapters:
   - nasa-ceh / ch06-nasa-ceh-decision-support-analyses.md (already there)
   - nasa-se-handbook / ch34-6-8-decision-analysis.md (already there)
   - federal-bca / ch04-uncertainty-and-sensitivity.md (moved)
   - federal-bca / ch06-reporting-and-decision-use.md (moved)
   - dod-vva-rpg / ch06-accreditation-agent-role.md (moved)

3. Envelope: keep `"schema_version": 2` (int) and `"map_version": "1.18.0"`. Set `"generated_on"` to the execute-day ISO date (`YYYY-MM-DD`). Do not invent schema 3. Do not write `"1.19.0"` on map_version.

4. Omit support-file rows for `nasa-std-8719-14` and `is-gps-200n` unless classification proves a pack is essentially single-cluster (research recommendation: omit). Expected arithmetic if 16 chapter adds and 0 new support rows: **628 + 16 = 644** entries, 63 chapter-bearing packs mapped. Opportunity 10→8. Decision Analysis 2→5. Assurance 9→8. Other deltas follow classification.

5. Sync `docs/capability-pack-map.md`: refresh the Summary table counts to match JSON; add one changelog bullet for v1.19 (new slugs `nasa-std-8719-14` + `is-gps-200n`, leftover RPG ch11–ch13, three-row remap). Keep the existing v1.18.0 / v1.17.0 lines (single-line convention). Do not rewrite the per-cluster human tables unless a moved/added chapter would leave them lying — at minimum the Decision Analysis, Opportunity, Assurance, Validation, Interfaces, and Ops/Maint chapter tables must match JSON.

6. Spot-check protocol (mandatory): for EACH new pack plus leftover RPG, re-read SKILL.md When-to-use vs assigned clusters. Record 3 chapters per new pack (and all 3 leftover RPG chapters) with one-line rationale in the task summary. Remap chapters are **not** judgment — they are locked.

7. Run `python tooling/check_capability_map.py`. It will still fail on MAP-19-02 floors until Task 2 adds THRESHOLDS — that is expected if you add the thresholds first. Prefer: finish JSON+md in this task, then Task 2 adds THRESHOLDS and re-runs to GREEN. If you add THRESHOLDS in the same session before committing this task, still keep the THRESHOLDS edit in Task 2's commit.

8. Commit only `docs/capability-pack-map.json` + `docs/capability-pack-map.md` (`docs(map): regen v1.19 chapters + MAP-19-03 Decision Analysis move`). `git show --name-only` must have zero `catalog.json`, `README.md`, `.claude-plugin/`, `CHANGELOG.md`, `RELEASE-INFO.txt`, `tooling/check_release.py`, and no new `tooling/*generator*`.
  </action>
  <verify>
    <automated>python -c "import json,pathlib; d=json.load(open('docs/capability-pack-map.json',encoding='utf-8')); assert d['schema_version']==2 and d['map_version']=='1.18.0' and d['generated_on']; packs={e['pack'] for c in d['clusters'] for e in c['chapters']}; disk={p.name for p in pathlib.Path('packs').iterdir() if (p/'chapters').exists()}; assert not (disk-packs), disk-packs; assert not (packs-disk), packs-disk; pairs=[(e['pack'],e['chapter']) for c in d['clusters'] for e in c['chapters']]; assert len(pairs)==len(set(pairs)), 'duplicate (pack,chapter)'; by={c['name']:c for c in d['clusters']}; da=[(e['pack'],e['chapter']) for e in by['Decision Analysis & Trade Studies']['chapters']]; want={('nasa-ceh','ch06-nasa-ceh-decision-support-analyses.md'),('nasa-se-handbook','ch34-6-8-decision-analysis.md'),('federal-bca','ch04-uncertainty-and-sensitivity.md'),('federal-bca','ch06-reporting-and-decision-use.md'),('dod-vva-rpg','ch06-accreditation-agent-role.md')}; assert set(da)==want, (set(da),want); opp={(e['pack'],e['chapter']) for e in by['Opportunity/Benefit Management']['chapters']}; assert ('federal-bca','ch04-uncertainty-and-sensitivity.md') not in opp and ('federal-bca','ch06-reporting-and-decision-use.md') not in opp; assert {('federal-bca','ch01-purpose-scope-and-principles.md'),('federal-bca','ch02-identifying-measuring-benefits-costs.md'),('federal-bca','ch03-discounting-and-time.md'),('federal-bca','ch05-distributional-effects-and-incidence.md')} <= opp; ass={(e['pack'],e['chapter']) for e in by['Assurance & System Assurance']['chapters']}; assert ('dod-vva-rpg','ch06-accreditation-agent-role.md') not in ass; val={(e['pack'],e['chapter']) for e in by['Validation']['chapters']}; assert ('dod-vva-rpg','ch08-validation-fundamentals.md') in val; risk={(e['pack'],e['chapter']) for e in by['Risk Management']['chapters']}; assert ('dod-vva-rpg','ch10-risk-and-vva.md') in risk; integ=by['Integration']['chapters']; assert len(integ)==4 and len({e['pack'] for e in integ})==4; print('MAP_MOVE_OK', sum(len(c['chapters']) for c in d['clusters']), 'entries', len(packs), 'packs', 'generated_on', d['generated_on'])" && grep -q "nasa-std-8719-14" docs/capability-pack-map.md && grep -q "is-gps-200n" docs/capability-pack-map.md && test ! -e tooling/generate_capability_map.py && test ! -e tooling/gen_capability_map.py && python -c "import json; c=json.load(open('catalog.json',encoding='utf-8')); p=next(x for x in c['packs'] if x['slug']=='dod-vva-rpg'); assert p['chapters']==10, p['chapters']" && grep -c http docs/SOURCE-VETTING.md | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - schema_version is int 2; map_version is exactly "1.18.0"; generated_on is execute-day ISO
    - 63 chapter-bearing packs mapped; on_disk_only=0; map_only=0; no duplicate (pack, chapter)
    - DA membership is exactly the five locked/pre-existing rows (5 entries / 4 packs)
    - Opportunity still has federal-bca ch01–ch03, ch05 + 3 support files; ch04/ch06 gone from Opportunity
    - dod-vva-rpg ch06 gone from Assurance; ch08 still Validation; ch10 still Risk
    - Integration still 4/4 — no new Integration slug, no raided chapters
    - nasa-std-8719-14 and is-gps-200n chapters each appear once; support files omitted unless pack is single-cluster
    - capability-pack-map.md summary + v1.19 changelog bullet name the new slugs and remap
    - no generator script; catalog dod-vva-rpg.chapters still 10; SOURCE-VETTING http count 0
    - scoped commit is json+md only
  </acceptance_criteria>
  <done>Map JSON+md regenerated: 16 chapters classified, three-row MOVE applied, envelope 2 / 1.18.0 / execute-day, uniqueness holds, md synced. Committed.</done>
</task>

<task type="auto" reversibility="easy">
  <name>Task 2: MAP-19-02 THRESHOLDS >=4 + conjunct verify print</name>
  <files>tooling/check_capability_map.py</files>
  <read_first>tooling/check_capability_map.py, docs/capability-pack-map.json, .planning/phases/12-map-regen-hygiene-gate-wiring/12-RESEARCH.md</read_first>
  <action>
Encode the MAP-19-02 floor in the existing name-keyed THRESHOLDS dict. Do not add a second schema field. Do not resolve by array index.

1. In `tooling/check_capability_map.py` THRESHOLDS (today lines 34-39), **keep** existing floors:
   - "Training & Documentation Delivery": 1
   - "Requirements Traceability & Allocation": 3
   - "Opportunity/Benefit Management": 2

2. **Raise** "Interface Management & ICIDs" from 3 to **4**.

3. **Add** (name-keyed, exact cluster strings as they appear in the JSON):
   - "Decision Analysis & Trade Studies": 4
   - "Validation": 4
   - "Integration": 4
   - "Operations, Maintenance & Disposal": 4

   Integration already sits at 4/4. Encoding ≥4 preserves the floor without inventing a pack. Do **not** raid other clusters to "move" Integration. Record in the summary: "floor held; AAF still deferred; no raid."

4. Do not weaken any existing floor. Do not add a pack-count dimension to the gate — the "AND 1 pack" half is a one-shot verify print, not a committed tool.

5. Run `python tooling/check_capability_map.py`. Must print all 32 cluster counts + TOTAL and exit 0 (`PASS: capability map OK`). Then run it a second time and confirm stdout is identical (idempotent). If a listed-primary threshold fails, return to Task 1 assignments — do **not** weaken THRESHOLDS.

6. One-shot conjunct print (not a new file). Fail the task if any listed primary is `(count < 4 and n_packs == 1)`:

```python
import json
data = json.load(open("docs/capability-pack-map.json", encoding="utf-8"))
want = [
    "Decision Analysis & Trade Studies",
    "Validation",
    "Integration",
    "Interface Management & ICIDs",
    "Operations, Maintenance & Disposal",
]
by = {c["name"]: c for c in data["clusters"]}
bad = False
for name in want:
    ch = by[name]["chapters"]
    packs = {e["pack"] for e in ch}
    fail = len(ch) < 4 and len(packs) == 1
    print(f"{name}: {len(ch)} entries / {len(packs)} packs  floor_fail={fail}")
    bad = bad or fail
raise SystemExit(1 if bad else 0)
```

   Expect DA `5 entries / 4 packs  floor_fail=False`. Paste the five lines into the summary.

7. Commit only `tooling/check_capability_map.py` (`fix(tooling): MAP-19-02 listed-primary floors >=4`). Do not touch check_release.py. Do not change the map-gate docstring "Standalone — not imported…" yet (12-02 owns the wire + docstring).
  </action>
  <verify>
    <automated>python tooling/check_capability_map.py && python -c "import pathlib,re; t=pathlib.Path('tooling/check_capability_map.py').read_text(encoding='utf-8'); m=re.search(r'THRESHOLDS: dict\[str, int\] = \{([^}]+)\}', t, re.S); body=m.group(1); assert '\"Training & Documentation Delivery\": 1' in body; assert '\"Requirements Traceability & Allocation\": 3' in body; assert '\"Opportunity/Benefit Management\": 2' in body; assert '\"Interface Management & ICIDs\": 4' in body; assert '\"Decision Analysis & Trade Studies\": 4' in body; assert '\"Validation\": 4' in body; assert '\"Integration\": 4' in body; assert '\"Operations, Maintenance & Disposal\": 4' in body; assert '\"Interface Management & ICIDs\": 3' not in body; print('THRESHOLDS_OK')" && python -c "import json; data=json.load(open('docs/capability-pack-map.json',encoding='utf-8')); want=['Decision Analysis & Trade Studies','Validation','Integration','Interface Management & ICIDs','Operations, Maintenance & Disposal']; by={c['name']:c for c in data['clusters']}; bad=False
for name in want:
    ch=by[name]['chapters']; packs={e['pack'] for e in ch}; fail=len(ch)<4 and len(packs)==1; print(f'{name}: {len(ch)} entries / {len(packs)} packs  floor_fail={fail}'); bad=bad or fail
raise SystemExit(1 if bad else 0)" && python -c "from pathlib import Path; t=Path('tooling/check_release.py').read_text(encoding='utf-8'); assert 'check_capability_map' not in t"</automated>
  </verify>
  <acceptance_criteria>
    - THRESHOLDS contains the four new >=4 keys plus Interfaces raised 3->4
    - Training 1 / Traceability 3 / Opportunity 2 unchanged
    - check_capability_map.py exits 0; second run stdout identical
    - one-shot print: none of the five primaries is floor_fail=True; DA is 5/4
    - Integration still 4 entries / 4 packs; no new pack slug
    - check_release.py still does not import the map gate
  </acceptance_criteria>
  <done>MAP-19-02 floors encoded; gate GREEN twice; conjunct print recorded; Integration not raided. Committed.</done>
</task>

<task type="auto" reversibility="easy">
  <name>Task 3: MAP-19-05 CONTRACT paragraph (628+ / 502 residue / Cyber+DE unbound)</name>
  <files>docs/capability-map-CONTRACT.md</files>
  <read_first>docs/capability-map-CONTRACT.md, docs/capability-pack-map.json, docs/capability-pack-map.md</read_first>
  <action>
Add **one paragraph** to `docs/capability-map-CONTRACT.md` (MAP-19-05). Pack-side contract only. Do not edit ROLE-AGENTS-REQUIREMENTS-V2. Do not bind Cyber/DE.

1. Insert a short section (recommended: new `## 6. Live snapshot vs residue` after the existing §5 threshold table, or a paragraph at the end of §1/§2 if that reads cleaner). The paragraph MUST state all three facts:
   - The live committed snapshot is **628+** chapter entries (cite the post-regen TOTAL from Task 1 — expected **644** if 16 adds / 0 support).
   - The **502** figure is residue (historical ROLE-AGENTS-REQUIREMENTS-V2 draft count). Consumers must read the live JSON, not 502.
   - **Cybersecurity & Security Engineering** (live 69 entries / 10 packs) and **Digital Engineering & Digital Twins** (live 25 entries / 4 packs) remain **unbound**. Binding those clusters is se-agents-side work, not this milestone.

2. Update the §5 threshold table to match Task 2 THRESHOLDS (Interfaces ≥4; add DA/Validation/Integration/Ops ≥4). Do not claim the gate is wired into check_release — that sentence stays until 12-02.

3. Do not bump the example envelope `map_version` in §1 to 1.19.0. You MAY refresh `generated_on` in the example to the execute day so it does not contradict the live JSON.

4. Confirm `docs/capability-pack-map.md` still links to CONTRACT (already present). Do not add Cyber/DE binding tables.

5. Confirm `grep -c http docs/SOURCE-VETTING.md` is still 0. Do not put URLs in SOURCE-VETTING.

6. Commit only `docs/capability-map-CONTRACT.md` (`docs(contract): MAP-19-05 live 628+ / 502 residue / Cyber-DE unbound`). Write `12-01-SUMMARY.md` per the summary template: red gate start state, classification table for 16 chapters, MOVE confirmation, DA 5/4, conjunct print, CONTRACT paragraph quote, Phase 13 leftovers not stolen, `## Deviations` ledger.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('docs/capability-map-CONTRACT.md').read_text(encoding='utf-8'); assert '628' in t; assert '502' in t; assert 'Cybersecurity' in t; assert 'Digital Engineering' in t; assert 'unbound' in t.lower() or 'not bound' in t.lower() or 'remain unbound' in t.lower(); print('CONTRACT_PARAGRAPH_OK')" && grep -q "capability-map-CONTRACT.md" docs/capability-pack-map.md && python tooling/check_capability_map.py && python -c "import json; d=json.load(open('docs/capability-pack-map.json',encoding='utf-8')); assert d['map_version']=='1.18.0'; p=json.load(open('.claude-plugin/plugin.json')); assert p['version']=='1.18.0'" && grep -c http docs/SOURCE-VETTING.md | grep -qx 0 && test ! -e .planning/phases/12-map-regen-hygiene-gate-wiring/12-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - CONTRACT contains 628, 502, Cybersecurity, Digital Engineering, and an unbound/not-bound statement
    - §5 threshold table matches the new floors
    - §4 still says the gate is standalone (12-02 will flip that)
    - map_version still 1.18.0; plugin still 1.18.0
    - map gate still PASS; SOURCE-VETTING http count 0
    - no 12-CONTEXT.md invented; 12-01-SUMMARY.md written with Deviations ledger
  </acceptance_criteria>
  <done>MAP-19-05 paragraph landed; floors documented; gate still GREEN standalone; summary + Deviations recorded. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packs/ on disk -> map JSON | agent classification is the only writer; uniqueness + staleness gate is the backstop |
| map JSON -> se-agents consumer | public contract; shape regressions and silent 502 residue mislead the generator |
| remap rows -> Decision Analysis | copy-not-move would ship a duplicate (pack, chapter) and fail uniqueness |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | docs/capability-pack-map.json | medium | mitigate | Gate validates envelope, file existence, bidirectional staleness, uniqueness (Task 1 verify) |
| T-12-02 | Tampering | MAP-19-03 remap | medium | mitigate | Membership assert: three chapters exist only in DA; old clusters do not contain them (Task 1 verify) |
| T-12-03 | Elevation | Integration / invented packs | medium | mitigate | Integration 4/4 assert; no new slug; THRESHOLDS Integration >=4 without raid (Task 2) |
| T-12-04 | Information disclosure | CONTRACT Cyber/DE | low | mitigate | Document unbound; do not add binding tables (Task 3) |
| T-12-05 | Repudiation | map_version / generated_on | low | mitigate | Keep map_version 1.18.0; generated_on = execute day; Phase 13 owns 1.19.0 |
</threat_model>

<verification>
- `python tooling/check_capability_map.py` exits 0 (envelope, staleness 0, uniqueness, MAP-19-02 floors).
- JSON: schema 2, map_version 1.18.0, generated_on execute day, 63 packs, DA 5/4, Opportunity still has ch01–ch03/ch05, remap is MOVE.
- One-shot print: none of the five primaries is `<4 AND 1 pack`.
- CONTRACT paragraph greps: 628, 502, Cybersecurity, Digital Engineering.
- Phase 13 fence: plugin/CHANGELOG/RELEASE-INFO 1.18.0; catalog dod-vva-rpg.chapters == 10; no generator; SOURCE-VETTING http == 0.
</verification>

<success_criteria>
- MAP-19-01: regenerated map includes all v1.19 packs/chapters; gate PASS.
- MAP-19-02: listed primaries have THRESHOLDS >=4; conjunct holds; Integration not invented.
- MAP-19-03: three locked chapters MOVEd into Decision Analysis; DA 5/4.
- MAP-19-05: CONTRACT paragraph documents live 628+, 502 residue, Cyber+DE unbound.
</success_criteria>

<output>
Create `.planning/phases/12-map-regen-hygiene-gate-wiring/12-01-SUMMARY.md` when done
</output>
