---
phase: 12-map-regen-hygiene-gate-wiring
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - tooling/check_release.py
  - tooling/check_capability_map.py
  - docs/capability-map-CONTRACT.md
  - CHANGELOG.md
  - .gitattributes
  - packs/mil-std-881f/SKILL.md
  - packs/dafman-63-119/SKILL.md
  - packs/mil-std-40051/SKILL.md
  - packs/federal-bca/SKILL.md
  - packs/federal-bca/PACK.yaml
autonomous: true
requirements: [MAP-19-04, HYG-01, HYG-02, HYG-03, HYG-04]
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "12-01 landed first: check_capability_map.py is GREEN before this plan imports it. Do not wire a RED map into check_release"
    - "check_release.py imports check_capability_map.main() in-process (no subprocess) and fail()s on non-zero; python tooling/check_release.py PASS"
    - "Map-gate docstring no longer says standalone; CONTRACT §4 no longer says the gate is not wired"
    - "HYG-01: CHANGELOG.md has no UTF-8 BOM and is LF-normalized; .gitattributes exists with exactly `*.md text eol=lf` (nothing else unless needed). No ## [1.19.0] section"
    - "HYG-02: four SKILL.md nits fixed (881F alpha, dafman AFOTEC-before-Agile, 40051 drop circular Topic Index, federal-bca label); validate_pack.py PASS on those four slugs"
    - "HYG-04: federal-bca PACK.yaml notes no longer claim zero literal (c) hits; wording records A-94 enumeration markers"
    - "HYG-03: sibling vet_source.py EXCLUDED gained afotec / dod-dag / cmu-sei keys OR 12-02-SUMMARY records an external issue/PR URL plus those three keywords. Do not vendor vet_source.py into this repo. Do not block close on a sibling merge"
    - "Version trio stays 1.18.0 (plugin.json == CHANGELOG top == RELEASE-INFO.txt). No v1.19* tag. map_version stays 1.18.0"
    - "CI validate.yml is not given a step that executes repo Python. Catalog dod-vva-rpg.chapters stays 10. README live-pack table not given new-slug rows here"
    - "No generator; no AAF/CBA/DoDM/stakeholder packs; no Cyber/DE bindings; SOURCE-VETTING http count 0"
    - "EDGE_ABSENT=1: no SPEC. Unclassified stay unresolved assumptions. HYG-03 close path (sibling commit vs record-only) is discretion — both satisfy ROADMAP SC-4"
    - "Idempotency: re-run of wire/hygiene edits the same lines in place; does not add a second import block or a second [1.19.0] heading"
    - "Concurrency: depends_on 12-01; exclusive ownership of check_release.py + CHANGELOG + .gitattributes + the four SKILL.md + federal-bca PACK.yaml. CONTRACT §4 sentence is this plan's (12-01 owns the MAP-19-05 paragraph only)"
  artifacts:
    - path: tooling/check_release.py
      provides: "MAP-19-04 in-process import of check_capability_map.main()"
      contains: "check_capability_map"
    - path: tooling/check_capability_map.py
      provides: "docstring no longer claims standalone / Phase 9 maybe-wire"
    - path: docs/capability-map-CONTRACT.md
      provides: "§4 refresh-path sentence updated: gate is wired into check_release"
    - path: CHANGELOG.md
      provides: "HYG-01 BOM stripped + LF; still headed ## [1.18.0]"
    - path: .gitattributes
      provides: "HYG-01 pin"
      contains: "*.md text eol=lf"
    - path: packs/mil-std-881f/SKILL.md
      provides: "HYG-02 alpha: PM / measurement / EVMS mapping between Missile and Program Element"
    - path: packs/dafman-63-119/SKILL.md
      provides: "HYG-02 alpha: AFOTEC before Agile"
    - path: packs/mil-std-40051/SKILL.md
      provides: "HYG-02 drop circular Topic Index target"
    - path: packs/federal-bca/SKILL.md
      provides: "HYG-02 label Opportunity cost / benefit identification"
    - path: packs/federal-bca/PACK.yaml
      provides: "HYG-04 (c) wording — enumeration markers, not copyright claims"
  key_links:
    - from: tooling/check_release.py
      to: tooling/check_capability_map.py
      via: "import check_capability_map; rc = check_capability_map.main()"
      pattern: "check_capability_map.main"
    - from: docs/capability-map-CONTRACT.md
      to: tooling/check_release.py
      via: "§4 no longer says standalone"
      pattern: "check_release.py"
    - from: .gitattributes
      to: CHANGELOG.md
      via: "future md checkouts are LF"
      pattern: "eol=lf"

# Prohibitions (must-NOT). No check_kind / check_target fields.
# - must-NOT bump version to 1.19 (plugin / CHANGELOG [1.19.0] / RELEASE-INFO / map_version / tag)
# - must-NOT invent packs (AAF / Army CBA / stakeholder / dodm-5000-102)
# - must-NOT leave remap as copy (remap already applied in 12-01; do not re-edit map JSON here)
# - must-NOT put URLs in SOURCE-VETTING
# - must-NOT steal REL-19 (catalog chapter integer, README new-slug rows, GitHub Release)
# - must-NOT write a generator; must-NOT subprocess the map gate; must-NOT add a CI repo-Python step
# - must-NOT vendor jgs-reference-skill/tools/vet_source.py into this repo
# - must-NOT rewrite the whole repo's line endings
# - must-NOT check MAP-19 / HYG boxes (verify does that)
---

<objective>
Wave B (12-RESEARCH recommended shape; depends_on 12-01): wire the now-GREEN map gate into `check_release.py`, then land the four hygiene nits (BOM/.gitattributes, topic-index, federal-bca "(c)", sibling EXCLUDED or recorded external PR).

Purpose: one local command (`python tooling/check_release.py`) proves map freshness; v1.18 audit leftovers close without stealing Phase 13 version/tag. Output: wired release gate + hygiene files. Version trio stays 1.18.0.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-map-regen-hygiene-gate-wiring/12-RESEARCH.md
@.planning/phases/12-map-regen-hygiene-gate-wiring/12-PATTERNS.md
@.planning/phases/12-map-regen-hygiene-gate-wiring/12-01-PLAN.md
@tooling/check_release.py
@tooling/check_capability_map.py
@docs/capability-map-CONTRACT.md
@CHANGELOG.md
@packs/mil-std-881f/SKILL.md
@packs/dafman-63-119/SKILL.md
@packs/mil-std-40051/SKILL.md
@packs/federal-bca/SKILL.md
@packs/federal-bca/PACK.yaml
</context>

<plan_notes>
depends_on 12-01: map gate MUST be GREEN before the import lands. If `python tooling/check_capability_map.py` is still RED, stop — do not wire.

TRACER_MODE: Task 1 is the production slice (import + CONTRACT §4 + docstring + both gates PASS). Tasks 2–3 are hygiene around that already-wired gate.

REVERSIBILITY: revert the scoped commits; un-import restores the Phase 8 standalone gate; hygiene is wording/encoding only.

EDGE_ABSENT=1: no SPEC. HYG-03 sibling-writable vs record-only is discretion (ROADMAP SC-4 allows either). Unclassified stay unresolved assumptions.

Stay on main. No branches / worktrees. Use `python`. `$REF` = `C:/Users/gower/OneDrive/Documents/GitHub/jgs-reference-skill`.

## Deviations ledger
Executor MUST keep a `## Deviations` section in 12-02-SUMMARY.md. Record every departure (HYG-03 record-only vs sibling commit, extra .gitattributes lines, unexpected check_release insertion point). If none: write `None.`
</plan_notes>

<claim_verification>
Live commands run this planner session (2026-08-17), cwd repo root. Pre-12-01 start state — executor re-runs the map/release pair at task start and must see map GREEN (12-01) before wiring.

| claim | command | observed | status |
|---|---|---|---|
| Branch is main | `git branch --show-current` | `main` | VERIFIED |
| Map gate RED at plan time (12-01 must flip this) | `python tooling/check_capability_map.py` | `FAIL: 19 issue(s)`; exit 1 | VERIFIED |
| Release gate PASS and does not import map | `python tooling/check_release.py`; python membership | `RELEASE CHECK: PASS`; `check_capability_map` absent from check_release.py | VERIFIED |
| Import analog already in-process | `tooling/check_release.py:118-128` | `sys.path.insert(0, str(ROOT / "tooling"))` then `import validate_pack` | VERIFIED |
| Map-gate main() returns 0/1 | `tooling/check_capability_map.py:238-245` | prints `FAIL: N issue(s)` / `PASS: capability map OK`; `return 1` / `return 0` | VERIFIED |
| Map-gate docstring still standalone | `tooling/check_capability_map.py:16-17` | `stdlib only. Standalone — not imported by check_release.py (Phase 9 may wire it).` | VERIFIED |
| CONTRACT §4 standalone sentence | `docs/capability-map-CONTRACT.md:81-82` | `The gate is standalone (not wired into check_release.py in this milestone;` | VERIFIED |
| CHANGELOG has UTF-8 BOM + CRLF | python first bytes | `efbbbf3c212d2d0d`; BOM True; `crlf_count` 620 | VERIFIED |
| `.gitattributes` missing | `Path('.gitattributes').exists()` | `False` | VERIFIED |
| Version trio 1.18.0; CHANGELOG top is [1.18.0] | plugin / CHANGELOG / RELEASE-INFO | all `1.18.0`; line 12 `## [1.18.0]: 2026-08-17` | VERIFIED |
| HYG-02 live nits | Read SKILL.md | 881F:89 `PM / measurement / EVMS mapping` last; dafman:64-65 Agile then AFOTEC; 40051:77 `ch01, ch07, ch08, Topic Index`; federal-bca:74 `Opportunity/Benefit Analysis` | VERIFIED |
| HYG-04 live wording | `packs/federal-bca/PACK.yaml:20` | `no third-party copyright / (c) / all-rights-reserved notices` | VERIFIED |
| Sibling EXCLUDED missing the three keys | `$REF/tools/vet_source.py:41-56` | EXCLUDED has iso/iec/ieee/incose/swebok/mitre/omg/togaf/wiley…; `afotec` 0, `cmu` 0, `sei` 0; `defense acquisition` is a US_GOV publisher signal, not an EXCLUDED key | VERIFIED |
| CI never execs repo Python | `.github/workflows/validate.yml:4-6` | `never executes checked-out repository code` | VERIFIED |
| No v1.19 tag | `git tag -l 'v1.19*'` | empty | VERIFIED |
| Link policy | `grep -c http docs/SOURCE-VETTING.md` | `0` | VERIFIED |
| Catalog leftover is Phase 13 | python catalog | `dod-vva-rpg chapters 10` | VERIFIED |
</claim_verification>

<tasks>

<task type="auto" reversibility="easy">
  <name>Task 1: Wire check_capability_map.main() into check_release (MAP-19-04)</name>
  <files>tooling/check_release.py, tooling/check_capability_map.py, docs/capability-map-CONTRACT.md</files>
  <read_first>tooling/check_release.py, tooling/check_capability_map.py, docs/capability-map-CONTRACT.md, .planning/phases/12-map-regen-hygiene-gate-wiring/12-RESEARCH.md</read_first>
  <action>
Hard gate before any edit: run `python tooling/check_capability_map.py`. If it is not PASS, STOP. 12-01 is not done. Do not import a RED map.

1. In `tooling/check_release.py`, after the existing pack-validate block (`# 5. validate every pack`, today lines 118-128) and **before** the `# report` block (today line 231), add an in-process map-gate call. Recommended insertion: new numbered bullet immediately after the cursor-manifest block (~line 213) and before authored-file headers, so a map failure is a first-class release issue. Copy the existing import style — **no subprocess**.

```python
    # 5d. MAP-19-04: capability-pack map freshness (same process; prints its own counts)
    try:
        import check_capability_map  # type: ignore
        rc = check_capability_map.main()
        if rc != 0:
            fail(errs, "[map] check_capability_map.py failed (see output above)")
    except Exception as e:
        fail(errs, f"[map] check_capability_map failed to run: {e}")
```

   `sys.path.insert(0, str(ROOT / "tooling"))` already happened in §5 — reuse it; do not insert a second path hack unless the map import is placed *above* that line (it must not be). Duplicate stdout from `main()` (cluster-count block) is fine.

2. Update the `check_release.py` module docstring list of checks to mention the map gate (new numbered item). Do not claim CI runs it.

3. In `tooling/check_capability_map.py` header docstring, replace
   `stdlib only. Standalone — not imported by check_release.py (Phase 9 may wire it).`
   with a sentence that it is stdlib-only and **is** invoked by `check_release.py` (local/trusted; CI still does not exec repo Python).

4. In `docs/capability-map-CONTRACT.md` §4, replace the sentence
   `The gate is standalone (not wired into check_release.py in this milestone; release wiring is optional follow-on).`
   with: the refresh path still runs `python tooling/check_capability_map.py`, **and** `python tooling/check_release.py` now invokes `check_capability_map.main()` in-process. Do not touch the MAP-19-05 paragraph from 12-01. Do not bump example `map_version` to 1.19.0.

5. Do **not** add a CI step to `.github/workflows/validate.yml`. Do **not** bump plugin / CHANGELOG / RELEASE-INFO / map_version / tag.

6. Run `python tooling/check_capability_map.py` (still PASS) then `python tooling/check_release.py` (must now print the map cluster-count block and end `RELEASE CHECK: PASS`). Version trio must still read 1.18.0.

7. Commit the three files (`fix(tooling): wire check_capability_map into check_release`). `git show --name-only` must have zero `catalog.json`, `README.md` live-pack edits, `.github/workflows/validate.yml`, `.claude-plugin/plugin.json`, `RELEASE-INFO.txt`.
  </action>
  <verify>
    <automated>python tooling/check_capability_map.py && python tooling/check_release.py && python -c "from pathlib import Path; t=Path('tooling/check_release.py').read_text(encoding='utf-8'); assert 'import check_capability_map' in t; assert 'check_capability_map.main()' in t; assert 'subprocess' not in t.split('check_capability_map')[1][:400] if 'check_capability_map' in t else True; d=Path('tooling/check_capability_map.py').read_text(encoding='utf-8'); assert 'Standalone — not imported' not in d; assert 'check_release.py' in d; c=Path('docs/capability-map-CONTRACT.md').read_text(encoding='utf-8'); assert 'standalone (not wired' not in c; assert 'check_release.py' in c; print('WIRE_OK')" && python -c "import json,re; from pathlib import Path; p=json.loads(Path('.claude-plugin/plugin.json').read_text())['version']; cl=Path('CHANGELOG.md').read_text(encoding='utf-8'); m=re.search(r'^##\s*\[(\d+\.\d+\.\d+)\]', cl, re.M); ri=Path('RELEASE-INFO.txt').read_text(encoding='utf-8'); mm=re.search(r'Version:\s*([0-9]+\.[0-9]+\.[0-9]+)', ri); assert p==m.group(1)==mm.group(1)=='1.18.0', (p,m.group(1),mm.group(1)); print('VERSION_TRIO', p)" && python -c "from pathlib import Path; t=Path('.github/workflows/validate.yml').read_text(encoding='utf-8'); assert 'check_capability_map' not in t"</automated>
  </verify>
  <acceptance_criteria>
    - check_release.py imports check_capability_map and calls main(); fail on non-zero
    - no subprocess used for the map gate
    - map-gate docstring no longer says standalone / Phase 9 maybe-wire
    - CONTRACT §4 no longer says the gate is not wired
    - both python commands PASS; map counts appear in check_release stdout
    - plugin / CHANGELOG top / RELEASE-INFO still 1.18.0
    - validate.yml still does not name check_capability_map
  </acceptance_criteria>
  <done>MAP-19-04 wired in-process; both gates PASS; CONTRACT §4 + docstring updated; version trio untouched. Committed.</done>
</task>

<task type="auto" reversibility="easy">
  <name>Task 2: HYG-01 BOM+LF+.gitattributes + HYG-02 four SKILL nits + HYG-04 (c) wording</name>
  <files>CHANGELOG.md, .gitattributes, packs/mil-std-881f/SKILL.md, packs/dafman-63-119/SKILL.md, packs/mil-std-40051/SKILL.md, packs/federal-bca/SKILL.md, packs/federal-bca/PACK.yaml</files>
  <read_first>CHANGELOG.md, packs/mil-std-881f/SKILL.md, packs/dafman-63-119/SKILL.md, packs/mil-std-40051/SKILL.md, packs/federal-bca/SKILL.md, packs/federal-bca/PACK.yaml</read_first>
  <action>
Four small in-tree nits. Do not regenerate SKILL.md bodies. Do not add `## [1.19.0]`. Do not rewrite the whole repo's line endings.

**HYG-01**
1. Strip the UTF-8 BOM from `CHANGELOG.md` (first bytes today `ef bb bf`). Normalize that file to LF only (`\n`, no `\r\n`). Keep content otherwise byte-stable — especially `## [1.18.0]: 2026-08-17`. JGSC copyright + SPDX header must remain in the first 600 chars (check_release authored-file header check).
2. Create `.gitattributes` with exactly:
```
*.md text eol=lf
```
   Nothing else unless a follow-on line is required for the file to be valid. Do not add `* text=auto` (that would rewrite the tree).

**HYG-02** (7-GAP R6 / IN-01 — exact line fixes; re-read the live line in case 12-01 did not touch these files):
3. `packs/mil-std-881f/SKILL.md` Topic Index: `"PM / measurement / EVMS mapping"` is last (live :89), out of alpha. Move that exact bullet to sit after `Missile / ordnance (C) / strategic (D)` (live :77) and before `Program Element / defense materiel item` (live :78). Do not rename the label.
4. `packs/dafman-63-119/SKILL.md` Topic Index: live :64-65 has `"Agile / …"` then `"AFOTEC / …"`. Move the AFOTEC row **before** Agile.
5. `packs/mil-std-40051/SKILL.md` live :77: `"Training & Documentation" → ch01, ch07, ch08, Topic Index`. Drop `, Topic Index` (circular self-target). Leave the ch01, ch07, ch08 targets.
6. `packs/federal-bca/SKILL.md` live :74: `"Opportunity/Benefit Analysis"` → rename label to `Opportunity cost / benefit identification`. Keep the same chapter targets (`ch01, ch02, ch06`).

7. Re-run `python tooling/validate_pack.py` on each of the four slugs. All must exit 0.

**HYG-04**
8. `packs/federal-bca/PACK.yaml` notes (live :20) currently claim `no third-party copyright / (c) / all-rights-reserved notices`. Reword to:
   `no third-party copyright / all-rights-reserved notices (literal "(c)" hits in A-94 are enumeration markers, not copyright claims)`
   Do not change P7-PRE-2 Army-CBA fetch-fail substance. Do not add a URL.

9. Confirm `grep -c http docs/SOURCE-VETTING.md` is still 0. Confirm CHANGELOG has no `## [1.19.0]`. Confirm first bytes of CHANGELOG are not `ef bb bf`.

10. Commit hygiene only (`chore: HYG-01 BOM pin, HYG-02 topic-index, HYG-04 federal-bca (c)`). Do not include catalog/README/plugin/RELEASE-INFO.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; b=Path('CHANGELOG.md').read_bytes(); assert not b.startswith(b'\xef\xbb\xbf'), b[:8]; assert b.count(b'\r\n')==0, b.count(b'\r\n'); t=b.decode('utf-8'); assert '## [1.18.0]' in t and '## [1.19.0]' not in t; print('HYG01_CHANGELOG_OK')" && python -c "from pathlib import Path; t=Path('.gitattributes').read_text(encoding='utf-8'); assert '*.md text eol=lf' in t; print('HYG01_ATTR_OK')" && python -c "
from pathlib import Path
s881=Path('packs/mil-std-881f/SKILL.md').read_text(encoding='utf-8')
assert 'PM / measurement / EVMS mapping' in s881
idx_ms=s881.find('Missile / ordnance')
idx_pm=s881.find('PM / measurement / EVMS mapping')
idx_pe=s881.find('Program Element')
assert 0 < idx_ms < idx_pm < idx_pe, (idx_ms, idx_pm, idx_pe)
sdaf=Path('packs/dafman-63-119/SKILL.md').read_text(encoding='utf-8')
assert sdaf.find('AFOTEC') < sdaf.find('Agile /'), (sdaf.find('AFOTEC'), sdaf.find('Agile /'))
s400=Path('packs/mil-std-40051/SKILL.md').read_text(encoding='utf-8')
assert 'Training & Documentation' in s400
assert 'ch08, Topic Index' not in s400
assert 'Topic Index' not in [ln for ln in s400.splitlines() if 'Training & Documentation' in ln][0]
sbca=Path('packs/federal-bca/SKILL.md').read_text(encoding='utf-8')
assert 'Opportunity cost / benefit identification' in sbca
assert 'Opportunity/Benefit Analysis' not in sbca
print('HYG02_OK')
" && python tooling/validate_pack.py packs/mil-std-881f && python tooling/validate_pack.py packs/dafman-63-119 && python tooling/validate_pack.py packs/mil-std-40051 && python tooling/validate_pack.py packs/federal-bca && python -c "from pathlib import Path; t=Path('packs/federal-bca/PACK.yaml').read_text(encoding='utf-8'); assert 'enumeration markers' in t; assert 'copyright / (c) / all-rights-reserved notices' not in t; print('HYG04_OK')" && grep -c http docs/SOURCE-VETTING.md | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG first bytes are not EF BB BF; zero CRLF; still headed [1.18.0]; no [1.19.0]
    - .gitattributes exists and pins `*.md text eol=lf`
    - 881F PM/EVMS row sits in alpha (before Program Element)
    - dafman AFOTEC row precedes Agile
    - 40051 Training & Documentation line has no Topic Index target
    - federal-bca SKILL label is Opportunity cost / benefit identification
    - validate_pack.py PASS on all four slugs
    - federal-bca PACK.yaml notes mention enumeration markers and no longer claim zero (c) literals
    - SOURCE-VETTING http count 0
  </acceptance_criteria>
  <done>HYG-01/02/04 landed; four packs revalidated; CHANGELOG un-BOMed; .gitattributes pinned. Committed.</done>
</task>

<task type="auto" reversibility="easy">
  <name>Task 3: HYG-03 sibling EXCLUDED or SUMMARY record + both gates PASS</name>
  <files>.planning/phases/12-map-regen-hygiene-gate-wiring/12-02-SUMMARY.md</files>
  <read_first>C:/Users/gower/OneDrive/Documents/GitHub/jgs-reference-skill/tools/vet_source.py, .planning/phases/6-source-vetting-unverified-resolution/6-GAP_ANALYSIS.md, .planning/phases/12-map-regen-hygiene-gate-wiring/12-RESEARCH.md</read_first>
  <action>
HYG-03 is the sibling `jgs-reference-skill` EXCLUDED dict (`$REF/tools/vet_source.py` lines 41-56). This execute tree is `jgs-se-knowledge-packs`. Do **not** vendor `vet_source.py` into `tooling/`.

Valid close (either is enough — ROADMAP SC-4):

**Path A (if `$REF` is writable in this session):** add three EXCLUDED keys with one-line reasons matching 6-GAP Thread 3, then commit/PR **in the sibling repo** (not this repo):
- `afotec` — AFOTEC OT&E / CERT products are not a redistribution grant for this library
- `dod dag` **and/or** `defense acquisition guidebook` — DAG is not packageable here (note: `defense acquisition` already exists as a US_GOV *publisher* signal; EXCLUDED keys must be specific enough not to nuke every DoD PD title — prefer `defense acquisition guidebook` / `dod dag` over the bare publisher token)
- `cmu` / `sei` (or `carnegie mellon` / `software engineering institute`) — SEI technical reports are not a blanket redistribution grant

Do not weaken existing EXCLUDED keys. Do not treat a sibling merge-to-main as required for Phase 12 close.

**Path B (default if sibling is not writable, or if a commit/PR cannot be opened):** record in `12-02-SUMMARY.md`:
- the three keyword groups (`afotec`, `dod-dag` / `defense acquisition guidebook`, `cmu-sei`)
- an external issue or PR URL **if one exists**; if the sibling remote cannot be reached, record `external PR not opened this session` plus the exact keys that still need adding
- the live sibling file path `$REF/tools/vet_source.py`

Path B is a valid close. Do not leave Phase 12 open waiting on a sibling merge.

Then:
1. Re-run `python tooling/check_capability_map.py` and `python tooling/check_release.py`. Both must PASS (wire from Task 1 + map from 12-01 still green after hygiene).
2. Confirm version trio 1.18.0; `git tag -l 'v1.19*'` empty; catalog `dod-vva-rpg.chapters == 10`; `grep -c http docs/SOURCE-VETTING.md` == 0; no `## [1.19.0]` in CHANGELOG.
3. Write `12-02-SUMMARY.md` per the summary template: wire insertion point, both-gate stdout, HYG-01 bytes, HYG-02 before/after lines, HYG-04 wording, HYG-03 Path A or B evidence, Phase 13 leftovers not stolen, `## Deviations` ledger.
4. Commit the SUMMARY in this repo (`docs(planning): 12-02 SUMMARY`). If Path A produced a sibling commit, that commit lives only in `$REF` — do not `git add` sibling files here.
  </action>
  <verify>
    <automated>python tooling/check_capability_map.py && python tooling/check_release.py && python -c "import json,re; from pathlib import Path; p=json.loads(Path('.claude-plugin/plugin.json').read_text())['version']; cl=Path('CHANGELOG.md').read_text(encoding='utf-8'); m=re.search(r'^##\s*\[(\d+\.\d+\.\d+)\]', cl, re.M); ri=Path('RELEASE-INFO.txt').read_text(encoding='utf-8'); mm=re.search(r'Version:\s*([0-9]+\.[0-9]+\.[0-9]+)', ri); assert {p,m.group(1),mm.group(1)}=={'1.18.0'}; assert '## [1.19.0]' not in cl; c=json.load(open('catalog.json',encoding='utf-8')); assert next(x for x in c['packs'] if x['slug']=='dod-vva-rpg')['chapters']==10; print('P13_FENCE_OK')" && grep -c http docs/SOURCE-VETTING.md | grep -qx 0 && test -z "$(git tag -l 'v1.19*')" && test ! -e tooling/vet_source.py && python -c "
from pathlib import Path
s=Path('.planning/phases/12-map-regen-hygiene-gate-wiring/12-02-SUMMARY.md').read_text(encoding='utf-8')
assert 'Deviations' in s
assert 'afotec' in s.lower() and ('dod-dag' in s.lower() or 'defense acquisition guidebook' in s.lower()) and ('cmu' in s.lower() or 'sei' in s.lower())
print('HYG03_RECORD_OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - both gates PASS
    - version trio 1.18.0; no v1.19 tag; no CHANGELOG [1.19.0]; catalog dod-vva-rpg.chapters still 10
    - SOURCE-VETTING http count 0; tooling/vet_source.py does not exist
    - 12-02-SUMMARY records HYG-03 Path A (sibling keys) or Path B (keywords + external PR/issue or explicit not-opened)
    - SUMMARY has a ## Deviations ledger
  </acceptance_criteria>
  <done>HYG-03 closed as sibling keys or recorded external PR; both gates PASS; Phase 13 fence held; SUMMARY + Deviations written. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| check_release.py -> check_capability_map.main() | in-process import of repo Python; local/trusted only — CI must not gain this path |
| this repo -> sibling jgs-reference-skill | HYG-03 is out of tree; vendoring would fake the sync |
| CHANGELOG encoding -> version-single-source | BOM/CRLF strip must not drop the ## [1.18.0] header check_release parses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-06 | Tampering | check_release map wire | medium | mitigate | Import + fail on non-zero; no subprocess; wire only after map GREEN (Task 1 hard gate) |
| T-12-07 | Elevation | CI validate.yml | medium | mitigate | Do not add a repo-Python step; verify.yml assert `check_capability_map` absent |
| T-12-08 | Spoofing | HYG-03 vendor copy | low | mitigate | Forbid tooling/vet_source.py; SUMMARY must record sibling path or external PR |
| T-12-09 | Tampering | CHANGELOG version header | medium | mitigate | BOM/LF only; assert ## [1.18.0] remains and ## [1.19.0] is absent |
| T-12-10 | Repudiation | version trio / tag | low | mitigate | Task 3 fence: plugin/CHANGELOG/RELEASE-INFO 1.18.0; no v1.19* tag |
</threat_model>

<verification>
- `python tooling/check_capability_map.py` PASS.
- `python tooling/check_release.py` PASS and contains `import check_capability_map` / `.main()`.
- CHANGELOG no BOM, LF only, still [1.18.0].
- `.gitattributes` pins `*.md text eol=lf`.
- Four SKILL nits gone; validate_pack ×4 PASS; federal-bca PACK.yaml enumeration-markers wording.
- HYG-03 Path A or B recorded in 12-02-SUMMARY.
- Phase 13 fence: version trio 1.18.0; catalog chapters 10; no v1.19 tag; SOURCE-VETTING http 0.
</verification>

<success_criteria>
- MAP-19-04: check_release invokes the map gate; both commands PASS.
- HYG-01: BOM gone; .gitattributes pin present.
- HYG-02: four topic-index nits fixed and revalidated.
- HYG-03: sibling EXCLUDED or recorded external PR/issue.
- HYG-04: federal-bca "(c)" wording polished.
</success_criteria>

<output>
Create `.planning/phases/12-map-regen-hygiene-gate-wiring/12-02-SUMMARY.md` when done
</output>
