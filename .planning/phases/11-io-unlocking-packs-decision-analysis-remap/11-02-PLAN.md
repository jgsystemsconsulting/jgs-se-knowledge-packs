---
phase: 11-io-unlocking-packs-decision-analysis-remap
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - packs/dod-vva-rpg/**
  - packs/federal-bca/SKILL.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - catalog.json
  - SKILLS.md
  - NOTICE
  - .cursor-plugin/plugin.json
  - docs/packs.html
  - README.md
autonomous: true
requirements: [IO-01, IO-02, IO-05, IO-06, IO-07]
estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "dod-vva-rpg chapter count > 10 (IO-02); new chNN files linked from SKILL.md; no packs/dodm-5000-102"
    - "Each new RPG chapter: P7-PRE-4 (authorship / PD / no DIST B–F) + chars/page >= 300 + check_overlap exit 0 against that chapter full_text"
    - "ROADMAP SC-2 Validation reading (explicit): DoDM was the 'new pack' path and stays deferred; Validation depth moves via additional chapters in the existing pack — do not invent a slug to satisfy the noun 'pack'"
    - "IO-01 remap table is written in 11-02-SUMMARY (and named in REQUIREMENTS parenthetical). It names federal-bca ch04 + ch06 and dod-vva-rpg ch06 → Decision Analysis. Phase 11 does NOT edit docs/capability-pack-map.json. Decision Analysis live count leave-2 is Phase 12 MAP-19-03 — not a numeric contract this phase (EDGE_ABSENT=1)"
    - "IO-05 and IO-06 dated DEFERRED parentheticals exist; IO-07 dated ACCEPT parenthetical exists; boxes stay `- [ ]`; no AAF pack; no invented stakeholder pack"
    - "Thin-register of nasa-std-8719-14 + is-gps-200n only: catalog 63, SKILLS 63 (+2 signposts), cursor 64, dirs 65; check_release.py PASS. No version bump, no CHANGELOG IO narrative, no REL-19-02 tag"
    - "No https:// in changed pack files; grep -c http docs/SOURCE-VETTING.md still 0; no sources/ or full_text.txt in commits"
    - "SP-7084 not built; Army CBA pack not built; IS-300 / ICD-GPS-153 not ingested"
    - "Idempotency: re-run updates the same chapter files / parentheticals / catalog objects in place; does not duplicate IO annotations or catalog slugs"
    - "Concurrency: 11-02 starts only after 11-01 commits both new pack dirs; stay on main"
  artifacts:
    - path: packs/dod-vva-rpg/chapters/
      provides: "IO-02 leftover RPG chapters (Checklist + ≤2); count > 10"
    - path: packs/dod-vva-rpg/PACK.yaml
      provides: "bumped chapters + summed source_pages + per-new-chapter P7-PRE-4 provenance (titles + date, no URL)"
    - path: packs/dod-vva-rpg/SKILL.md
      provides: "updated Chapter/Topic Index; When-to-use + Prerequisites retained"
    - path: packs/federal-bca/SKILL.md
      provides: "optional Decision Analysis Topic Index row → ch04/ch06 (pack-side nudge, not a map edit)"
    - path: .planning/REQUIREMENTS.md
      provides: "IO-01 remap-specified note; IO-02 chapters-not-a-pack; IO-05/06 DEFERRED; IO-07 ACCEPT (boxes open)"
    - path: .planning/STATE.md
      provides: "Phase 11 deviations bullet (GO packs + honest records); frontmatter untouched"
    - path: catalog.json
      provides: "thin-register objects for nasa-std-8719-14 and is-gps-200n"
    - path: SKILLS.md
      provides: "two new rows; header 63 packs (+2 signposts)"
    - path: NOTICE
      provides: "[pack: nasa-std-8719-14] and [pack: is-gps-200n] Public Domain blocks"
    - path: .cursor-plugin/plugin.json
      provides: "+2 skill paths; version stays 1.18.0"
    - path: docs/packs.html
      provides: "regenerated via gen_packs_page.py (never hand-edit)"
  key_links:
    - from: packs/dod-vva-rpg/SKILL.md
      to: packs/dod-vva-rpg/chapters/
      via: "Chapter Index links every new chNN"
      pattern: "chapters/ch1"
    - from: 11-02-SUMMARY remap table
      to: Phase 12 MAP-19-03
      via: "named chapters to move into Decision Analysis; no JSON edit now"
      pattern: "Decision Analysis & Trade Studies"
    - from: catalog.json
      to: packs/nasa-std-8719-14/
      via: "thin-register slug object status live"
      pattern: "nasa-std-8719-14"

# Prohibitions (must-NOT). No check_kind / check_target fields.
# - must-NOT create dodm-5000-102 / Army CBA / AAF / stakeholder / SP-7084 packs
# - must-NOT put URLs in packs or docs/SOURCE-VETTING.md
# - must-NOT edit docs/capability-pack-map.json
# - must-NOT commit sources/ or full_text.txt
# - must-NOT ingest GPS Apps II–IV / IS-300 / ICD-GPS-153
# - must-NOT treat leaning as skip-confirm (IO-02 still runs P7-PRE-4 per chapter)
# - must-NOT bump plugin version / CHANGELOG / tag REL-19-02
# - must-NOT check IO-01..07 boxes (verify does that)
# - must-NOT add all ~17 RPG special topics or legacy role guides
---

<objective>
Wave B (11-RESEARCH recommended shape): extend `dod-vva-rpg` (IO-02), write the IO-01 Decision Analysis remap table (spec only), record IO-05/06 deferred and IO-07 accept, and thin-register the two Wave-A slugs so `check_release.py` stays green on main.

Purpose: close every IO-01..07 ID without silent ticks and without stealing Phase 12 map work or Phase 13 version/tag.
Output: extended VV&A pack + dated planning records + thin-register surfaces. Map JSON untouched.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-io-unlocking-packs-decision-analysis-remap/11-RESEARCH.md
@.planning/phases/11-io-unlocking-packs-decision-analysis-remap/11-PATTERNS.md
@.planning/phases/11-io-unlocking-packs-decision-analysis-remap/11-01-PLAN.md
@docs/PACK-SPEC.md
@docs/SOURCE-VETTING.md
@packs/dod-vva-rpg/PACK.yaml
@packs/dod-vva-rpg/SKILL.md
@packs/federal-bca/SKILL.md
@catalog.json
@SKILLS.md
@.cursor-plugin/plugin.json
</context>

<plan_notes>
depends_on 11-01: both new pack dirs must exist on main before thin-register. Do not re-vet Phase 10. Do not re-scaffold `dod-vva-rpg` (`build_pack.py` refuses an existing slug).

TRACER_MODE: Wave B thin slice is (a) Checklist chapter-add proving the extend path and (b) the remap table as the IO-01 artifact. REVERSIBILITY: revert the three scoped commits; map JSON was never touched.

EDGE_ABSENT=1: unclassified IOs stay unresolved assumptions. IO-01 boundary: Decision Analysis count leave-2 is Phase 12 after remap apply — this plan delivers the table only. Do not invent a numeric live-map acceptance.

URLs below are research-store only. Never copy them into packs/ or SOURCE-VETTING.md.

Index (research store): `https://www.cto.mil/sea/vva_rpg/` ; UCO `https://www.cto.mil/sea/vva-rpg-uco/`. Fetch path: cto.mil index → DEBoK OTMM guest rendition (Phase 7 deviation #2). If Checklist cannot be fetched, record IO-02 **partial deferral** rather than substituting DoDM or inventing a slug.
</plan_notes>

<claim_verification>
Live commands run this planner session (2026-08-17), cwd repo root. Do not invent replacements.

| claim | command | observed | status |
|---|---|---|---|
| Branch is main | `git branch --show-current` | `main` | VERIFIED |
| dod-vva-rpg has 10 chapter files; PACK.yaml `chapters: 10`, `source_pages: 283` | `ls packs/dod-vva-rpg/chapters \| wc -l`; PACK.yaml lines 12–13 | `10`; `source_pages: 283` / `chapters: 10` | VERIFIED |
| T&E/V&V Checklist already named as selection drop | `grep -n "TEVVchecklist" packs/dod-vva-rpg/PACK.yaml` | line 42: `Dropped from set (selection, not licence): T&E/V&V Checklist (TEVVchecklist-pr.PDF)` | VERIFIED |
| No `dodm-5000-102` / AAF / Army CBA / SP-7084 / new GPS slugs yet (pre-11-01 this session) | `ls packs \| grep -E 'dodm-5000\|aaf-\|army-cba\|nasa-sp-7084\|is-gps'` | empty | VERIFIED |
| federal-bca chapters include ch04 + ch06; Topic Index has Sensitivity / NPV but no Decision Analysis row | `ls packs/federal-bca/chapters`; `grep -n Decision packs/federal-bca/SKILL.md` | ch04-uncertainty-and-sensitivity.md, ch06-reporting-and-decision-use.md; Decision only in body/ch06 title, not Topic Index | VERIFIED |
| dod-vva-rpg already has Decision analysis Topic Index row | `grep -n "Decision analysis" packs/dod-vva-rpg/SKILL.md` | line 76: `ch06, ch08, ch10` | VERIFIED |
| Live map Decision Analysis = 2 entries / 2 packs; Opportunity = 10/2; Validation = 5/4 | python on `docs/capability-pack-map.json` | Decision Analysis & Trade Studies 2/2; Opportunity/Benefit 10/2; Validation 5/4 | VERIFIED |
| Registration basis now (pre-thin-register): dirs 63 / catalog 61 / SKILLS 61(+2) / cursor 62 / README packs-61 / plugin version 1.18.0 | live counts this session | 63 / 61 / 61(+2) / 62 / packs-61 / 1.18.0 | VERIFIED |
| After 11-01 (expected at 11-02 start): dirs 65, catalog still 61 → check_release [index]/[cursor] would fail without thin-register | 11-RESEARCH Pitfall 7 + `tooling/check_release.py:160-210` | SKILLS count must equal non-signpost dirs; cursor must equal commercial packs | VERIFIED |
| Analog catalog object shape | python dump `nasa-ms-7009` / `faa-std-025` | slug, title, publisher, source_version, license, license_tier, commercial_use, chapters, status `live` | VERIFIED |
| IO boxes still open with Phase 10 handoff notes only | `grep -n "IO-0" .planning/REQUIREMENTS.md` | all `- [ ]`; no Phase 11 DEFERRED/ACCEPT yet | VERIFIED |
| Link Policy | `grep -c http docs/SOURCE-VETTING.md` | `0` | VERIFIED |
| `sources/` gitignored | `.gitignore:17` | `sources/` | VERIFIED |
| check_release PASS on current (pre-11-01) tree | `python tooling/check_release.py` | `RELEASE CHECK: PASS` | VERIFIED |
</claim_verification>

<tasks>

<task type="auto">
  <name>Task 1: Extend packs/dod-vva-rpg (IO-02) — Checklist + ≤2 topics; no dodm-5000-102</name>
  <files>packs/dod-vva-rpg/**</files>
  <read_first>packs/dod-vva-rpg/PACK.yaml, packs/dod-vva-rpg/SKILL.md, .planning/phases/11-io-unlocking-packs-decision-analysis-remap/11-RESEARCH.md, docs/PACK-SPEC.md</read_first>
  <action>
Pattern 3 — extend in place. Do **not** run `build_pack.py` on this slug. Do **not** create `packs/dodm-5000-102`. Do **not** ingest 5000.102 from any mirror. Do **not** add legacy role guides. Do **not** add all ~17 special topics.

Build set (keep lean — Validation primary):

1. **Must try:** T&E/V&V Checklist (`TEVVchecklist-pr.PDF`) — already licence-passed in Phase 7; dropped only to stay in the 8–10 band. This is the first new chapter (`ch11-…`).
2. **Should try:** Use Case Overview (`vva-rpg-uco`) **if** it is a fetchable PDF that frames intended-use / validation decisions. If it is HTML-only, skip and record that in PACK.yaml notes / SUMMARY — do not block the phase.
3. **At most 1–2 more** validation-adjacent special topics discovered on the **live** cto.mil index (referent, face validation, independent review, or similar). **Do not pre-invent titles.** Total new chapters = Checklist + ≤2.

Per new PDF:

1. Fetch via cto.mil index → DEBoK OTMM guest rendition (same path as Phase 7). Store under gitignored `sources/dod-vva-rpg/`.
2. **P7-PRE-4 (hard gate, same as shipped pack — do not invent a new gate):** confirm DoD/RPG authorship in body text and DEBoK Copyright Details = Public Domain; no third-party all-rights / proprietary / DIST B–F. Cover text layers often **lack** a printed DIST-A block — accept DEBoK PD + OSD/OUSD authorship as DIST-equivalent and record it (PACK.yaml already documents this pattern at lines 37–41). Halt that chapter if contrary notice.
3. EXTRACT (`--mode technical --install-missing no`). chars/page >= 300. Write `sources/dod-vva-rpg/work_dir_ch11.txt` (and ch12/ch13 as needed) via `printf '%s'`. Copy extracts to `sources/dod-vva-rpg/chapter_fulltexts/chNN.txt`.
4. GENERATE `chapters/chNN-*.md` with the standard chapter sections. Topic Index vocabulary for **new** rows: **Validation** (primary), Verification, Test & Evaluation. Do **not** retarget ch08 away from Validation. Do not move ch06 in the pack files — remap of ch06 is a Phase 12 map apply, not a file rename.

Then update:

- SKILL.md header chapter count; How-to-Use `ch01` through `ch1N`; Chapter Index table; Topic Index (add Validation / T&E checklist routing). Keep `## When to use` + `**Prerequisites:**`. Scope & Limits: remove or rewrite the sentence that says the Checklist was not packaged.
- PACK.yaml `build.chapters` = new total; `source_pages` = previous 283 + sum of **new** metadata.json page counts; notes: append per-new-chapter provenance (title + retrieved date + P7-PRE-4 finding, **no URL**); record IO-02 SC-2 reading (chapters-not-a-pack; DoDM still deferred). No TODO. No URL.

VALIDATE + SCAN + OVERLAP against **each new** full_text:

```
python tooling/validate_pack.py packs/dod-vva-rpg
python "$REF/tools/scan_generated_skill.py" packs/dod-vva-rpg
python "$REF/tools/check_overlap.py" --source sources/dod-vva-rpg/chapter_fulltexts/ch11.txt --pack packs/dod-vva-rpg
```

Repeat overlap per new chapter. Exit 0 mandatory.

If Checklist cannot be fetched: do **not** invent a replacement pack. Write IO-02 partial deferral (Pattern 5) into REQUIREMENTS + SUMMARY and leave chapter count at 10. That still satisfies ROADMAP SC-2 via documented deferral.

Commit only `packs/dod-vva-rpg` (`feat(packs): extend dod-vva-rpg (IO-02 leftover RPG chapters)`). Zero `sources/` / `full_text.txt`. Do not check the IO-02 box.
  </action>
  <verify>
    <automated>python tooling/validate_pack.py packs/dod-vva-rpg && test "$(ls packs/dod-vva-rpg/chapters | wc -l)" -gt 10 && test ! -d packs/dodm-5000-102 && test "$(grep -c '^## When to use' packs/dod-vva-rpg/SKILL.md)" -ge 1 && test "$(grep -c '^\*\*Prerequisites:\*\*' packs/dod-vva-rpg/SKILL.md)" -ge 1 && ! grep -qi "TODO" packs/dod-vva-rpg/PACK.yaml && ! grep -R -qE "https?://" packs/dod-vva-rpg && python -c "import pathlib,re,sys; t=pathlib.Path('packs/dod-vva-rpg/PACK.yaml').read_text(encoding='utf-8'); m=re.search(r'chapters:\\s*(\\d+)', t); print('chapters', m.group(1) if m else None); sys.exit(0 if m and int(m.group(1))>10 else 1)" && REF="C:/Users/gower/OneDrive/Documents/GitHub/jgs-reference-skill"; for f in sources/dod-vva-rpg/chapter_fulltexts/ch1*.txt; do [ -f "$f" ] || continue; python "$REF/tools/check_overlap.py" --source "$f" --pack packs/dod-vva-rpg || exit 1; done && test -z "$(git show --name-only --pretty=format: HEAD | grep -E 'sources/|full_text.txt')" && test -z "$(git diff --name-only -- docs/capability-pack-map.json)"</automated>
  </verify>
  <acceptance_criteria>
    - packs/dod-vva-rpg/chapters file count > 10 and PACK.yaml build.chapters > 10
    - at least ch11 exists and is linked from SKILL.md Chapter Index
    - validate_pack.py exits 0; overlap exit 0 on each new chapter full_text
    - P7-PRE-4 provenance (title + date, no URL) present in PACK.yaml notes for each new chapter
    - packs/dodm-5000-102 does not exist
    - ch08 still exists (Validation fundamentals not retargeted/removed)
    - no http/https in the pack tree; HEAD commit has no sources/ or full_text.txt
    - docs/capability-pack-map.json unchanged
    - IO-02 checkbox still `- [ ]`
    - If fetch failed: REQUIREMENTS IO-02 parenthetical contains DEFERRED and still no dodm-5000-102 — treat that as the alternate pass (document in SUMMARY Deviations)
  </acceptance_criteria>
  <done>IO-02 Validation depth moved via leftover RPG chapters (or honest partial deferral). No DoDM pack. Gates pass. One scoped commit.</done>
</task>

<task type="auto">
  <name>Task 2: IO-01 remap table + IO-05/06 DEFERRED + IO-07 ACCEPT (no map JSON)</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md, packs/federal-bca/SKILL.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/STATE.md, .planning/phases/11-io-unlocking-packs-decision-analysis-remap/11-RESEARCH.md, packs/federal-bca/SKILL.md, packs/dod-vva-rpg/SKILL.md</read_first>
  <action>
Edit-only on the named files. Do **not** edit `docs/capability-pack-map.json` or `docs/capability-pack-map.md`. Do **not** create a CBA / stakeholder / AAF pack. Do **not** check any IO boxes. Do **not** write `http` into SOURCE-VETTING.md. Do not touch STATE.md YAML frontmatter (current_phase, progress, completed_plans).

**A. IO-01 remap spec (Pattern 4) — table goes in 11-02-SUMMARY when that file is written; also copy the same table into the REQUIREMENTS IO-01 parenthetical as a one-line pointer plus the three chapter names.**

Recommended set (move, do not copy — every chapter has exactly one cluster). Write this table verbatim into 11-02-SUMMARY under heading `## IO-01 remap table (MAP-19-03 apply)`:

| Pack | Chapter | From (today) | To | Why |
|---|---|---|---|---|
| `federal-bca` | `ch06-reporting-and-decision-use.md` | Opportunity/Benefit | Decision Analysis & Trade Studies | Decision-use / OMB-facing choice documentation; map note already flags "also decision analysis" |
| `federal-bca` | `ch04-uncertainty-and-sensitivity.md` | Opportunity/Benefit | Decision Analysis & Trade Studies | Uncertainty/sensitivity is the A-94 decision-analysis method spine |
| `dod-vva-rpg` | `ch06-accreditation-agent-role.md` | Assurance & System Assurance | Decision Analysis & Trade Studies | Accreditation is the authority **decision**; 7-RESEARCH targeted cluster 16 for this pack |

Leave federal-bca ch01–ch03, ch05 (+ support files) in Opportunity so cluster 15 does not collapse. Leave dod-vva-rpg ch08 in Validation. Leave ch10 in Risk. Result after Phase 12 apply: Decision Analysis 2 → 5 entries, 2 → 4 packs. That is the Phase 12 SC-1 contract — **not** verified against live JSON this phase.

Annotate REQUIREMENTS IO-01 (keep `- [ ]`) by appending a Phase 11 sentence: remap specified (ch04, ch06 federal-bca + ch06 dod-vva-rpg); map apply is MAP-19-03; no CBA pack; live count leave-2 is Phase 12. If a Phase 11 sentence already exists, update in place — do not stack duplicates.

Optional pack-side nudge (not a map edit): in `packs/federal-bca/SKILL.md` Topic Index add one row `**Decision Analysis** → ch04, ch06` if missing. Do not retitle the pack. Do not reopen Army CBA.

**B. IO-05 / IO-06 / IO-07 honest records (Pattern 5).** Append dated parentheticals; boxes stay open. Template:

```
- [ ] **IO-0X**: <existing stem> — *Phase 11 (2026-08-17): DEFERRED/ACCEPT.
  Source: <name>. Why: <reachability / not vetted / no Tier-1 candidate>.
  Not built. Not invented. Unblock when: <in-source grant / official PDF>.
  See 11-RESEARCH.md §IO-0X.*
```

Exact outcomes:

- IO-05: **DEFERRED.** Source: AAF Software pathway guidebooks. Why: VET-19-03 still NOT yet vetted — do not use; no in-source grant; `dod-rio` AAF chapters do not licence AAF guidebooks. Not built. Unblock when official guidebook PDF opened + redistribution grant quoted.
- IO-06: **DEFERRED.** Source: AAF Product Support Manager Guidebook. Same why / unblock.
- IO-07: **ACCEPT.** No clean Tier-1/2 candidate; Phase 10 cleared no stakeholder source. Do not invent a facilitation pack. SEBoK rematch of ch26–ch28 is optional Phase 12 map judgement (likely wrong cluster) — not a substitute for accept.

**C. STATE.md Deviations / Notes** — add one dated bullet (do not duplicate if present): Phase 11 (2026-08-17): built nasa-std-8719-14 + is-gps-200n; extended dod-vva-rpg; IO-01 remap table specified (no map JSON); IO-05/06 deferred; IO-07 accept; SP-7084 skipped.

If execute writes 11-02-SUMMARY at the end of this plan, the remap table **must** appear there even if Task 2 ran first — copy it in at SUMMARY time if needed.
  </action>
  <verify>
    <automated>grep -n "IO-01" .planning/REQUIREMENTS.md && grep -E "ch04-uncertainty-and-sensitivity|ch06-reporting-and-decision-use|ch06-accreditation-agent-role" .planning/REQUIREMENTS.md && grep -n "DEFERRED" .planning/REQUIREMENTS.md && grep -n "ACCEPT" .planning/REQUIREMENTS.md && grep -n "Phase 11 (2026-08-17)" .planning/STATE.md && test "$(grep -c '^\- \[ \] \*\*IO-01\*\*' .planning/REQUIREMENTS.md)" = "1" && test "$(grep -c '^\- \[ \] \*\*IO-05\*\*' .planning/REQUIREMENTS.md)" = "1" && test "$(grep -c '^\- \[ \] \*\*IO-06\*\*' .planning/REQUIREMENTS.md)" = "1" && test "$(grep -c '^\- \[ \] \*\*IO-07\*\*' .planning/REQUIREMENTS.md)" = "1" && test ! -d packs/aaf-product-support && test ! -d packs/aaf-software && test ! -d packs/army-cba && test ! -d packs/stakeholder-engagement && test ! -d packs/nasa-sp-7084 && test -z "$(git diff --name-only -- docs/capability-pack-map.json)" && test "$(grep -c http docs/SOURCE-VETTING.md)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS IO-01 parenthetical names the three remap chapters and says map apply is MAP-19-03 / Phase 12
    - REQUIREMENTS IO-05 and IO-06 contain dated DEFERRED and AAF; IO-07 contains dated ACCEPT
    - all IO-01, IO-05, IO-06, IO-07 boxes remain `- [ ]`
    - STATE.md has a Phase 11 (2026-08-17) deviations bullet
    - no packs/aaf-*, army-cba, stakeholder-engagement, nasa-sp-7084
    - git diff --name-only -- docs/capability-pack-map.json is empty
    - grep -c http docs/SOURCE-VETTING.md equals 0
    - 11-02-SUMMARY (when written) contains heading `## IO-01 remap table (MAP-19-03 apply)` and the three-row table
  </acceptance_criteria>
  <done>IO-01 specified for Phase 12; IO-05/06 deferred; IO-07 accepted. No invented packs. Map JSON untouched. Boxes still open.</done>
</task>

<task type="auto">
  <name>Task 3: Thin-register nasa-std-8719-14 + is-gps-200n (not a version bump)</name>
  <files>catalog.json, SKILLS.md, NOTICE, .cursor-plugin/plugin.json, docs/packs.html, README.md</files>
  <read_first>catalog.json, SKILLS.md, NOTICE, .cursor-plugin/plugin.json, packs/nasa-std-8719-14/PACK.yaml, packs/is-gps-200n/PACK.yaml, packs/nasa-std-8719-14/SKILL.md, packs/is-gps-200n/SKILL.md</read_first>
  <action>
11-RESEARCH Pattern / code example "Thin-register exception". Both Wave-A dirs must already exist (depends_on 11-01). Mirror 7-03 Task 2 **for the two new slugs only**.

Do **not**: bump `.cursor-plugin/plugin.json` `version` (stays `1.18.0`); edit CHANGELOG; write RELEASE-INFO; tag `v1.19.0`; regenerate `docs/capability-pack-map.json`; run a version-surface commit.

Do:

1. `catalog.json`: append two objects copying key shape from `nasa-ms-7009` / `faa-std-025` (slug, title, publisher, source_version, license, license_tier, commercial_use, chapters, status `live`). Title/publisher/version/chapters **must match** each PACK.yaml (license in catalog may stay the short `Public Domain (US Government work)` form used by existing NASA/FAA rows). Bump top-level `updated` to `2026-08-17`. If thin-register is already touching the `dod-vva-rpg` object, you **may** bump its `chapters` integer to the new count; otherwise leave it for Phase 13 (validate_pack / check_release do not check that integer).

2. `SKILLS.md`: add two rows (description from each SKILL.md frontmatter, licence label `Public Domain (US Gov)`). Header becomes `63 packs (+2 signposts)` (61+2 new; signposts unchanged). Keep the generated-from-frontmatter disclaimer. Do not add sebok to cursor.

3. `NOTICE`: append two Public Domain blocks modeled on `[pack: nasa-ms-7009]` / `[pack: faa-std-025]`:
   - `[pack: nasa-std-8719-14]` — NASA-STD-8719.14C, NASA / OSMA, 17 U.S.C. 105
   - `[pack: is-gps-200n]` — IS-GPS-200 Rev N, USSF / SSC MilComm & PNT, 17 U.S.C. 105 + DIST-A
   No URLs.

4. `.cursor-plugin/plugin.json`: insert `./packs/nasa-std-8719-14/SKILL.md` and `./packs/is-gps-200n/SKILL.md` into the `skills` array (keep sort style consistent with existing `./packs/<slug>/SKILL.md` paths). **version remains `1.18.0`.** Eligible commercial pack count becomes 64 (sebok still excluded).

5. `python tooling/gen_packs_page.py` — never hand-edit `docs/packs.html`.

6. README badge: bump `packs-61` → `packs-63` so the human-facing count does not drift from SKILLS. Do not retitle the release as v1.19.0.

7. `python tooling/check_release.py` must PASS.

Expected basis (record in SUMMARY for Phase 13): dirs 65 / catalog 63 / SKILLS 63 (+2 signposts) / cursor 64 / README packs-63 / plugin version still 1.18.0.

Commit registration surfaces only (`chore(catalog): thin-register nasa-std-8719-14 and is-gps-200n`). Do not include pack trees (already committed). Zero `sources/`. Do not tag.
  </action>
  <verify>
    <automated>python -c "import json;c=json.load(open('catalog.json'));s={p['slug'] for p in c['packs']}; assert 'nasa-std-8719-14' in s and 'is-gps-200n' in s, s; print('catalog', len(c['packs']))" && test "$(python -c "import json;print(len(json.load(open('catalog.json'))['packs']))")" = "63" && grep -n "63 packs (+2 signposts)" SKILLS.md && grep -n "\[\`nasa-std-8719-14\`\]" SKILLS.md && grep -n "\[\`is-gps-200n\`\]" SKILLS.md && grep -n "\[pack: nasa-std-8719-14\]" NOTICE && grep -n "\[pack: is-gps-200n\]" NOTICE && python -c "import json;p=json.load(open('.cursor-plugin/plugin.json')); assert p['version']=='1.18.0', p['version']; slugs=set();
import re
for s in p['skills']:
    m=re.search(r'packs[/\\\\]([^/\\\\]+)[/\\\\]', s); slugs.add(m.group(1) if m else s)
assert 'nasa-std-8719-14' in slugs and 'is-gps-200n' in slugs
print('cursor', len(p['skills']), 'version', p['version'])" && grep -oE "packs-63" README.md && python tooling/check_release.py && test ! -d packs/nasa-sp-7084 && test -z "$(git diff --name-only -- docs/capability-pack-map.json)" && ! grep -q "1.19.0" .cursor-plugin/plugin.json && test "$(grep -c http docs/SOURCE-VETTING.md)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - catalog.json has 63 packs including nasa-std-8719-14 and is-gps-200n, both status live
    - SKILLS.md header is 63 packs (+2 signposts) and lists both new slugs
    - NOTICE has both new [pack:] blocks and no URL
    - plugin.json version is still 1.18.0 and lists both new skill paths (cursor 64)
    - README badge is packs-63
    - docs/packs.html was regenerated (gen_packs_page.py), not hand-patched
    - python tooling/check_release.py prints PASS
    - no v1.19.0 tag; plugin version not 1.19.0; capability-pack-map.json untouched
    - packs/nasa-sp-7084 does not exist
  </acceptance_criteria>
  <done>Thin-register only. Local release gate green on main. Version/tag remain Phase 13. Map remains Phase 12.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| DEBoK OTMM guest session → `sources/dod-vva-rpg/` | Chapter PDFs are untrusted build inputs, never committed |
| leftover RPG chapter → existing pack | Licence-safety per chapter (P7-PRE-4 + overlap); do not inherit clearance from shipped chapters blindly |
| Phase 11 remap table → live map JSON | Writing the table is in-scope; applying it is Phase 12. Premature JSON edit will be clobbered |
| new pack dirs on `main` → check_release counts | Unregistered dirs fail [index]/[cursor]; thin-register is the mechanical exception, not a version release |
| `.planning` URL store → packs/docs | Link Policy |

## STRIDE / Link Policy / source leak / leaning-as-cleared

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-11-01 | Information Disclosure (Link Policy) | extended pack + NOTICE/catalog | high | mitigate | No `http`/`https` in pack files or SOURCE-VETTING. Provenance is title + date only |
| T-11-03 | Elevation / spoofed Integration-Logistics unlock | packs/aaf-* | high | mitigate | IO-05/06 are records only. Verify asserts no aaf/stakeholder/CBA dirs |
| T-11-04 | Information Disclosure (source leak) | `sources/`, `full_text.txt` | high | mitigate | gitignored; Task 1 `git show` leak check |
| T-11-05 | Tampering (leaning / prior chapter treated as skip-confirm) | new RPG PDFs | high | mitigate | P7-PRE-4 on **each new** PDF. Halt that chapter on DIST B–F / all-rights |
| T-11-06 | Tampering (double-build IO-01) | `docs/capability-pack-map.json` | high | mitigate | files_modified excludes the map. Every task verify: `git diff --name-only -- docs/capability-pack-map.json` empty |
| T-11-07 | Repudiation / silent tick | REQUIREMENTS checkboxes | medium | mitigate | Boxes stay `- [ ]`. Dated DEFERRED/ACCEPT parentheticals. Verify greps unchecked IO lines |
| T-11-08 | Elevation (steal Phase 13) | plugin version / git tag | high | mitigate | version stays 1.18.0; no REL-19-02 tag; Task 3 verify asserts both |

No unmitigated high. Do not proceed if T-11-01 / T-11-03 / T-11-04 / T-11-06 is red.
</threat_model>

## Artifacts this phase produces

This plan (Wave B) produces:

- Extended `packs/dod-vva-rpg/` (ch11+ leftover RPG chapters, updated SKILL.md / PACK.yaml)
- Optional `packs/federal-bca/SKILL.md` Decision Analysis Topic Index row
- IO-01 remap table in `11-02-SUMMARY.md` (apply-ready for MAP-19-03)
- Dated IO-05/06 DEFERRED + IO-07 ACCEPT annotations in REQUIREMENTS.md + STATE.md bullet
- Thin-register: catalog.json, SKILLS.md, NOTICE, `.cursor-plugin/plugin.json` (version unchanged), docs/packs.html, README badge
- Basis recorded for Phase 13: 65 dirs / 63 catalog / 63(+2) SKILLS / 64 cursor / plugin 1.18.0

Not this plan: map regen, MAP-19-02 floor, HYG-*, CHANGELOG / GitHub Release / v1.19.0 tag, SP-7084 pack.

## Deviations ledger (executor)

If execute must diverge from this plan, append a bullet under **Deviations** in `.planning/phases/11-io-unlocking-packs-decision-analysis-remap/11-02-SUMMARY.md`:

- What changed
- Why
- Which IO-01 / IO-02 / IO-05 / IO-06 / IO-07 acceptance is affected

If no divergence: write `None.` Empty/None is valid.

Valid pre-authorized deviations (still record them):

- Checklist fetch failed → IO-02 partial deferral (no DoDM substitute)
- UCO is HTML-only → skip UCO; Checklist + ≤2 other PDFs still satisfy the lean set
- Thin-register already present from a re-run → update in place; do not duplicate catalog objects

Do not flip AAF to built. Do not invent a stakeholder or CBA pack. Do not apply the remap to the JSON.

<verification>
After Task 3: `python tooling/validate_pack.py packs/dod-vva-rpg && python tooling/validate_pack.py packs/nasa-std-8719-14 && python tooling/validate_pack.py packs/is-gps-200n && python tooling/check_release.py` all PASS; IO-01 table present in SUMMARY; IO-05/06/07 dated records; `git diff --name-only -- docs/capability-pack-map.json` empty; `grep -c http docs/SOURCE-VETTING.md` = 0; no forbidden slugs; plugin version 1.18.0; no v1.19.0 tag.
</verification>

<success_criteria>
IO-01 specified (not applied). IO-02 extended (or honestly deferred). IO-05/06 deferred. IO-07 accepted. Thin-register keeps check_release green without stealing REL-19-02. ROADMAP SC-2/SC-3/SC-4 recorded honestly. SC-1 leave-2 handed to Phase 12. SC-5 held on every built/extended pack.
</success_criteria>

<output>
Create `.planning/phases/11-io-unlocking-packs-decision-analysis-remap/11-02-SUMMARY.md` when done (must include `## IO-01 remap table (MAP-19-03 apply)`)
</output>
