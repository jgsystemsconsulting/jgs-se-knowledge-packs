---
phase: 11-io-unlocking-packs-decision-analysis-remap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packs/nasa-std-8719-14/**
  - packs/is-gps-200n/**
autonomous: true
requirements: [IO-03, IO-04]
estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "packs/nasa-std-8719-14 exists and passes tooling/validate_pack.py (IO-03 / ROADMAP SC-5)"
    - "P11-PRE-1 third-party insert scan was run on the extracted 8719.14C copy; PACK.yaml notes quote the finding (Internet Public / title-page; no contrary all-rights insert). Tier 1 leaning is not skip-confirm"
    - "packs/is-gps-200n exists and passes tooling/validate_pack.py (IO-04 / ROADMAP SC-5)"
    - "P11-PRE-2 DIST-A sentence is present in the extracted IS-GPS-200N copy and quoted in PACK.yaml notes; SAIC contractor line recorded as watch-item only"
    - "is-gps-200n is an ICD exemplar (SKILL.md Scope & Limits names faa-std-025); Apps II–IV were not transcribed; 705J/800J/IS-300/ICD-GPS-153 were not ingested"
    - "Every built SKILL.md has ## When to use immediately followed by **Prerequisites:**; no TODO in either PACK.yaml"
    - "check_overlap.py exits 0 for each pack; scan_generated_skill dispositioned in notes; chars/page >= 300"
    - "No https:// (or http) in either pack tree; no sources/ or full_text.txt in either scoped commit"
    - "No Army CBA / AAF / stakeholder / SP-7084 / dodm-5000-102 pack; docs/capability-pack-map.json untouched; REL-19-02 tag not created"
    - "ROADMAP SC-2 Ops/Maint + Interface Management each gained a new pack (this plan). Decision Analysis leave-2 is NOT a live map contract here"
    - "EDGE_ABSENT=1: unclassified IO probes stay unresolved assumptions — do not invent check_kind / check_target. IO-01 numeric leave-2 is Phase 12"
    - "Idempotency: re-run updates existing pack trees in place; do not re-scaffold a slug that already exists"
    - "Concurrency: stay on main; Task 2 may follow Task 1 in the same session; exclusive ownership of each slug"
  artifacts:
    - path: packs/nasa-std-8719-14/SKILL.md
      provides: "IO-03 index + When-to-use/Prerequisites + chapter/topic indexes"
      contains: "## When to use"
    - path: packs/nasa-std-8719-14/PACK.yaml
      provides: "8719.14C provenance + P11-PRE-1 quote + license_tier 1"
      contains: "NASA-STD-8719.14C"
    - path: packs/nasa-std-8719-14/LICENSE
      provides: "17 U.S.C. § 105 statute prose; no URL"
      contains: "17 U.S.C."
    - path: packs/nasa-std-8719-14/chapters/
      provides: "6–7 synthesized chapters (postmission disposal = gold)"
    - path: packs/is-gps-200n/SKILL.md
      provides: "IO-04 exemplar index; faa-std-025 cross-link"
      contains: "faa-std-025"
    - path: packs/is-gps-200n/PACK.yaml
      provides: "IS-GPS-200N provenance + DIST-A licence string + P11-PRE-2 quote"
      contains: "Distribution Statement A"
    - path: packs/is-gps-200n/LICENSE
      provides: "statute + DIST-A prose; no URL"
    - path: packs/is-gps-200n/chapters/
      provides: "5–6 exemplar chapters; no App II–IV dump"
  key_links:
    - from: packs/nasa-std-8719-14/SKILL.md
      to: packs/nasa-std-8719-14/chapters/
      via: "Chapter Index links resolve"
      pattern: "chapters/ch0"
    - from: packs/is-gps-200n/SKILL.md
      to: packs/faa-std-025/SKILL.md
      via: "Scope & Limits complementary routing"
      pattern: "faa-std-025"
    - from: packs/nasa-std-8719-14/PACK.yaml
      to: docs/SOURCE-VETTING.md
      via: "P11-PRE-1 notes quote Internet Public / title-page; no URL"
      pattern: "Internet Public"

# Prohibitions (must-NOT). No check_kind / check_target fields.
# - must-NOT build an Army CBA pack / AAF pack / stakeholder pack / SP-7084 / dodm-5000-102
# - must-NOT put URLs in packs (or docs/SOURCE-VETTING.md)
# - must-NOT edit docs/capability-pack-map.json
# - must-NOT commit sources/ or full_text.txt
# - must-NOT ingest GPS Apps II–IV payloads / IS-300 / ICD-GPS-153 / 705J / 800J
# - must-NOT treat Tier 1 leaning as skip-build-time-confirm
# - must-NOT tag REL-19-02 / bump plugin version / write CHANGELOG IO narrative
# - must-NOT check IO-03/IO-04 boxes (verify does that)
---

<objective>
Wave A (11-RESEARCH recommended shape): build the two Phase 10 GO packs — `nasa-std-8719-14` (IO-03, NASA-STD-8719.14C) and `is-gps-200n` (IO-04 ICD exemplar, IS-GPS-200N only) — via the Phase 7 / Pattern 1 pipeline, with build-time P11-PRE-1 third-party scan and P11-PRE-2 DIST-A confirmation on the copies actually extracted.

Purpose: fatten Ops/Maint/Disposal and Interface Management primaries with licence-clean packs. Registration, remap, deferrals, and map JSON are out of this plan.
Output: two validated pack trees under packs/, one scoped commit per pack. Catalog/version stay Phase 13 except the 11-02 thin-register exception.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-io-unlocking-packs-decision-analysis-remap/11-RESEARCH.md
@.planning/phases/11-io-unlocking-packs-decision-analysis-remap/11-PATTERNS.md
@docs/PACK-SPEC.md
@docs/SOURCE-VETTING.md
@packs/nasa-ms-7009/PACK.yaml
@packs/nasa-ms-7009/SKILL.md
@packs/nasa-ms-7009/LICENSE
@packs/faa-std-025/SKILL.md
@packs/faa-std-025/PACK.yaml
</context>

<plan_notes>
Discuss was skipped (no CONTEXT.md). Locked decisions come from 11-RESEARCH user_constraints + Phase 10 GO/NO-GO. Do not re-vet.

TRACER_MODE: these two GO packs are the thin slice. REVERSIBILITY: delete the two pack dirs + revert the two commits; sources/ stay gitignored.

Stay on main. No branches / worktrees. REF = `C:/Users/gower/OneDrive/Documents/GitHub/jgs-reference-skill`. Use `python`.

URLs in this plan are research-store only (`.planning`). Never copy a URL into packs/.
</plan_notes>

<claim_verification>
Live commands run this planner session (2026-08-17), cwd repo root. Do not invent replacements.

| claim | command | observed | status |
|---|---|---|---|
| Branch is main | `git branch --show-current` | `main` | VERIFIED |
| 63 pack dirs today; neither new slug exists | `ls packs \| wc -l`; `ls packs \| grep -E 'nasa-std-8719\|is-gps'` | `63`; empty | VERIFIED |
| Catalog 61; SKILLS "61 packs (+2 signposts)"; cursor 62; README packs-61 | python len(catalog packs); `head` SKILLS.md:9; plugin skills len; `grep -oE 'packs-[0-9]+' README.md` | `61` / `61 packs (+2 signposts)` / `62` / `packs-61` | VERIFIED |
| New slugs absent from catalog / SKILLS / NOTICE | python slug membership; `grep -c '[`nasa-std-8719-14`]' SKILLS.md`; NOTICE pack-block grep | False/False; `0`; `0` | VERIFIED |
| Analog trees exist with full PACK-SPEC file list | `ls packs/nasa-ms-7009 packs/faa-std-025` | SKILL.md PACK.yaml LICENSE chapters/ glossary.md patterns.md cheatsheet.md | VERIFIED |
| Sibling REF pipeline tools exist | `ls $REF/tools/{vet_source,outline,build_pack,scan_generated_skill,check_overlap}.py $REF/scripts/extract.py` | all present | VERIFIED |
| Repo tooling present | `ls tooling/*.py` | build_pack, check_capability_map, check_release, gen_packs_page, validate_pack | VERIFIED |
| check_release currently PASS (no new dirs yet) | `python tooling/check_release.py` | `RELEASE CHECK: PASS — repo is release-ready against the mechanical gate.` | VERIFIED |
| Link Policy on SOURCE-VETTING | `grep -c http docs/SOURCE-VETTING.md` | `0` | VERIFIED |
| `sources/` is gitignored | `grep -n "sources/" .gitignore` | `17:sources/` | VERIFIED |
| Phase 11 handoff GO names | `grep -n "Phase 11 handoff" docs/SOURCE-VETTING.md` | line 168 `### Phase 11 handoff (v1.19.0)` | VERIFIED |
| Map JSON exists and is Phase 12-owned (do not edit) | `python` cluster counts on `docs/capability-pack-map.json` | schema 2, map_version 1.18.0; Decision Analysis 2/2; Interfaces 4/3; Ops/Maint 6/4 | VERIFIED |
| IO-03/IO-04 boxes still open | `grep -n "IO-03\\|IO-04" .planning/REQUIREMENTS.md` | both `- [ ]` with Phase 10 GO handoff notes | VERIFIED |
</claim_verification>

<tasks>

<task type="auto">
  <name>Task 1: Build packs/nasa-std-8719-14 (IO-03) — P11-PRE-1 third-party scan</name>
  <files>packs/nasa-std-8719-14/**</files>
  <read_first>.planning/phases/11-io-unlocking-packs-decision-analysis-remap/11-RESEARCH.md, docs/PACK-SPEC.md, packs/nasa-ms-7009/PACK.yaml, packs/nasa-ms-7009/SKILL.md, packs/nasa-ms-7009/LICENSE, docs/SOURCE-VETTING.md</read_first>
  <action>
Execute 11-RESEARCH Pattern 1 for slug `nasa-std-8719-14`. Analog file list + LICENSE/PACK.yaml prose: `packs/nasa-ms-7009/`. Do not create any other slug.

1. `mkdir -p sources/nasa-std-8719-14`. Download the official NTSS PDF into that dir (research-store URL only — from 11-RESEARCH §IO-03 / 10-RESEARCH; never write the URL into the pack):
   `https://standards.nasa.gov/sites/default/files/standards/NASA/C/0/nasa-std-871914c.pdf`
   Expect ~905,584 bytes, `%PDF-1.7`, 77 pp. Record NTSS record title-page identity: NASA-STD-8719.14C, Process for Limiting Orbital Debris, Approved 2021-11-05, ACTIVE, supersedes 8719.14B.

2. VET FIRST: `python "$REF/tools/vet_source.py" --title "Process for Limiting Orbital Debris" --publisher "NASA" --license "Public Domain (US Government work, 17 U.S.C. § 105)"`. Expect tier 1, exit 0. Third-party-quote advisory is expected, not a blocker.

3. EXTRACT: `python "$REF/scripts/extract.py" sources/nasa-std-8719-14/*.pdf --mode technical --install-missing no`. Copy `book_skill_work` under `sources/nasa-std-8719-14/`. Write work root with `printf '%s'` to `sources/nasa-std-8719-14/work_dir.txt`. Read later via `tr -d '\r\n'`.

4. **P11-PRE-1 (hard gate — before GENERATE):** grep the extracted `full_text.txt` for `Copyright`, `All rights`, third-party insert markers. IADC / UN / SPD-3 citations as *consistency* are allowed; do **not** package NASA-HDBK-8719.14. Halt and surface if a contrary all-rights / contractor-owned insert appears. Quote Internet Public + title-page lines (no URL) into PACK.yaml notes. Tier 1 leaning is not skip-confirm.

5. chars/page floor: `len(full_text.txt) / metadata.json pages` must be >= 300. Fail = wrong/scan PDF → re-fetch, do not generate.

6. OUTLINE: `python "$REF/tools/outline.py" --source "$WRK/book_skill_work/full_text.txt" --out sources/nasa-std-8719-14/outline.json`.

7. SCAFFOLD (folder must not already exist): `$REF/tools/build_pack.py` with `--out-dir packs` **or** `python tooling/build_pack.py --slug nasa-std-8719-14 --title "Process for Limiting Orbital Debris (NASA-STD-8719.14C)" --publisher "NASA" --version "NASA-STD-8719.14C (Approved 2021-11-05)" --license "Public Domain (US Government work, 17 U.S.C. § 105)" --tier 1 --commercial-use true`.

8. GENERATE per PACK-SPEC (transform, do not transcribe). Required files: `SKILL.md`, `PACK.yaml`, `LICENSE`, `chapters/chNN-*.md`, plus analog support files `glossary.md`, `patterns.md`, `cheatsheet.md`. Chapter files use Core Idea / Frameworks Introduced / Key Concepts / Mental Models / Anti-patterns / Key Takeaways / Connects To.

   Chapter strategy (6–7 chapters; 11-RESEARCH §IO-03 table):
   - ch01 — §1 + NPR 8715.6 frame / applicability (Ops/Maint/Disposal; Standards)
   - ch02 — §4.1–4.2 assessment overview (Ops/Maint/Disposal)
   - ch03 — §4.3 debris released during normal operations
   - ch04 — §4.4–4.5 explosions/breakups + collisions (Safety secondary)
   - ch05 — §4.6 postmission disposal (**Ops/Maint/Disposal primary gold**)
   - ch06 — §4.7 reentry surviving debris
   - ch07 — §4.8 + App A/B ODAR / EOMP (Governance secondary)

   Skip dumping every figure/table. Do not ingest NASA-HDBK-8719.14.

   SKILL.md body order (PACK-SPEC): `## When to use` immediately followed by `**Prerequisites:** none — plain Markdown; no MCP server, API key, or licence tier needed at runtime.` Then How to Use / Core Frameworks / Chapter Index / Topic Index / Supporting Files / Scope & Limits. Body < ~4,000 tokens. Topic Index vocabulary: Operations, Maintenance, Disposal, orbital debris, ODAR, EOMP.

   PACK.yaml (11-PATTERNS): `license_tier: 1`, `commercial_use: true`, `share_alike: false`, `attribution_required: false`, `source_pages` from metadata.json (expect ~77), `chapters` = generated count, `built_on: "2026-08-17"`, `change_indication: "reconstructed as synthesized reference notes"`, notes include P11-PRE-1 quote + Internet Public + scan disposition. No `source_url`. No TODO stubs.

   LICENSE: copy nasa-ms-7009 statute pattern; identify NASA-STD-8719.14C + OSMA + Approved 2021-11-05 textually; "no source-material download link"; pack-content licence independent of repo MIT. No URL.

9. VALIDATE + SCAN + OVERLAP:
   `python tooling/validate_pack.py packs/nasa-std-8719-14`
   `python "$REF/tools/scan_generated_skill.py" packs/nasa-std-8719-14` — disposition in notes
   `python "$REF/tools/check_overlap.py" --source "$WRK/book_skill_work/full_text.txt" --pack packs/nasa-std-8719-14` — exit 0 mandatory

10. Commit only `packs/nasa-std-8719-14` (`feat(packs): add nasa-std-8719-14 (Tier 1)`). `git show --name-only` must have zero `sources/` or `full_text.txt`. Do not register. Do not edit the map. Do not check the IO-03 box.
  </action>
  <verify>
    <automated>REF="C:/Users/gower/OneDrive/Documents/GitHub/jgs-reference-skill"; WRK=$(cat sources/nasa-std-8719-14/work_dir.txt | tr -d '\r\n') && python tooling/validate_pack.py packs/nasa-std-8719-14 && python "$REF/tools/check_overlap.py" --source "$WRK/book_skill_work/full_text.txt" --pack packs/nasa-std-8719-14 && python "$REF/tools/scan_generated_skill.py" packs/nasa-std-8719-14 && test "$(grep -c '^## When to use' packs/nasa-std-8719-14/SKILL.md)" -ge 1 && test "$(grep -c '^\*\*Prerequisites:\*\*' packs/nasa-std-8719-14/SKILL.md)" -ge 1 && ! grep -qi "TODO" packs/nasa-std-8719-14/PACK.yaml && grep -q "NASA-STD-8719.14C" packs/nasa-std-8719-14/PACK.yaml && grep -qi "Internet Public" packs/nasa-std-8719-14/PACK.yaml && ! grep -R -qE "https?://" packs/nasa-std-8719-14 && test ! -d packs/nasa-hdbk-8719-14 && python -c "import json,sys,pathlib;w=pathlib.Path('$WRK'.replace(chr(92),'/'))/'book_skill_work';ft=(w/'full_text.txt').read_text(errors='ignore');m=json.load(open(w/'metadata.json'));pg=m.get('pages') or m.get('num_pages');c=len(ft)/pg;print('nasa-std-8719-14 chars/page:',round(c,1));sys.exit(0 if c>=300 else 1)" && test -z "$(git show --name-only --pretty=format: HEAD | grep -E 'sources/|full_text.txt')" && test -z "$(git diff --name-only -- docs/capability-pack-map.json)"</automated>
  </verify>
  <acceptance_criteria>
    - packs/nasa-std-8719-14/{SKILL.md,PACK.yaml,LICENSE,chapters/} exist
    - validate_pack.py exits 0; check_overlap.py exits 0
    - SKILL.md has ## When to use immediately followed by **Prerequisites:**
    - PACK.yaml has NASA-STD-8719.14C, license_tier 1, P11-PRE-1 / Internet Public quote, no TODO
    - 6–7 chapter files; Chapter Index links resolve
    - chars/page >= 300 on the extracted copy
    - no http/https in the pack tree; HEAD commit has no sources/ or full_text.txt
    - docs/capability-pack-map.json unchanged; IO-03 checkbox still `- [ ]`
    - no NASA-HDBK-8719.14 pack
  </acceptance_criteria>
  <done>IO-03 pack gated: P11-PRE-1 recorded, validators pass, When-to-use/Prerequisites present, one scoped commit, zero URL/source leak.</done>
</task>

<task type="auto">
  <name>Task 2: Build packs/is-gps-200n (IO-04) — P11-PRE-2 DIST-A, exemplar not dump</name>
  <files>packs/is-gps-200n/**</files>
  <read_first>.planning/phases/11-io-unlocking-packs-decision-analysis-remap/11-RESEARCH.md, docs/PACK-SPEC.md, packs/faa-std-025/SKILL.md, packs/faa-std-025/PACK.yaml, packs/faa-std-025/LICENSE, docs/SOURCE-VETTING.md</read_first>
  <action>
Repeat the Task 1 pipeline (same gates: vet → extract → chars/page >= 300 → outline → scaffold → generate → validate → scan → overlap → scoped commit) for slug `is-gps-200n`. Analog role: `packs/faa-std-025/` (ICD *preparation* pack — this pack is the worked ICD). Do **not** search for IS-300. Do **not** package ICD-GPS-153. Do **not** build 705J/800J.

1. `mkdir -p sources/is-gps-200n`. Download official gps.gov PDF (research-store URL only):
   `https://www.gps.gov/sites/default/files/2025-07/IS-GPS-200N.pdf`
   Expect ~3,338,120 bytes, `%PDF-1.6`. Identity: NAVSTAR GPS Space Segment / Navigation User Segment Interfaces; IS-GPS-200 Rev N; SSC / MilComm & PNT.

2. VET: `python "$REF/tools/vet_source.py" --title "NAVSTAR GPS Space Segment / Navigation User Segment Interfaces" --publisher "US Space Force / Space Systems Command (MilComm & PNT)" --license "Public Domain (US Government work, 17 U.S.C. § 105; Distribution Statement A — Approved for public release; distribution is unlimited)"`. Expect tier 1, exit 0.

3. EXTRACT (`--mode technical --install-missing no`); copy book_skill_work; `printf '%s'` → `sources/is-gps-200n/work_dir.txt`.

4. **P11-PRE-2 (hard gate — before GENERATE):** the DIST-A sentence must appear in **this** extracted copy (`DISTRIBUTION STATEMENT A` / `Approved for public release` / `Distribution is unlimited`). Halt if absent. Record the SAIC Interface Control Contractor line in PACK.yaml notes as a **watch-item only** (not an all-rights-reserved notice). Quote DIST-A verbatim into notes. Tier 1 leaning is not skip-confirm.

5. chars/page >= 300. OUTLINE from full_text. SCAFFOLD with:
   - title: `NAVSTAR GPS Space Segment / Navigation User Segment Interfaces (IS-GPS-200N)`
   - publisher: `US Space Force / Space Systems Command (MilComm & PNT)`
   - version: `IS-GPS-200 Rev N (cover date 01-AUG-2022; public-release revision record)`
   - licence: DIST-A variant string (exact, 11-RESEARCH Standard Stack)
   - tier 1, commercial_use true

6. GENERATE **exemplar, not dump** (11-RESEARCH Pattern 6 + §IO-04 table). 5–6 chapters:
   - ch01 — §1 + cover/change control: what an IS/ICD is; DIST-A; revision/IRN discipline. Skip contractor street address as content.
   - ch02 — §3.1–3.2: interface definition vs identification; document-as-contract. Skip chip-by-chip P/Y/C/A/CM/CL tables.
   - ch03 — §3.3.1: interface-criteria pattern (freq plan, levels, phasing, polarization) as ICD shalls. Skip numeric RF dumps beyond illustrating the pattern.
   - ch04 — §3.2.2 + §3.3.3: NAV data as interface payload (message families, not bits). Skip FEC polynomials / bit fields.
   - ch05 — §3.3.4 + §6: Time/Z-count + definition hygiene (URA, CEI, reserved/invalid). Skip SV-block trivia.
   - ch06 — Apps I–IV **as a map**: how annexes carry normative payload; when to open which appendix. **Do not transcribe Apps II–IV** (no PRN/Gold-code tables, no CNAV/LNAV bit fields).

   SKILL.md Scope & Limits **must** state this pack complements `faa-std-025` (preparation rules, not a live ICD) — same slug-distinction pattern as dote vs dod-te-guidebook. Topic Index: Interface Management, ICD/IS, change/IRN, Requirements Traceability, CM.

   PACK.yaml licence = DIST-A variant; notes quote P11-PRE-2 DIST-A + SAIC watch-item + scan disposition; `source_pages` from metadata.json; no TODO; no URL.

   LICENSE: statute + DIST-A prose; identify IS-GPS-200 Rev N + SSC / MilComm & PNT textually; no URL.

7. VALIDATE + SCAN + OVERLAP (exit 0). Confirm chapter bodies do not contain long transcribed NAV-bit / PRN tables (overlap gate is the mechanical backstop).

8. Commit only `packs/is-gps-200n` (`feat(packs): add is-gps-200n (Tier 1 ICD exemplar)`). Zero `sources/` / `full_text.txt`. Do not register. Do not edit the map. Do not check the IO-04 box. Do not create `packs/gps-is-200n` (slug is locked to `is-gps-200n`).
  </action>
  <verify>
    <automated>REF="C:/Users/gower/OneDrive/Documents/GitHub/jgs-reference-skill"; WRK=$(cat sources/is-gps-200n/work_dir.txt | tr -d '\r\n') && python tooling/validate_pack.py packs/is-gps-200n && python "$REF/tools/check_overlap.py" --source "$WRK/book_skill_work/full_text.txt" --pack packs/is-gps-200n && python "$REF/tools/scan_generated_skill.py" packs/is-gps-200n && test "$(grep -c '^## When to use' packs/is-gps-200n/SKILL.md)" -ge 1 && test "$(grep -c '^\*\*Prerequisites:\*\*' packs/is-gps-200n/SKILL.md)" -ge 1 && grep -qi "faa-std-025" packs/is-gps-200n/SKILL.md && ! grep -qi "TODO" packs/is-gps-200n/PACK.yaml && grep -qi "Distribution Statement A" packs/is-gps-200n/PACK.yaml && grep -qi "SAIC" packs/is-gps-200n/PACK.yaml && grep -Eqi "DISTRIBUTION STATEMENT A|Approved for public release" "$WRK/book_skill_work/full_text.txt" && ! grep -R -qE "https?://" packs/is-gps-200n && test ! -d packs/is-gps-705j && test ! -d packs/is-gps-800j && test ! -d packs/icd-gps-153 && test ! -d packs/gps-is-200n && python -c "import json,sys,pathlib;w=pathlib.Path('$WRK'.replace(chr(92),'/'))/'book_skill_work';ft=(w/'full_text.txt').read_text(errors='ignore');m=json.load(open(w/'metadata.json'));pg=m.get('pages') or m.get('num_pages');c=len(ft)/pg;print('is-gps-200n chars/page:',round(c,1));sys.exit(0 if c>=300 else 1)" && test -z "$(git show --name-only --pretty=format: HEAD | grep -E 'sources/|full_text.txt')" && test -z "$(git diff --name-only -- docs/capability-pack-map.json)"</automated>
  </verify>
  <acceptance_criteria>
    - packs/is-gps-200n/{SKILL.md,PACK.yaml,LICENSE,chapters/} exist
    - validate_pack.py exits 0; check_overlap.py exits 0
    - SKILL.md has When-to-use + Prerequisites and names faa-std-025
    - PACK.yaml uses the DIST-A licence string, quotes P11-PRE-2, records SAIC as watch-item, no TODO
    - extracted full_text contains DISTRIBUTION STATEMENT A / Approved for public release
    - 5–6 chapters; ch06 (if present) maps appendices and does not transcribe App II–IV payloads
    - no packs/is-gps-705j, is-gps-800j, icd-gps-153, gps-is-200n, or IS-300 slug
    - no http/https in the pack tree; HEAD commit has no sources/ or full_text.txt
    - docs/capability-pack-map.json unchanged; IO-04 checkbox still `- [ ]`
  </acceptance_criteria>
  <done>IO-04 exemplar gated: P11-PRE-2 DIST-A on this copy, faa-std-025 cross-link, no appendix dump, one scoped commit, zero URL/source leak.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| internet → `sources/` | Official PDFs are untrusted build inputs, never committed |
| extracted copy → Phase 10 leaning verdict | A different mirror/file can lack DIST-A or carry a third-party insert |
| source text → pack content | Licence-safety: verbatim runs must not cross (`check_overlap`) |
| `.planning` URL store → `packs/` | Link Policy: URLs must not leak into published pack files |

## STRIDE / Link Policy

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-11-01 | Information Disclosure (Link Policy) | packs/nasa-std-8719-14 | high | mitigate | No `http`/`https` in pack files. NASA host is `standards.nasa.gov` (matches `nasa.gov` SOURCE_HOSTS / CI grep). Task 1 verify greps the tree |
| T-11-02 | Information Disclosure (Link Policy) + spoofed ICD | packs/is-gps-200n | high | mitigate | No URLs in pack. DIST-A must be on **this** extract (P11-PRE-2). Exemplar-not-dump reduces overlap/IP risk |
| T-11-04 | Information Disclosure (source leak) | `sources/`, `full_text.txt` | high | mitigate | gitignored `sources/`; per-task `git show --name-only` leak check |
| T-11-05 | Tampering / spoofed clearance | Tier 1 *leaning* treated as skip-confirm | high | mitigate | P11-PRE-1 and P11-PRE-2 are hard gates before GENERATE. Halt on contrary notice or missing DIST-A |
| T-11-06 | IP theft (verbatim) | chapters/ | high | mitigate | `check_overlap.py` exit 0; do not transcribe Apps II–IV |
| T-11-07 | Elevation (scope creep) | forbidden slugs / map JSON / tag | high | mitigate | files_modified is the two new pack trees only; verify asserts map JSON untouched and no 705J/800J/153/SP-7084/AAF/CBA dirs |

No unmitigated high. Do not proceed if T-11-01 / T-11-02 / T-11-04 / T-11-05 is red.
</threat_model>

## Artifacts this phase produces

This plan (Wave A) produces:

- `packs/nasa-std-8719-14/{SKILL.md,PACK.yaml,LICENSE,chapters/ch01–ch07,glossary.md,patterns.md,cheatsheet.md}`
- `packs/is-gps-200n/{SKILL.md,PACK.yaml,LICENSE,chapters/ch01–ch06,glossary.md,patterns.md,cheatsheet.md}`
- gitignored extract work under `sources/nasa-std-8719-14/` and `sources/is-gps-200n/` (never committed)
- two scoped pack commits

Not this plan: catalog/SKILLS/NOTICE/cursor (11-02 thin-register), IO-01 remap table, IO-02 extend, IO-05/06/07 records, map JSON, version/tag.

## Deviations ledger (executor)

If execute must diverge from this plan, append a bullet under **Deviations** in `.planning/phases/11-io-unlocking-packs-decision-analysis-remap/11-01-SUMMARY.md`:

- What changed
- Why
- Which IO-03 / IO-04 acceptance is affected

If no divergence: write `None.` Empty/None is valid.

Do not flip a NO-GO (Army CBA, DoDM 5000.102, AAF, SP-7084-as-must, IS-300) into a build. Do not skip P11-PRE-1 / P11-PRE-2 because Phase 10 said "leaning Tier 1".

<verification>
After Task 2: `python tooling/validate_pack.py packs/nasa-std-8719-14 && python tooling/validate_pack.py packs/is-gps-200n` passes; `git log --oneline -2` shows one commit per pack; `git status` clean of `sources/` paths; pack dir count is 65 (63+2); catalog still 61 (thin-register is 11-02); `git diff --name-only -- docs/capability-pack-map.json` empty; `grep -c http docs/SOURCE-VETTING.md` still `0`.
</verification>

<success_criteria>
Two GO packs built and gated (IO-03, IO-04 / ROADMAP SC-2 for Ops/Maint + Interfaces, SC-5). P11-PRE-1 and P11-PRE-2 recorded on the extracted copies. Exemplar-not-dump held. No URL leak, no source leak, no map edit, no version/tag, no forbidden packs.
</success_criteria>

<output>
Create `.planning/phases/11-io-unlocking-packs-decision-analysis-remap/11-01-SUMMARY.md` when done
</output>
