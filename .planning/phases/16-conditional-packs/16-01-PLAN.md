---
phase: 16-conditional-packs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/SOURCE-VETTING.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: [PACK-20-01, PACK-20-02, PACK-20-03]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Phase 15 handoff remains 2 NO-GO + 1 document-only; Phase 16 consumed it and did not flip any row to GO"
    - "PACK-20-01 is recorded deferred-with-evidence dated 2026-08-20; no Army CBA / Decision Analysis pack directory exists"
    - "PACK-20-02 is recorded deferred-with-evidence dated 2026-08-20; IO-05 stays deferred; no AAF Software / Integration pack directory exists"
    - "PACK-20-03 is recorded deferred-with-evidence dated 2026-08-20; IO-06 stays deferred; no AAF Product Support / Logistics pack directory exists"
    - "Live REQUIREMENTS.md PACK-20-01..03 boxes remain unchecked and each line carries a 2026-08-20 deferred parenthetical"
    - "STATE.md Deviations/Notes has a Phase 16 (2026-08-20) PACK-20 deferred-with-evidence bullet; YAML frontmatter progress fields are untouched"
    - "scheme-string count on docs/SOURCE-VETTING.md remains 0"
    - "git diff --name-only -- packs/ is empty; no catalog or tooling path is edited"
    - "Idempotency (explicit): re-running updates existing PACK-20 suffixes / parentheticals / STATE bullet in place; it does not duplicate the Phase 16 record sentence or a second handoff table"
    - "Concurrency (explicit): single writer on SOURCE-VETTING.md then REQUIREMENTS.md/STATE.md; interrupted run re-reads and applies only missing scoped edits"
  artifacts:
    - path: docs/SOURCE-VETTING.md
      provides: "PACK-20-01..03 deferred-with-evidence suffixes on the existing v1.19.1 Not-cleared bullets plus one Phase 16 record sentence after the existing handoff table"
      contains: "PACK-20-01"
    - path: .planning/REQUIREMENTS.md
      provides: "PACK-20-01..03 parentheticals (boxes still open)"
      contains: "deferred"
    - path: .planning/STATE.md
      provides: "Phase 16 deviations bullet with PACK-20 deferred-with-evidence honesty"
      contains: "Phase 16 (2026-08-20)"
  key_links:
    - from: docs/SOURCE-VETTING.md
      to: Phase 15 handoff table
      via: "existing ### Phase 16 handoff (v1.19.1) still 2 NO-GO + document-only; Phase 16 record sentence cites that table"
      pattern: "Phase 16 handoff"
    - from: .planning/REQUIREMENTS.md
      to: docs/SOURCE-VETTING.md
      via: "PACK-20 parentheticals match register deferrals"
      pattern: "PACK-20-01"
    - from: .planning/STATE.md
      to: docs/SOURCE-VETTING.md
      via: "Phase 16 deviations bullet names PACK-20-01..03 deferred-with-evidence"
      pattern: "PACK-20-01..03"

# Prohibitions (must-NOT). No check_kind / check_target fields.
# - must-NOT put scheme strings in docs/SOURCE-VETTING.md (Link Policy)
# - must-NOT create packs/ or sources/ trees, or run extract.py / catalog / vet_source.py / build_pack.py
# - must-NOT invent redistribution rights or an Army CBA / AAF pack
# - must-NOT flip Phase 16 handoff rows from NO-GO / document-only to GO
# - must-NOT insert a second Phase 16 handoff table
# - must-NOT check PACK-20-01..03 boxes (verify / phase.complete does that)
# - must-NOT uncheck already-complete VET-20-01..03 boxes
# - must-NOT git add -A / git add . ; explicit pathspecs only; stay on main
---

<objective>
Record PACK-20-01..03 as deferred-with-evidence because Phase 15 handoff is NO-GO for Army CBA and AAF (GO cells = 0). Zero packs built. Licence-clean core value holds: uncleared sources stay unused.

Purpose: Phase 17/18 and the v1.19.1 CHANGELOG consume an on-record PACK-20 deferral instead of re-guessing whether Phase 16 built packs. Deferred-with-evidence is the valid done state for these three requirements.

Output: SOURCE-VETTING PACK-20 suffixes + one Phase 16 record sentence + REQUIREMENTS/STATE annotations. Zero packs. Zero scheme strings in the published register. PACK-20 boxes stay open.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/STATE.md
.planning/phases/16-conditional-packs/16-RESEARCH.md
.planning/phases/16-conditional-packs/16-PATTERNS.md
docs/SOURCE-VETTING.md
.planning/phases/15-source-retries/15-VERIFICATION.md
.planning/phases/15-source-retries/15-01-SUMMARY.md
</read_first>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/PROJECT.md
@docs/SOURCE-VETTING.md
@.planning/phases/16-conditional-packs/16-RESEARCH.md
@.planning/phases/16-conditional-packs/16-PATTERNS.md
@.planning/phases/15-source-retries/15-VERIFICATION.md
@.planning/phases/15-source-retries/15-01-SUMMARY.md
</context>

<plan_notes>
Discuss skipped (no 16-CONTEXT.md). Locked constraints come from ROADMAP Phase 16 + live REQUIREMENTS PACK-20-01..03 + 16-RESEARCH user_constraints / expected DEFERRED_ALL. No D-XX IDs.

TRACER_MODE: Task 1 is the production slice (Phase 15 handoff NO-GO → published PACK-20-01 deferral on SOURCE-VETTING). Tasks 2–3 expand AAF PACK-20-02/03 and planning-surface annotations.

This plan is the else-branch of PACK-20-01..03. Phase 15 verification already closed VET-20 with 2 NO-GO + 1 document-only. Do not re-fetch PDFs. Do not treat a later 200 HTML landing page as clearance. If execute-day the handoff table has been edited to GO, halt — this plan does not own pack construction.

Do not tick live PACK-20 boxes. That is phase.complete after verify. Do not uncheck VET-20-01..03 (already Complete).

Stay on main. Explicit pathspecs. python, not py. GSD per-task commits allowed with --no-verify and explicit paths. OneDrive: timeout may still mean the commit landed — run git log --oneline -1 before retry.

Executor MUST keep a ## Deviations section in 16-01-SUMMARY.md. Empty/None is valid.

Link Policy: scheme strings belong only in 15-RESEARCH.md (and already-committed 10-RESEARCH.md). Published docs/SOURCE-VETTING.md stays locator-free.

16-PATTERNS.md already captures the deferral recording pattern. Do not create or edit 16-PATTERNS.md or 16-RESEARCH.md in this plan.
</plan_notes>

<claim_verification>
Live commands run this planner session (2026-08-20), cwd repo root. Do not invent replacements.

| claim | command | observed | status |
|---|---|---|---|
| Branch is main | `git branch --show-current` | `main` | VERIFIED |
| No 16-CONTEXT.md | `ls .planning/phases/16-conditional-packs/` | 16-RESEARCH.md, 16-PATTERNS.md, master_flow_state.json (plus this PLAN after write) | VERIFIED |
| PACK-20-01..03 unchecked | grep `**PACK-20-0` in `.planning/REQUIREMENTS.md` | three `- [ ]` requirement lines; traceability Pending | VERIFIED |
| VET-20-01..03 already Complete | grep `**VET-20-0` in `.planning/REQUIREMENTS.md` | three `- [x]` lines with 2026-08-20 parentheticals | VERIFIED |
| Link Policy currently holds | `grep -c http docs/SOURCE-VETTING.md` | `0` | VERIFIED |
| Phase 16 handoff heading exists | `grep -n "Phase 16 handoff" docs/SOURCE-VETTING.md` | line 192 `### Phase 16 handoff (v1.19.1)` | VERIFIED |
| Handoff is 2 NO-GO + document-only | python split after that heading | `NO-GO count 2`; `document-only True`; GO cells = 0 | VERIFIED |
| FUT-04 Not-cleared DEFERRED 2026-08-20 | grep FUT-04 in SOURCE-VETTING v1.19.1 section | DEFERRED; 403; no in-source; Phase 16 must not build Army CBA | VERIFIED |
| AAF still NOT yet vetted | grep Product Support Manager Guidebook | Excluded-pending + Not-cleared bullet 2026-08-20 | VERIFIED |
| No Army CBA / AAF / ROSAP pack dirs | `ls packs/` grep army/cba/aaf plus glob | no matches | VERIFIED |
| sources/ has no Army CBA / AAF / ROSAP trees | `ls sources/` grep | no matches | VERIFIED |
| packs/ working tree clean | `git diff --name-only -- packs/` | empty (0 lines) | VERIFIED |
| No catalog army/aaf slug | grep army-cba / aaf- in catalog paths | no hits | VERIFIED |
| DEFERRED / NOT yet vetted present | `grep -c "DEFERRED\\|NOT yet vetted" docs/SOURCE-VETTING.md` | `9` | VERIFIED |
| estimate-calibration | `gsd_run query estimate-calibration` | factor 1, sample_count 0, confidence low | VERIFIED |
</claim_verification>

<tasks>

<task type="tracer" reversibility="reversible">
  <name>End-to-end PACK-20-01 deferral — handoff NO-GO to published register</name>
  <files>docs/SOURCE-VETTING.md</files>
  <read_first>docs/SOURCE-VETTING.md, .planning/phases/16-conditional-packs/16-RESEARCH.md, .planning/phases/15-source-retries/15-VERIFICATION.md</read_first>
  <precondition>docs/SOURCE-VETTING.md contains heading ### Phase 16 handoff (v1.19.1) whose body has exactly two NO-GO rows and one document-only row</precondition>
  <action>
PACK-20-01. One path through the only layer this phase publishes: consume the Phase 15 handoff, write the Army CBA deferral on the published register. Stay on main. Do not create packs/. Do not write scheme characters into docs/SOURCE-VETTING.md (Link Policy; 17 U.S.C. section 105 is allowed). Edit-only (SOURCE-VETTING has an SPDX/copyright HTML comment — never rewrite the file wholesale). Do not re-fetch ASAFM or any other locator.

1. Confirm cwd is repo root and git branch --show-current prints main. Halt if not main.

2. Read the existing `### Phase 16 handoff (v1.19.1)` table. Confirm the FUT-04 Army CBA row still reads NO-GO. If that row reads GO, halt this plan (pack construction is out of files_modified and out of DEFERRED_ALL). Do not invent a grant. Do not flip the row.

3. On the existing FUT-04 / US Army Cost Benefit Analysis Guide bullet under `### Not cleared this session (v1.19.1 retry)`, append in place (do not add a second FUT-04 bullet): Phase 16 PACK-20-01 deferred-with-evidence 2026-08-20; no Army CBA pack built. (Also verified 2026-08-20.) If that PACK-20-01 suffix already exists, update it in place.

4. Immediately after the existing Phase 16 handoff table (before `### Def Stan 00-051`), if a sentence beginning `Phase 16 record (2026-08-20):` is absent, insert exactly one sentence: Phase 16 record (2026-08-20): PACK-20-01 deferred-with-evidence per this table; PACK-20-02/03 recorded in the next task; zero packs built. If the sentence already exists from a re-run, update in place and do not insert a second copy. Do not add a second handoff table. Do not add a new heading.

5. After edits run a whole-file scheme-string count on docs/SOURCE-VETTING.md and expect 0. If non-zero, remove the leaking locator without changing verdicts.

6. Confirm git diff --name-only -- packs/ is empty and no packs/army, packs/cba, or packs/aaf directory appeared.

7. Commit explicit paths only: git add -- docs/SOURCE-VETTING.md then git commit --no-verify -m "docs(16): PACK-20-01 deferred-with-evidence". OneDrive: check git log --oneline -1 before retry. Do not stage packs, catalog, tooling, REQUIREMENTS, STATE, or this PLAN file here.
  </action>
  <verify>
    <automated>python -c "
from pathlib import Path
sv = Path('docs/SOURCE-VETTING.md').read_text(encoding='utf-8')
assert '### Phase 16 handoff (v1.19.1)' in sv
sec16 = sv.split('### Phase 16 handoff (v1.19.1)',1)[1].split('### Def Stan',1)[0]
assert sec16.count('| NO-GO') == 2
assert 'document-only' in sec16
assert 'PACK-20-01' in sv
head = sv.split('### Not cleared this session (v1.19.1 retry)',1)[1].split('### Phase 16 handoff',1)[0]
assert 'PACK-20-01' in head
assert 'deferred-with-evidence' in head
assert '2026-08-20' in head
assert 'Army' in head and 'Cost Benefit' in head
assert sv.count('### Phase 16 handoff (v1.19.1)') == 1
assert 'http' not in sv.lower()
print('PACK20_01_TRACER_OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - FUT-04 Not-cleared bullet contains PACK-20-01 and deferred-with-evidence and 2026-08-20
    - Phase 16 handoff table still has exactly two NO-GO rows and one document-only row
    - Exactly one `### Phase 16 handoff (v1.19.1)` heading
    - scheme-string count on docs/SOURCE-VETTING.md equals 0
    - git diff --name-only -- packs/ is empty
  </acceptance_criteria>
  <done>PACK-20-01 holds via the else-branch: Army CBA is on the record as deferred-with-evidence; no pack built; handoff not flipped to GO.</done>
</task>

<task type="auto" reversibility="reversible">
  <name>Record PACK-20-02 and PACK-20-03 AAF deferrals on the same register</name>
  <files>docs/SOURCE-VETTING.md</files>
  <read_first>docs/SOURCE-VETTING.md</read_first>
  <action>
PACK-20-02 and PACK-20-03. Expand the proven Task 1 slice onto the AAF Not-cleared bullet. Still no packs/. Still no scheme characters in docs/SOURCE-VETTING.md. Still no second handoff table. Update in place if the named suffixes already exist.

A. Confirm the AAF Product Support + Software pathway handoff row still reads NO-GO. If it reads GO, halt. Do not invent a guidebook grant. Do not re-fetch WarU or successor hosts.

B. On the existing AAF Product Support Manager Guidebook + Software pathway bullet under `### Not cleared this session (v1.19.1 retry)`, append in place (do not add a second AAF bullet): Phase 16 PACK-20-02 (IO-05 Software pathway) deferred-with-evidence 2026-08-20; Phase 16 PACK-20-03 (IO-06 Product Support) deferred-with-evidence 2026-08-20; no AAF pack built. (Also verified 2026-08-20.)

C. Update the Task 1 `Phase 16 record (2026-08-20):` sentence so it now states PACK-20-01..03 all deferred-with-evidence per this table; zero packs built. Do not insert a second record sentence.

D. Do not add an AAF Excluded-table row. Do not change the ROSAP document-only row into a pack action. Do not create a PACK-20-04. Leave shipped faa-std-025 unchanged.

E. Scheme-string count on docs/SOURCE-VETTING.md must equal 0. Confirm git diff --name-only -- packs/ is empty.

F. Commit explicit paths: git add -- docs/SOURCE-VETTING.md then git commit --no-verify -m "docs(16): PACK-20-02 and PACK-20-03 deferred-with-evidence".
  </action>
  <verify>
    <automated>python -c "
from pathlib import Path
sv = Path('docs/SOURCE-VETTING.md').read_text(encoding='utf-8')
head = sv.split('### Not cleared this session (v1.19.1 retry)',1)[1].split('### Phase 16 handoff',1)[0]
assert 'PACK-20-02' in head
assert 'PACK-20-03' in head
assert 'IO-05' in head
assert 'IO-06' in head
assert head.lower().count('deferred-with-evidence') >= 2
sec16 = sv.split('### Phase 16 handoff (v1.19.1)',1)[1].split('### Def Stan',1)[0]
assert sec16.count('| NO-GO') == 2
assert 'document-only' in sec16
assert 'PACK-20-01..03' in sec16 or ('PACK-20-01' in sec16 and 'PACK-20-03' in sec16)
assert sv.count('### Phase 16 handoff (v1.19.1)') == 1
aaf_excl = [ln for ln in sv.splitlines() if 'Product Support Manager Guidebook' in ln and ln.startswith('|')]
assert len(aaf_excl) == 1
assert 'http' not in sv.lower()
print('PACK20_02_03_OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - AAF Not-cleared bullet contains PACK-20-02, PACK-20-03, IO-05, IO-06, deferred-with-evidence, and 2026-08-20
    - Exactly one AAF Excluded-table row remains
    - Phase 16 handoff still 2 NO-GO + document-only; heading count is 1
    - Phase 16 record sentence names PACK-20-01..03 deferred-with-evidence
    - scheme-string count on docs/SOURCE-VETTING.md equals 0
    - git diff --name-only -- packs/ is empty
  </acceptance_criteria>
  <done>PACK-20-02 and PACK-20-03 hold via the else-branch: IO-05 and IO-06 stay deferred on the register; no AAF pack built.</done>
</task>

<task type="auto" reversibility="reversible">
  <name>Annotate PACK-20 parentheticals and STATE deviations without ticking boxes</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/STATE.md, docs/SOURCE-VETTING.md</read_first>
  <action>
Planning-surface close-out after the register wave. Do not edit docs/SOURCE-VETTING.md in this task except if a scheme-string leak appeared (then strip locators only). Do not tick any PACK-20 checkbox. Do not uncheck VET-20-01..03. Do not touch STATE.md YAML frontmatter (current_phase, progress, completed_plans). Execute workflow owns those. Leave the REQUIREMENTS traceability table PACK-20 rows as Pending.

REQUIREMENTS.md — append one italic parenthetical on the existing PACK-20-01, PACK-20-02, and PACK-20-03 lines (do not rewrite the requirement text; do not stack a second copy on re-run):

- PACK-20-01: deferred 2026-08-20 — Phase 15 handoff NO-GO; VET-20-01 not cleared; FUT-04 remains DEFERRED; no invented pack
- PACK-20-02: deferred 2026-08-20 — Phase 15 handoff NO-GO; VET-20-02 Software pathway not cleared; IO-05 stays deferred
- PACK-20-03: deferred 2026-08-20 — Phase 15 handoff NO-GO; VET-20-02 Product Support not cleared; IO-06 stays deferred

Live boxes stay `- [ ]`. Leave HYG-20 and TOOL-20 and MAP-20 / REL-20 boxes untouched.

STATE.md — under ## Deviations / Notes append a single bullet dated Phase 16 (2026-08-20): PACK-20-01..03 all deferred-with-evidence per Phase 15 handoff; zero packs built. Do not duplicate the bullet on re-run. Optionally under ## Decisions append `- [Phase 16]: PACK-20-01..03 deferred-with-evidence; no Army CBA or AAF pack` without duplicating.

Fence: git diff --name-only for this task must be a subset of .planning/REQUIREMENTS.md and .planning/STATE.md (plus SOURCE-VETTING.md only if a leak strip was required). No packs/, no catalog, no tooling.

Commit explicit paths: git add -- .planning/REQUIREMENTS.md .planning/STATE.md then git commit --no-verify -m "docs(16): annotate PACK-20 deferrals without ticking boxes".
  </action>
  <verify>
    <automated>python -c "
from pathlib import Path
live = Path('.planning/REQUIREMENTS.md').read_text(encoding='utf-8')
st = Path('.planning/STATE.md').read_text(encoding='utf-8')
sv = Path('docs/SOURCE-VETTING.md').read_text(encoding='utf-8')
for i in range(1,4):
    lines = [ln for ln in live.splitlines() if f'**PACK-20-0{i}**' in ln]
    assert len(lines)==1, i
    assert lines[0].startswith('- [ ] '), lines[0]
    assert '2026-08-20' in lines[0], lines[0]
    assert 'deferred' in lines[0].lower(), lines[0]
vet = [ln for ln in live.splitlines() if '**VET-20-0' in ln and ln.startswith('- [')]
assert vet and all(ln.startswith('- [x] ') for ln in vet), vet
assert 'Phase 16 (2026-08-20)' in st
assert 'PACK-20-01..03' in st
assert 'http' not in sv.lower()
print('PACK20_ANNOTATIONS_OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - Three PACK-20-01..03 list items remain unchecked and each contain 2026-08-20 and deferred
    - Three VET-20-01..03 list items remain checked
    - STATE.md contains Phase 16 (2026-08-20) and does not claim Army CBA or AAF as pack-built
    - docs/SOURCE-VETTING.md scheme-string count remains 0
    - Diff fence holds (planning docs only)
  </acceptance_criteria>
  <done>Planning surfaces match the register: PACK-20-01..03 annotated with dated deferrals, boxes open for verify; STATE records the Phase 16 deferrals; no packs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 15 handoff table → Phase 16 pack decision | Only a GO cell plus a quoted in-source grant may authorise a pack; this plan must not mint a pack from NO-GO |
| 15-RESEARCH.md (private URL store) → docs/SOURCE-VETTING.md (published) | Verdicts and dates may cross; locators and raw fetch URLs must not |
| Executor → pack tree | Phase 16 must not create library objects when handoff is NO-GO |
| live REQUIREMENTS.md → phase.complete | PACK-20 boxes stay open until verify |
| HTML landing / 403 page → licence judgment | A host 200 or copyright footer is not an in-source redistribution grant |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Information Disclosure | docs/SOURCE-VETTING.md | high | mitigate | Tasks 1–2 whole-file scheme-string count must equal 0; no locators copied from 15-RESEARCH.md |
| T-16-02 | Spoofing | Uncleared sources labelled as built packs | high | mitigate | Halt if handoff row reads GO; files_modified is docs + planning only; no extract/build/catalog; python asserts no second AAF Excluded row and handoff stays 2 NO-GO |
| T-16-03 | Elevation of privilege | packs/ | high | mitigate | git diff --name-only -- packs/ empty after each task; no army/cba/aaf directories; no sources/ trees |
| T-16-04 | Spoofing | AAF copyright footer treated as grant | high | mitigate | Do not re-fetch; keep NOT yet vetted — do not use; PACK-20-02/03 stay deferred-with-evidence |
| T-16-05 | Tampering | live REQUIREMENTS.md PACK-20 boxes | medium | mitigate | Task 3 forbidden from ticking boxes; python assert three PACK-20 lines stay unchecked and VET-20 stay checked |
| T-16-06 | Tampering | git staging | high | mitigate | Explicit pathspecs only; never git add -A |
| T-16-07 | Repudiation | Undated PACK-20 deferrals | medium | mitigate | Every new note carries 2026-08-20 / Also verified 2026-08-20 |
| T-16-08 | Information disclosure | planning docs | low | accept | Docs-only phase; locators stay in 15-RESEARCH.md; no secrets; no new fetches |
</threat_model>

## Artifacts

| Artifact | Produced by | Consumed by |
|----------|-------------|-------------|
| SOURCE-VETTING FUT-04 PACK-20-01 suffix | Task 1 | Tasks 2–3, Phase 17/18, verify |
| SOURCE-VETTING Phase 16 record sentence | Task 1 (seed) / Task 2 (complete) | verify / CHANGELOG honesty |
| SOURCE-VETTING AAF PACK-20-02/03 suffix | Task 2 | REQUIREMENTS parentheticals |
| REQUIREMENTS PACK-20 parentheticals | Task 3 | phase verify (boxes still open) |
| STATE Phase 16 deviations bullet | Task 3 | session continuity |
| 16-01-SUMMARY.md claim transcript | Tasks 1–3 | phase verify |

<verification>
1. SOURCE-VETTING still has exactly one Phase 16 handoff heading with 2 NO-GO + 1 document-only, plus PACK-20-01..03 deferred-with-evidence on the v1.19.1 Not-cleared bullets and one Phase 16 record sentence.
2. No Army CBA / AAF pack directory; git diff --name-only -- packs/ empty; catalog untouched.
3. Link Policy: scheme-string count on docs/SOURCE-VETTING.md equals 0.
4. Live PACK-20-01..03 boxes remain unchecked and contain 2026-08-20 plus deferred; VET-20-01..03 remain checked.
5. STATE.md contains Phase 16 (2026-08-20) PACK-20-01..03 deferred-with-evidence; YAML progress fields not task-edited.
</verification>

<success_criteria>
- PACK-20-01: else-branch delivered — FUT-04 stays deferred with evidence; no invented Army CBA pack.
- PACK-20-02: else-branch delivered — IO-05 stays deferred; no AAF Software / Integration pack.
- PACK-20-03: else-branch delivered — IO-06 stays deferred; no AAF Product Support / Logistics pack.
- Published register stays locator-free.
- Deferred-with-evidence is visible on SOURCE-VETTING, REQUIREMENTS, and STATE.
</success_criteria>

<output>
Create `.planning/phases/16-conditional-packs/16-01-SUMMARY.md` when done. Include a command transcript under ## Claim verification and a ## Deviations section.
</output>
