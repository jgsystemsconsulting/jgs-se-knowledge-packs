---
phase: 17-tooling-in-02-fut-05
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tooling/check_overlap.py
  - tooling/check_release.py
  - docs/capability-map-CONTRACT.md
  - .planning/phases/17-tooling-in-02-fut-05/17-01-SUMMARY.md
autonomous: true
requirements: [TOOL-20-01, TOOL-20-02, TOOL-20-03]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "python tooling/check_overlap.py exits 0 on current repo (OVERLAP PASS); the only live chapter-basename collision is ch01-introduction.md across dau-se-guidebook, nasa-npr-7123, nasa-system-safety and that name is in WHITELIST"
    - "check_release.py imports check_overlap and calls check_overlap.main() in-process (no subprocess) and fail()s on non-zero; python tooling/check_release.py still prints RELEASE CHECK: PASS"
    - "Overlap thresholds and the intentional-shared-name whitelist are documented (module docstring plus CONTRACT); support files under pack root are excluded by scanning only packs/*/chapters/*.md"
    - "TOOL-20-03 residual is honest in CONTRACT: mechanical slice is check_capability_map uniqueness/staleness/existence/thresholds; cluster assignment and note fields remain agent procedure; no byte-stable full-map generator is claimed"
    - "python tooling/check_capability_map.py still PASS; schema_version 2; map_version stays 1.19.0; catalog 63 / packs dirs 65"
    - "Version trio stays 1.19.0; no new version heading; no new git tag; .github/workflows/validate.yml is not given a repo-Python step"
    - "Live REQUIREMENTS.md TOOL-20-01..03 boxes remain unchecked"
    - "No new Python dependencies; stdlib only; no packs/ invented or rebuilt"
    - "Idempotency: re-run edits the same import block, WHITELIST, and CONTRACT paragraphs in place; does not add a second overlap import or a second FUT-05 section"
    - "Concurrency: single writer on check_release.py then CONTRACT.md; exclusive ownership of check_overlap.py"
  artifacts:
    - path: tooling/check_overlap.py
      provides: "stdlib multi-pack chapter-basename collision gate with WHITELIST"
      contains: "WHITELIST"
    - path: tooling/check_release.py
      provides: "in-process overlap gate on the release path"
      contains: "check_overlap"
    - path: docs/capability-map-CONTRACT.md
      provides: "overlap thresholds/whitelist plus FUT-05 residual agent procedure"
      contains: "check_overlap"
  key_links:
    - from: tooling/check_release.py
      to: tooling/check_overlap.py
      via: "import check_overlap; rc = check_overlap.main()"
      pattern: "check_overlap.main"
    - from: docs/capability-map-CONTRACT.md
      to: tooling/check_overlap.py
      via: "documented whitelist and scan scope"
      pattern: "ch01-introduction.md"
    - from: docs/capability-map-CONTRACT.md
      to: tooling/check_capability_map.py
      via: "FUT-05 mechanical slice named; residual classification not claimed as a generator"
      pattern: "check_capability_map"

# Prohibitions (must-NOT). No check_kind / check_target fields.
# - must-NOT tick TOOL-20-01..03 boxes (verify / phase.complete does that)
# - must-NOT bump map_version, plugin.json, CHANGELOG top, or RELEASE-INFO (Phase 18 owns the v1.19.1 surface)
# - must-NOT add a CI step that executes repo Python; must-NOT edit .github/workflows/validate.yml
# - must-NOT add pip/npm/cargo dependencies or a pytest suite
# - must-NOT write a full map generator or claim byte-stable regen from committed inputs alone
# - must-NOT invent packs or edit packs/ content
# - must-NOT subprocess the overlap or map gates
# - must-NOT git add -A / git add . ; explicit pathspecs only; stay on main
---

<objective>
Ship IN-02 (multi-pack chapter basename overlap checker on the release path) and an honest FUT-05 residual: the mechanical map slice already in check_capability_map.py plus CONTRACT language for the agent classification that cannot be regenerated without judgment.

Purpose: release tooling catches un-whitelisted chapter-name collisions; FUT-05 is not papered over as a full generator. Phase 18 still owns map_version and the public v1.19.1 surface.

Output: tooling/check_overlap.py, in-process wire in check_release.py, CONTRACT overlap + residual sections. Both local gates PASS. TOOL-20 boxes stay open.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/17-tooling-in-02-fut-05/17-RESEARCH.md
.planning/phases/17-tooling-in-02-fut-05/17-PATTERNS.md
tooling/check_release.py
tooling/check_capability_map.py
docs/capability-map-CONTRACT.md
</read_first>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-tooling-in-02-fut-05/17-RESEARCH.md
@.planning/phases/17-tooling-in-02-fut-05/17-PATTERNS.md
@tooling/check_release.py
@tooling/check_capability_map.py
@docs/capability-map-CONTRACT.md
</context>

<plan_notes>
Discuss skipped (no 17-CONTEXT.md). No D-XX locked decisions. Constraints come from ROADMAP Phase 17 success criteria, REQUIREMENTS TOOL-20-01..03, 17-RESEARCH.md, and 17-PATTERNS.md.

TRACER_MODE: Task 1 is the production slice (new checker + in-process release wire, both commands green). Tasks 2–3 expand documentation and the FUT-05 residual around that already-wired gate.

REVERSIBILITY: delete check_overlap.py and un-import restores the Phase 12 dual-gate; CONTRACT wording is local.

Stay on main. No branches / worktrees. Use `python`, not `py`. GSD per-task commits allowed with --no-verify and explicit paths. OneDrive: timeout may still mean the commit landed — run git log --oneline -1 before retry.

Executor MUST keep a `## Deviations` section in 17-01-SUMMARY.md. If none: write `None.`

Do not tick live TOOL-20 boxes. That is phase.complete after verify.

Link Policy: do not put scheme strings in docs/capability-map-CONTRACT.md.

17-PATTERNS.md and 17-RESEARCH.md are inputs. Do not edit them in this plan.
</plan_notes>

<claim_verification>
Live commands run this planner session (2026-08-20), cwd repo root. Do not invent replacements.

| claim | command | observed | status |
|---|---|---|---|
| Branch is main | `git branch --show-current` | `main` | VERIFIED |
| No 17-CONTEXT.md | `ls .planning/phases/17-tooling-in-02-fut-05/` | 17-RESEARCH.md, 17-PATTERNS.md, master_flow_state.json (plus this PLAN after write) | VERIFIED |
| check_overlap.py absent | `ls tooling/check_overlap.py` | no such file | VERIFIED |
| Map gate PASS, TOTAL 644, map_version 1.19.0 | `python tooling/check_capability_map.py` | `PASS: capability map OK`; TOTAL 644; envelope schema 2 / map_version 1.19.0 | VERIFIED |
| Release gate PASS and already imports map | `python tooling/check_release.py` | `RELEASE CHECK: PASS`; `import check_capability_map` at check_release.py:217 | VERIFIED |
| sys.path.insert already present | `tooling/check_release.py:120` | `sys.path.insert(0, str(ROOT / "tooling"))` before pack validate | VERIFIED |
| Live chapter-basename collisions | python packs glob of `*/chapters/*.md` | exactly 1 duplicate basename: `ch01-introduction.md` in `dau-se-guidebook`, `nasa-npr-7123`, `nasa-system-safety`; 536 chapter files / 534 unique basenames | VERIFIED |
| Support files sit at pack root not under chapters/ | count glossary/patterns/cheatsheet | 63 each; overlap scan of chapters/ therefore excludes them by design | VERIFIED |
| Version trio 1.19.0 | plugin.json / CHANGELOG top / RELEASE-INFO | all `1.19.0` | VERIFIED |
| TOOL-20-01..03 unchecked | grep TOOL-20 in `.planning/REQUIREMENTS.md` | three `- [ ]` requirement lines; traceability Pending | VERIFIED |
| CI never execs repo Python | `.github/workflows/validate.yml` header | `never executes checked-out repository code`; `check_overlap` absent; `check_capability_map` absent | VERIFIED |
| Tags | `git tag -l 'v1.19*'` | `v1.19.0` only | VERIFIED |
| Catalog / dirs | catalog.json packs length; packs/ dirs | 63 catalog / 65 dirs | VERIFIED |
| Link policy on SOURCE-VETTING | `grep -c http docs/SOURCE-VETTING.md` | `0` | VERIFIED |
| CONTRACT has no FUT-05 residual yet | `docs/capability-map-CONTRACT.md` | 113 lines; no FUT-05 heading; refresh path already names check_release in-process map call | VERIFIED |
| estimate-calibration | `gsd_run query estimate-calibration` | factor 1, sample_count 0, confidence low | VERIFIED |
</claim_verification>

<tasks>

<task type="tracer" reversibility="reversible">
  <name>End-to-end overlap check — one path through checker and release gate</name>
  <files>tooling/check_overlap.py, tooling/check_release.py</files>
  <read_first>tooling/check_release.py, tooling/check_capability_map.py, .planning/phases/17-tooling-in-02-fut-05/17-PATTERNS.md</read_first>
  <precondition>python tooling/check_release.py currently exits 0 (RELEASE CHECK: PASS) and tooling/check_overlap.py does not exist</precondition>
  <action>
TOOL-20-01 and TOOL-20-02 implementation. One path: new stdlib checker, then the same in-process import shape already used for check_capability_map. Stay on main. Use python. Do not edit packs/. Do not bump versions. Do not touch validate.yml.

Create tooling/check_overlap.py matching sibling checkers (shebang python3, JGSC copyright line, SPDX MIT, from __future__ import annotations, pathlib Path, sys). ROOT is parent of tooling/. Export WHITELIST as a set containing exactly ch01-introduction.md (the only live multi-pack chapter basename; three packs, intentional canonical topic). Scan only (ROOT / "packs").glob("*/chapters/*.md") so pack-root support files (glossary.md, patterns.md, cheatsheet.md, SKILL.md, PACK.yaml) are excluded by design. Group by Path.name to pack slug (p.parent.parent.name). A collision is a basename with two or more packs. Un-whitelisted collisions are failures; whitelisted collisions are not. Collect errors with a fail(errs, msg) helper like check_capability_map. Print OVERLAP: PASS and return 0 when the un-whitelisted set is empty; otherwise print OVERLAP: FAIL (N issue(s)), one line per colliding basename with sorted pack slugs, return 1. Module docstring must state: stdlib only; invoked by check_release.py (local/trusted; CI does not exec repo Python); scan scope; threshold (any un-whitelisted chapter basename collision fails); WHITELIST contents and why. if __name__ == "__main__": sys.exit(main()). No PyYAML, no pytest, no network.

In tooling/check_release.py, reuse the existing sys.path.insert for tooling/ (do not add a second path insert). Immediately before the current MAP-19-04 check_capability_map.main() block, insert an in-process overlap call in the same try/except/fail shape: import check_overlap, rc = check_overlap.main(), fail errs with prefix [overlap] on non-zero, and catch Exception into fail errs. Comment the new block as overlap (TOOL-20) so it is distinct from the map block; leave the map import in place after it (renumber the map comment from 5d to 5e if you touch that comment). Update the check_release module docstring numbered list to mention the overlap gate. Do not claim CI runs it. Do not add check_overlap.py to REQUIRED_FILES or to the authored-header list (check_capability_map is also absent from both). Do not subprocess.

Run python tooling/check_overlap.py (must exit 0) then python tooling/check_release.py (must still end RELEASE CHECK: PASS and must print the overlap PASS line before or with the map cluster block). Confirm version trio still 1.19.0.

Commit only those two files (fix(tooling): add check_overlap and wire into check_release). Explicit pathspecs. git show --name-only must have zero catalog.json, README.md, plugin.json, RELEASE-INFO.txt, validate.yml, packs/.
  </action>
  <verify>
    <automated>python tooling/check_overlap.py && python tooling/check_release.py && python -c "from pathlib import Path; t=Path('tooling/check_release.py').read_text(encoding='utf-8'); assert 'import check_overlap' in t; assert 'check_overlap.main()' in t; assert 'import check_capability_map' in t; d=Path('tooling/check_overlap.py').read_text(encoding='utf-8'); assert 'Copyright (c) 2026 JG Systems Consulting Ltd.' in d; assert 'SPDX-License-Identifier: MIT' in d; assert 'ch01-introduction.md' in d; print('OVERLAP_WIRE_OK')"</automated>
  </verify>
  <done>check_overlap.py exists, stdlib, WHITELIST covers the live intro collision, exits 0; check_release imports and calls it in-process; both commands PASS; versions untouched. Committed.</done>
</task>

<task type="auto" reversibility="reversible">
  <name>Document overlap thresholds and prove the whitelist with a runnable assert</name>
  <files>tooling/check_overlap.py, docs/capability-map-CONTRACT.md</files>
  <read_first>tooling/check_overlap.py, docs/capability-map-CONTRACT.md</read_first>
  <action>
TOOL-20-02 documentation plus the tiny assert demo. Edit-only on CONTRACT (HTML copyright comment at top — never rewrite the file wholesale). Do not bump the example map_version in the JSON envelope. Do not add scheme strings.

If Task 1 docstring already names scan scope, threshold (un-whitelisted collision fails), and WHITELIST, leave it; only fill a gap if a reader could not learn those three facts from the module docstring.

Append a short new CONTRACT section after the live snapshot (current end of file) titled so it is greppable as overlap: state that python tooling/check_overlap.py is on the local release path via check_overlap.main() in check_release.py; scan is packs/*/chapters/*.md basenames; threshold is zero un-whitelisted collisions; support files at pack root are out of scope because they are not under chapters/; WHITELIST currently contains ch01-introduction.md because three distinct source packs legitimately share that canonical topic (dau-se-guidebook, nasa-npr-7123, nasa-system-safety). Adding a new shared basename requires an explicit WHITELIST edit, not a silent pass.

Do not create tests/ or a pytest file. Instead keep (or add) WHITELIST as an importable module constant so this runnable check works from repo root:

python -c "import sys; from pathlib import Path; sys.path.insert(0, str(Path('tooling').resolve())); import check_overlap; assert 'ch01-introduction.md' in check_overlap.WHITELIST; chaps={}; 
from pathlib import Path as P; 
root=P('.').resolve();
[chaps.setdefault(p.name, []).append(p.parent.parent.name) for p in (root/'packs').glob('*/chapters/*.md')];
dups={k:v for k,v in chaps.items() if len(v)>1};
assert set(dups)=={'ch01-introduction.md'}, dups;
assert not {k:v for k,v in dups.items() if k not in check_overlap.WHITELIST};
print('OVERLAP_ASSERT_OK')"

Record that command and its OVERLAP_ASSERT_OK line in 17-01-SUMMARY.md when you write it (Task 3). Run it now; it must print OVERLAP_ASSERT_OK.

Commit the CONTRACT (+ check_overlap.py only if the docstring actually changed): docs(tooling): overlap gate thresholds and whitelist.
  </action>
  <verify>
    <automated>python tooling/check_overlap.py && python -c "import sys; from pathlib import Path; sys.path.insert(0, str(Path('tooling').resolve())); import check_overlap; assert 'ch01-introduction.md' in check_overlap.WHITELIST; chaps={}; root=Path('.').resolve();
[chaps.setdefault(p.name, []).append(p.parent.parent.name) for p in (root/'packs').glob('*/chapters/*.md')]; dups={k:v for k,v in chaps.items() if len(v)>1}; assert set(dups)=={'ch01-introduction.md'}, dups; assert not {k:v for k,v in dups.items() if k not in check_overlap.WHITELIST}; print('OVERLAP_ASSERT_OK')" && python -c "from pathlib import Path; c=Path('docs/capability-map-CONTRACT.md').read_text(encoding='utf-8'); assert 'check_overlap' in c; assert 'ch01-introduction.md' in c; assert 'WHITELIST' in c or 'whitelist' in c.lower(); print('CONTRACT_OVERLAP_OK')"</automated>
  </verify>
  <done>CONTRACT documents scan scope, zero un-whitelisted-collision threshold, and the intro whitelist; runnable assert prints OVERLAP_ASSERT_OK. Committed if files changed.</done>
</task>

<task type="auto" reversibility="reversible">
  <name>Record FUT-05 residual in CONTRACT and close with both gates green</name>
  <files>docs/capability-map-CONTRACT.md, .planning/phases/17-tooling-in-02-fut-05/17-01-SUMMARY.md</files>
  <read_first>docs/capability-map-CONTRACT.md, tooling/check_capability_map.py</read_first>
  <action>
TOOL-20-03. Honest residual, not a generator. Edit-only on CONTRACT. Do not add tooling/generate_capability_map.py or any exporter. Do not rewrite cluster membership. Do not bump map_version, generated_on, plugin.json, CHANGELOG, or RELEASE-INFO.

In docs/capability-map-CONTRACT.md, add a residual section (after the overlap section from Task 2, still greppable as FUT-05) that states all of: (1) the largest deterministic slice already ships in tooling/check_capability_map.py — v2 envelope, bidirectional pack/chapter staleness, file existence, (pack, chapter) uniqueness, name-keyed thresholds; (2) cluster assignment and chapters[].note require agent judgment per the rules of construction in docs/capability-pack-map.md and therefore cannot be regenerated from committed inputs alone; (3) this milestone does not claim a byte-stable full-map generator; refresh path in section 4 remains the agent classification pass plus the map checker; (4) Phase 18 owns any later map_version bump on the public release surface.

Do not weaken section 4. Do not say FUT-05 is fully closed. Do not add a CI repo-Python step.

Then: re-run python tooling/check_overlap.py, python tooling/check_capability_map.py, and python tooling/check_release.py — all must PASS. Confirm map_version in docs/capability-pack-map.json is still 1.19.0; version trio 1.19.0; git tag -l with pattern v1.19.1 empty; REQUIREMENTS TOOL-20 boxes still unchecked; validate.yml still does not name check_overlap; grep -c http on docs/SOURCE-VETTING.md is still 0.

Write 17-01-SUMMARY.md per the summary template: overlap file path and WHITELIST, insertion point in check_release.py, both-gate stdout (overlap PASS + map TOTAL 644 + RELEASE CHECK: PASS), the OVERLAP_ASSERT_OK command, CONTRACT section names for overlap and FUT-05 residual (quote the residual sentence that refuses a full generator), Phase 18 leftovers not stolen, `## Deviations` ledger.

Commit CONTRACT + SUMMARY (docs(planning): 17-01 SUMMARY + FUT-05 residual). Explicit paths only.
  </action>
  <verify>
    <automated>python tooling/check_overlap.py && python tooling/check_capability_map.py && python tooling/check_release.py && python -c "import json,re; from pathlib import Path; p=json.loads(Path('.claude-plugin/plugin.json').read_text())['version']; cl=Path('CHANGELOG.md').read_text(encoding='utf-8'); m=re.search(r'^##\s*\[(\d+\.\d+\.\d+)\]', cl, re.M); ri=Path('RELEASE-INFO.txt').read_text(encoding='utf-8'); mm=re.search(r'Version:\s*([0-9]+\.[0-9]+\.[0-9]+)', ri); assert {p,m.group(1),mm.group(1)}=={'1.19.0'}; d=json.loads(Path('docs/capability-pack-map.json').read_text(encoding='utf-8')); assert d['map_version']=='1.19.0' and d['schema_version']==2; c=Path('docs/capability-map-CONTRACT.md').read_text(encoding='utf-8'); assert 'FUT-05' in c; assert 'check_capability_map' in c; assert 'agent' in c.lower(); s=Path('.planning/phases/17-tooling-in-02-fut-05/17-01-SUMMARY.md').read_text(encoding='utf-8'); assert 'Deviations' in s; assert 'OVERLAP_ASSERT_OK' in s or 'check_overlap' in s; print('FUT05_RESIDUAL_OK')" && python -c "from pathlib import Path; t=Path('.github/workflows/validate.yml').read_text(encoding='utf-8'); assert 'never executes checked-out repository code' in t; print('CI_FENCE_OK')" && python -c "from pathlib import Path; r=Path('.planning/REQUIREMENTS.md').read_text(encoding='utf-8'); assert '- [ ] **TOOL-20-01' in r; assert '- [ ] **TOOL-20-02' in r; assert '- [ ] **TOOL-20-03' in r; print('BOXES_OPEN')" && grep -c http docs/SOURCE-VETTING.md | grep -qx 0</automated>
  </verify>
  <done>CONTRACT names FUT-05 residual without claiming a full generator; both gates PASS; map_version and version trio stay 1.19.0; TOOL-20 boxes open; SUMMARY + Deviations written. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| check_release.py → check_overlap.main() | in-process import of repo Python; local/trusted only — CI must not gain this path |
| overlap scan → packs/*/chapters | filesystem read of committed chapter basenames; no untrusted network input |
| CONTRACT.md → consumers (se-agents) | published procedure; a false "full generator" claim would mis-set downstream regen |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering | check_overlap WHITELIST | medium | mitigate | Explicit module-level WHITELIST; CONTRACT lists the name; un-whitelisted collisions still fail the gate |
| T-17-02 | Elevation of Privilege | .github/workflows/validate.yml | medium | mitigate | Do not add a repo-Python step; verify check_overlap absent from validate.yml |
| T-17-03 | Tampering | check_release overlap wire | medium | mitigate | In-process import + fail on non-zero, same shape as check_capability_map; no subprocess |
| T-17-04 | Information Disclosure | overlap FAIL lines | low | accept | Pack slugs printed on collision are already public tree names |
| T-17-05 | Spoofing | FUT-05 CONTRACT claim | medium | mitigate | Residual section must name agent classification and refuse a byte-stable full generator; no new generator file |
| T-17-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager installs in this phase; stdlib only |
</threat_model>

<verification>
- `python tooling/check_overlap.py` exits 0.
- Runnable WHITELIST assert prints OVERLAP_ASSERT_OK.
- `python tooling/check_capability_map.py` PASS; map_version 1.19.0; TOTAL 644.
- `python tooling/check_release.py` PASS and contains import check_overlap / check_overlap.main().
- CONTRACT documents overlap whitelist/threshold and FUT-05 residual.
- Version trio 1.19.0; no v1.19.1 tag; TOOL-20 boxes unchecked; validate.yml unnamed for check_overlap.
</verification>

<success_criteria>
- TOOL-20-01: tooling/check_overlap.py exists, stdlib, detects un-whitelisted multi-pack chapter basename collisions.
- TOOL-20-02: checker is called from check_release.py; thresholds and whitelist documented; support files do not false-fail.
- TOOL-20-03: no full generator claimed; CONTRACT residual names the mechanical slice and the agent procedure.
</success_criteria>

<output>
Create `.planning/phases/17-tooling-in-02-fut-05/17-01-SUMMARY.md` when done
</output>
