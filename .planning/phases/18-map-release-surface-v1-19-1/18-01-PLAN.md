---
phase: 18-map-release-surface-v1-19-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/plugin.json
  - .cursor-plugin/plugin.json
  - CHANGELOG.md
  - RELEASE-INFO.txt
  - README.md
  - docs/index.html
  - docs/packs.html
  - docs/products/website/01-jgs-se-knowledge-packs.yaml
  - docs/products/website/catalog.yaml
  - docs/capability-pack-map.json
  - docs/capability-map-CONTRACT.md
  - docs/capability-pack-map.md
autonomous: true
requirements: [MAP-20-01, REL-20-01, REL-20-02]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "python tooling/check_release.py exits 0 printing RELEASE CHECK: PASS (overlap OVERLAP: PASS then map TOTAL: 644 print first inside the release gate)"
    - "python tooling/check_capability_map.py exits 0; schema_version 2; map_version 1.19.1; TOTAL 644"
    - "python -c \"import json;print(len(json.load(open('catalog.json', encoding='utf-8'))['packs']))\" prints 63 and pack directory count is 65"
    - "All 11 Phase-9/13 version surfaces plus plugin/CHANGELOG/RELEASE-INFO trio read 1.19.1; docs/packs.html is byte-identical to a fresh python tooling/gen_packs_page.py run"
    - "CHANGELOG first version heading is ## [1.19.1]; entry records hygiene, source-retry deferral, PACK deferred-with-evidence, overlap tooling, FUT-05 residual; still deferred FUT-04 / AAF / IO-05 / IO-06; no new packs; Catalogue still 63 (+2 signposts)"
    - "New CHANGELOG [1.19.1] body has zero Unicode em dashes and zero scheme-prefix tokens; JGSC copyright + SPDX header in the first 600 chars remain"
    - "Residual 1.19.0 outside .planning/.git/sources is history-only (CHANGELOG [1.19.0] region, capability-pack-map.md v1.19.0 changelog, SOURCE-VETTING v1.19.0 headings)"
    - "No pack rebuild; no map cluster/membership rewrite; no CI repo-Python; SKILLS/NOTICE/cursor skill lists not re-edited; catalog.json not rewritten"
    - "This plan does not tag, push, or gh-release (18-02 / REL-20-02 owns that)"
    - "REQUIREMENTS MAP-20-01 / REL-20-01 / REL-20-02 boxes stay unchecked"
  artifacts:
    - path: .claude-plugin/plugin.json
      provides: "gate trio member version 1.19.1"
      contains: '"version": "1.19.1"'
    - path: RELEASE-INFO.txt
      provides: "gate trio member Version/Tag 1.19.1 + live Staged timestamp"
      contains: "1.19.1"
    - path: CHANGELOG.md
      provides: "honest ## [1.19.1] entry above [1.19.0]"
      contains: "## [1.19.1]"
    - path: docs/packs.html
      provides: "regenerated REV 1.19.1 (never hand-edited)"
    - path: docs/capability-pack-map.json
      provides: "map_version bumped to 1.19.1; 644 entries untouched"
      contains: '"map_version": "1.19.1"'
    - path: docs/capability-map-CONTRACT.md
      provides: "live example envelope map_version 1.19.1"
      contains: '"map_version": "1.19.1"'
  key_links:
    - from: RELEASE-INFO.txt
      to: docs/packs.html
      via: "python tooling/gen_packs_page.py after Version/Tag bump"
      pattern: "1.19.1"
    - from: .claude-plugin/plugin.json
      to: CHANGELOG.md
      via: "check_release.py §4 trio (plugin == CHANGELOG top == RELEASE-INFO)"
      pattern: "1.19.1"
    - from: docs/capability-pack-map.json
      to: docs/capability-map-CONTRACT.md
      via: "envelope map_version tracks the release that publishes the bump"
      pattern: '"map_version": "1.19.1"'

# Prohibitions (must-NOT). No check_kind / check_target fields.
# - must-NOT rebuild or re-vet packs; must-NOT invent AAF / Army CBA / stakeholder / dodm-5000-102 packs
# - must-NOT reclassify map clusters; bump map_version string only; membership stays 644
# - must-NOT unwire check_capability_map.main() or check_overlap.main() from check_release
# - must-NOT add a CI repo-Python map step; must-NOT edit .github/workflows/validate.yml
# - must-NOT hand-edit docs/packs.html
# - must-NOT git add -A / git add docs/ / git add .
# - must-NOT write notes under /tmp; must-NOT create a lightweight tag
# - must-NOT bump branch protection
# - must-NOT re-edit SKILLS.md / NOTICE / cursor skill path lists
# - must-NOT tag, push, or gh release create (18-02)
# - must-NOT tick MAP-20-01 / REL-20-01 / REL-20-02 boxes
# - must-NOT edit catalog.json
---

<objective>
Wave 1 of the Phase 13 analog for v1.19.1: bump every version surface (including map_version + CONTRACT example envelope), land an honest CHANGELOG [1.19.1] (hygiene + deferred source retries + PACK deferred + overlap tooling + FUT-05 residual; still-deferred FUT-04/AAF/IO-05/06 visible), and leave both gates PASS at the same 63 catalog / 65 dirs basis. No new packs.

Purpose: MAP-20-01 (map validates; map_version 1.19.1) and REL-20-01 (full registration of any new packs = none; both gates PASS). REL-20-02 CHANGELOG honesty is landed here; annotated tag + GitHub Release is 18-02. Do not tag or push here.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<read_first>
.planning/phases/18-map-release-surface-v1-19-1/18-RESEARCH.md
.planning/phases/18-map-release-surface-v1-19-1/18-PATTERNS.md
.planning/milestones/v1.19.0-phases/13-release-surface-v1-19-0/13-01-PLAN.md
CHANGELOG.md
RELEASE-INFO.txt
.claude-plugin/plugin.json
docs/capability-pack-map.json
docs/capability-map-CONTRACT.md
README.md
</read_first>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-map-release-surface-v1-19-1/18-RESEARCH.md
@.planning/phases/18-map-release-surface-v1-19-1/18-PATTERNS.md
@.planning/milestones/v1.19.0-phases/13-release-surface-v1-19-0/13-01-PLAN.md
@CHANGELOG.md
@RELEASE-INFO.txt
@docs/capability-pack-map.json
@docs/capability-map-CONTRACT.md
</context>

<interfaces>
From tooling/check_release.py §4 (version single-source; all three must match after Task 2):
- `.claude-plugin/plugin.json` key `version` (JSON string)
- CHANGELOG.md first Keep-a-Changelog heading `## [<semver>]`
- RELEASE-INFO.txt line `Version:    <semver>`

From tooling/check_release.py §5c/5d/5e:
- `docs/packs.html` regenerated from RELEASE-INFO via `python tooling/gen_packs_page.py` (never hand-edited)
- in-process `check_overlap.main()` then `check_capability_map.main()`

From docs/capability-pack-map.json envelope:
- `schema_version` int 2 (do not bump)
- `map_version` string (bump to `1.19.1`)
- `generated_on` informational ISO date (leave `2026-08-17`; no membership regen)
- cluster chapter total 644 across 32 named clusters

From docs/capability-map-CONTRACT.md:
- example envelope `map_version` (line 15 live)
- `e.g. "<semver>"` current-shape example (line 35)
- historical `release 1.17.0` sentence (line 54) stays
</interfaces>

<plan_notes>
Discuss was skipped (no 18-CONTEXT.md). Locked decisions are 18-RESEARCH user_constraints. Analog is archived 13-01-PLAN.md (not Phase 16/17 pack or tooling work).

TRACER_MODE: Task 1 is the production slice for the version trio + generated page + map envelope. Task 2 lands CHANGELOG honesty. Task 3 proves both gates at the frozen 63/65 basis.

REVERSIBILITY: all edits in this plan are reversible (revert the scoped commits). Tag/push is 18-02.

Stay on main. No branches / worktrees. Use `python`. Record `PRE_RELEASE_HEAD=$(git rev-parse HEAD)` at the start of Task 1 into 18-01-SUMMARY.md so 18-02 can soft-reset to one release commit.

GSD per-task commits are allowed here. 18-02 will consolidate them into one `release(v1.19.1)` content commit before tagging (Phase 9/13 deviation, required).

Phase 16 DEFERRED_ALL: catalog 63 / dirs 65 frozen. Do not add packs. Do not rewrite catalog.json.

## Deviations ledger
Executor MUST keep a `## Deviations` section in 18-01-SUMMARY.md. Record every departure (extra files touched, CHANGELOG wording that drops a required honesty token, map membership edited). If none: write `None.` Do not invent silent scope.
</plan_notes>

<claim_verification>
Live commands run this planner session (2026-08-20), cwd repo root. Do not invent replacements.

| claim | command | observed | status |
|---|---|---|---|
| Branch is main; only v1.19.0 tag in 1.19 series | `git branch --show-current`; `git tag -l 'v1.19*'` | `main`; `v1.19.0` only | VERIFIED |
| Both gates PASS at 1.19.0 basis | `python tooling/check_capability_map.py`; `python tooling/check_release.py` | map `PASS: capability map OK` TOTAL 644 exit 0; overlap `OVERLAP: PASS` then map block then `RELEASE CHECK: PASS` exit 0 | VERIFIED |
| Version trio still 1.19.0; no [1.19.1] | plugin / cursor / CHANGELOG top / RELEASE-INFO | plugin `1.19.0`; cursor `1.19.0`; `## [1.19.0]: 2026-08-17`; `Version:    1.19.0` `Tag:        v1.19.0` `Staged:     2026-08-17T22:56:12Z` | VERIFIED |
| 11 display surfaces at 1.19.0 | grep/read | plugin L4; cursor L5; README L10/L58/L226; index.html L110/L226; packs.html L86; catalog.yaml L13; 01-jgs-se-knowledge-packs.yaml L15 | VERIFIED |
| map envelope still 1.19.0 / 644 | python json load | schema 2, map_version `1.19.0`, generated_on `2026-08-17`, 32 clusters, 644 entries | VERIFIED |
| CONTRACT example envelope 1.19.0 | `docs/capability-map-CONTRACT.md` | L15 `"map_version": "1.19.0"`; L35 `e.g. "1.19.0"`; L54 historical `1.17.0`; §8 FUT-05 residual already present | VERIFIED |
| catalog / dirs frozen | python catalog; `ls packs \| wc -l` | n_catalog 63; dirs 65 | VERIFIED |
| No leftover registration (unlike 13-01) | catalog dod-vva-rpg; README live rows | RPG chapters 13 already; nasa-std-8719-14 / is-gps-200n already live; do not re-edit catalog.json | VERIFIED |
| capability-pack-map.md history | L15-17 | Changelog (v1.19.0) / (v1.18.0) / (v1.17.0) present; add a v1.19.1 line, do not rewrite 1.19.0 | VERIFIED |
| CI never execs repo Python | `.github/workflows/validate.yml` header | `never executes checked-out repository code` | VERIFIED |
| Link policy SOURCE-VETTING | count of scheme prefixes in `docs/SOURCE-VETTING.md` | 0 | VERIFIED |
| gh authenticated | `gh auth status` | `systems-researcher` active (repo scope); `jgsystemsconsulting` present inactive; `gh repo view` succeeds | VERIFIED |
| Live tag style | `git tag -l -n3 v1.19.0 v1.18.0` | colon style, annotated (`git cat-file -t v1.19.0` == `tag`) | VERIFIED |
| Surprise untracked (do not ship) | `git status --short` | `??` `.planning/phases/18-map-release-surface-v1-19-1/master_flow_state.json` — never stage | VERIFIED |
| Admin-bypass still in force | `.planning/STATE.md` Deviations | Branch protection left at admin-bypass (2026-08-16); do not change | VERIFIED |
| Phase 16 DEFERRED_ALL | 16-01-SUMMARY / STATE | zero packs built; PACK-20-01..03 deferred-with-evidence | VERIFIED |
| Phase 17 overlap + FUT-05 residual | 17-01-SUMMARY / CONTRACT §8 | check_overlap on release path; FUT-05 = mechanical checker slice, no byte-stable full-map generator | VERIFIED |
| estimate-calibration | `gsd_run query estimate-calibration` | factor 1, sample_count 0, confidence low | VERIFIED |
</claim_verification>

<tasks>

<task type="tracer" reversibility="reversible">
  <name>End-to-end version surfaces — trio + packs.html + map envelope to 1.19.1</name>
  <files>.claude-plugin/plugin.json, RELEASE-INFO.txt, .cursor-plugin/plugin.json, README.md, docs/index.html, docs/products/website/01-jgs-se-knowledge-packs.yaml, docs/products/website/catalog.yaml, docs/packs.html, docs/capability-pack-map.json, docs/capability-map-CONTRACT.md, docs/capability-pack-map.md</files>
  <read_first>RELEASE-INFO.txt, .claude-plugin/plugin.json, .cursor-plugin/plugin.json, README.md, docs/index.html, docs/products/website/catalog.yaml, docs/products/website/01-jgs-se-knowledge-packs.yaml, docs/capability-pack-map.json, docs/capability-map-CONTRACT.md, docs/capability-pack-map.md, tooling/gen_packs_page.py</read_first>
  <action>
Record `PRE_RELEASE_HEAD=$(git rev-parse HEAD)` into `.planning/phases/18-map-release-surface-v1-19-1/18-01-SUMMARY.md` (create the file if needed with that one line). 18-02 soft-resets to this SHA.

Order is locked by the Phase 13 analog. Do NOT touch CHANGELOG.md here (Task 2 owns `## [1.19.1]`). After this task the trio is intentionally split (plugin/RELEASE-INFO 1.19.1, CHANGELOG still 1.19.0) — do not run check_release expecting PASS.

1. Edit `RELEASE-INFO.txt` FIRST:
   - `Version:    1.19.1`
   - `Tag:        v1.19.1`
   - `Staged:` real execution timestamp in `YYYY-MM-DDTHH:MM:SSZ` form (use actual UTC now, not a hardcoded value, not the old `2026-08-17T22:56:12Z`).

2. Run `python tooling/gen_packs_page.py`. Never hand-edit `docs/packs.html`. Confirm the REV span is 1.19.1. Re-run the generator; `git diff --stat docs/packs.html` must then be empty.

3. Hand-edit the remaining display surfaces 1.19.0 to 1.19.1 (version field / REV / badge only):
   - `.claude-plugin/plugin.json` line 4 `"version": "1.19.1"`
   - `.cursor-plugin/plugin.json` line 5 `"version": "1.19.1"` (version field only; do not touch the skills array)
   - `README.md` line 10 badge `version-1.19.1-green` + alt `Version 1.19.1`; line 58 `(version 1.19.1)`; line 226 `Current: 1.19.1.`
   - `docs/index.html` lines 110 and 226 (both REV spans)
   - `docs/products/website/catalog.yaml` line 13 `version: "1.19.1"`
   - `docs/products/website/01-jgs-se-knowledge-packs.yaml` line 15 `version: "1.19.1"`

4. Bump map envelope only (do not touch `clusters` / membership / notes):
   - `docs/capability-pack-map.json` line 3 `"map_version": "1.19.1"`. Leave `generated_on` as `2026-08-17` (regen day; informational). Do not regenerate the map.

5. Align CONTRACT live example:
   - line 15 example envelope `"map_version": "1.19.1"`
   - line 35 `e.g. "1.19.1"` (refresh the current-shape example)
   - Leave line 54 historical `1.17.0` untouched.
   - Do not rewrite CONTRACT §7 or §8 (overlap + FUT-05 residual already shipped in Phase 17).

6. Add a history line at `docs/capability-pack-map.md` immediately above the existing Changelog (v1.19.0) line: Changelog (v1.19.1) stating map_version bump only, membership still 644, no reclassification. Leave Changelog (v1.19.0), (v1.18.0), (v1.17.0) as history.

7. Do NOT touch SOURCE-VETTING headings, historical 1.17.0 refs, SKILLS.md, NOTICE, catalog.json, or pack files.

8. Commit these files only if GSD requires a per-task commit (`chore(release): bump version surfaces to 1.19.1`). Explicit paths. Never `-A`.
  </action>
  <verify>
    <automated>python -c "
from pathlib import Path
import json, re
assert json.loads(Path('.claude-plugin/plugin.json').read_text(encoding='utf-8'))['version']=='1.19.1'
assert json.loads(Path('.cursor-plugin/plugin.json').read_text(encoding='utf-8'))['version']=='1.19.1'
ri=Path('RELEASE-INFO.txt').read_text(encoding='utf-8')
assert re.search(r'Version:\s*1\.19\.1', ri)
assert re.search(r'Tag:\s*v1\.19\.1', ri)
assert '2026-08-17T22:56:12Z' not in ri
rd=Path('README.md').read_text(encoding='utf-8')
assert 'version-1.19.1-green' in rd and 'Current: 1.19.1.' in rd and '(version 1.19.1)' in rd
idx=Path('docs/index.html').read_text(encoding='utf-8')
assert idx.count('1.19.1')>=2 and '1.19.0' not in idx
assert '1.19.1' in Path('docs/packs.html').read_text(encoding='utf-8')
assert '1.19.0' not in Path('docs/packs.html').read_text(encoding='utf-8')
assert Path('docs/products/website/catalog.yaml').read_text(encoding='utf-8').splitlines()[12].find('1.19.1')>=0
assert Path('docs/products/website/01-jgs-se-knowledge-packs.yaml').read_text(encoding='utf-8').splitlines()[14].find('1.19.1')>=0
mp=json.loads(Path('docs/capability-pack-map.json').read_text(encoding='utf-8'))
assert mp['map_version']=='1.19.1' and mp['schema_version']==2
assert mp['generated_on']=='2026-08-17'
assert sum(len(c.get('chapters',[])) for c in mp['clusters'])==644
c=Path('docs/capability-map-CONTRACT.md').read_text(encoding='utf-8')
assert '\"map_version\": \"1.19.1\"' in c
assert 'release `1.17.0`' in c
assert 'FUT-05 residual' in c
cl=Path('CHANGELOG.md').read_text(encoding='utf-8')
assert '## [1.19.1]' not in cl and '## [1.19.0]' in cl
md=Path('docs/capability-pack-map.md').read_text(encoding='utf-8')
assert 'Changelog (v1.19.1)' in md and 'Changelog (v1.19.0)' in md
print('SURFACES_OK')
" && python tooling/gen_packs_page.py && git diff --stat docs/packs.html</automated>
  </verify>
  <acceptance_criteria>
    - RELEASE-INFO Version/Tag are 1.19.1 and Staged is a fresh UTC timestamp
    - packs.html regenerated (not hand-edited) and idempotent on second gen
    - plugin, cursor plugin, README x3, index.html x2, two website YAMLs all 1.19.1
    - map_version is 1.19.1; cluster membership still 644; schema_version still 2; generated_on still 2026-08-17
    - CONTRACT example envelope is 1.19.1; historical 1.17.0 line remains; §8 FUT-05 residual remains
    - CHANGELOG still headed ## [1.19.0] (Task 2 owns the new heading)
    - cursor skills array unchanged
    - catalog.json untouched
  </acceptance_criteria>
  <done>All 11 display surfaces + map_version + CONTRACT example read 1.19.1; packs.html regenerated and idempotent; CHANGELOG heading not yet inserted; PRE_RELEASE_HEAD recorded.</done>
</task>

<task type="auto" reversibility="reversible">
  <name>Task 2: CHANGELOG [1.19.1] honest cleanup + tooling + still-deferred items</name>
  <files>CHANGELOG.md</files>
  <read_first>CHANGELOG.md, .planning/ROADMAP.md, .planning/phases/18-map-release-surface-v1-19-1/18-RESEARCH.md, .planning/STATE.md</read_first>
  <action>
Insert `## [1.19.1]: YYYY-MM-DD` (today UTC date) above `## [1.19.0]: 2026-08-17` (today line 12). House format: Keep-a-Changelog, `### Added` / `### Changed` / `### Fixed` / deferred section. Lead with honesty about this milestone, not a pack list (REL-20-02). Starting shape (tighten if needed; ASCII hyphen / `->` only; no scheme-prefix tokens):

## [1.19.1]: YYYY-MM-DD

Hygiene, source-retry evidence, deferred pack builds, overlap tooling, and
FUT-05 residual on the same catalog 63 / dirs 65 basis as 1.19.0. No new packs.
Deferrals stay visible. Catalogue still 63 packs (+2 signposts).

### Added

- Overlap checker (`tooling/check_overlap.py`) on the `check_release.py` path
  (IN-02 / TOOL-20). Multi-pack chapter-basename collisions fail the release
  gate. Intentional shared `ch01-introduction.md` is WHITELIST-only.

### Changed

- Version surfaces and `map_version` 1.19.0 -> 1.19.1. Cluster membership still
  644; schema_version still 2. No reclassification.
- CONTRACT section 8 records FUT-05 residual: mechanical
  `check_capability_map.py` slice only; no byte-stable full-map generator.

### Fixed

- Phase 14 ledger / planning hygiene (milestone archives, MAP-19 ticks in the
  v1.19.0 archive, live surfaces).

### Deferred / accepted (not built)

- **FUT-04 Army CBA** DEFERRED - ASAFM PDF 403 on 2026-08-20 retry; no in-source grant.
- **AAF Product Support + Software pathway** still NOT yet vetted - do not use.
- **PACK-20-01..03** deferred-with-evidence; zero packs built this milestone.
- **IO-05 Integration** DEFERRED - AAF Software pathway still NOT yet vetted.
- **IO-06 Logistics diversity** DEFERRED - AAF Product Support still NOT yet vetted.
- **IO-07 Stakeholder Engagement** ACCEPT - no invented pack.

HARD CONSTRAINTS:
- Draft uses ASCII hyphen / `->` only. Unicode dash punctuation in the new entry is a fail.
- No scheme-prefix tokens in the new entry (link-policy scans CHANGELOG).
- Do not claim catalog grew. "Catalogue still 63 packs (+2 signposts)" is the honest now-count.
- Keep publisher names bare. No source-host links.
- Do not claim IO-05/06 were built, AAF was vetted, Army CBA shipped, or full FUT-05 closed.
- Do not invent packs or pack slugs.
- JGSC copyright + SPDX header in the first 600 chars must remain (check_release authored-file header).

Do not edit catalog.json, README live-pack rows, SKILLS.md, NOTICE, or packs.html here.

Commit if GSD requires (`docs(release): CHANGELOG 1.19.1 honesty`). Explicit paths only.
  </action>
  <verify>
    <automated>python -c "
from pathlib import Path
cl=Path('CHANGELOG.md').read_text(encoding='utf-8')
assert cl.count('## [1.19.1]')==1
assert cl.find('## [1.19.1]') &lt; cl.find('## [1.19.0]')
new=cl.split('## [1.19.1]')[1].split('## [1.19.0]')[0]
assert '\u2014' not in new, 'em dash in new CHANGELOG entry'
assert 'http' not in new
for token in ['Hygiene','FUT-04','AAF','PACK-20','Overlap','FUT-05','IO-05','IO-06','IO-07','DEFERRED','Catalogue still 63']:
    assert token in new, token
assert 'ACCEPT' in new
assert cl[:600].find('SPDX-License-Identifier')&gt;=0
print('CHANGELOG_OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG first version heading is ## [1.19.1] above ## [1.19.0]
    - New entry names hygiene, source-retry deferral, PACK deferred, overlap tooling, FUT-05 residual
    - FUT-04 / AAF / IO-05 / IO-06 recorded deferred; IO-07 recorded accept; no invented-pack claim
    - New entry has zero Unicode em dashes and zero scheme-prefix tokens
    - Catalogue still 63 packs (+2 signposts); no catalog growth claim
    - catalog.json still untouched; trio can now agree at 1.19.1
  </acceptance_criteria>
  <done>Honest [1.19.1] entry landed; trio can now agree at 1.19.1; REL-20-02 CHANGELOG half ready for 18-02 notes.</done>
</task>

<task type="auto" reversibility="reversible">
  <name>Task 3: Dual-gate PASS + residual-version sweep at 63/65</name>
  <files></files>
  <precondition>Tasks 1-2 edits complete. plugin == CHANGELOG top == RELEASE-INFO == 1.19.1. git status --short shows no surprise untracked outside .planning/ (master_flow_state.json must remain untracked).</precondition>
  <read_first>tooling/check_release.py, tooling/check_capability_map.py</read_first>
  <action>
Run the full validation battery. If any step fails, STOP and fix — do not proceed to 18-02.

1. `python tooling/gen_packs_page.py` then confirm `git diff --stat docs/packs.html` is empty.
2. `python tooling/check_capability_map.py` — exit 0; TOTAL 644; map_version 1.19.1.
3. `python tooling/check_release.py` — exit 0 printing `RELEASE CHECK: PASS` (overlap then map cluster block print first). Independently: catalog pack count 63; pack directory count 65.
4. Residual 1.19.0 sweep excl `.planning` / `.git` / `sources` must be history-only:
   - `CHANGELOG.md` `## [1.19.0]` region
   - `docs/capability-pack-map.md` Changelog (v1.19.0)
   - `docs/SOURCE-VETTING.md` v1.19.0 headings
   No live REV, plugin, RELEASE-INFO, README badge, index/packs.html, or website YAML may still say 1.19.0.
5. Historical 1.17.0 whitelist must still exist: CONTRACT line 54; capability-pack-map.md v1.17.0 changelog; CHANGELOG `[1.17.0]` region.
6. Fence: `git diff --name-only` (and any 18-01 commits) must not include `packs/*/chapters/*`, `.github/workflows/validate.yml`, `SKILLS.md`, `NOTICE`, `tooling/check_release.py`, `tooling/check_capability_map.py`, `tooling/check_overlap.py`, `catalog.json`. SOURCE-VETTING must still have zero scheme-prefix tokens.
7. Re-run `python tooling/check_release.py` immediately before handing off to 18-02 (OneDrive sync-lag; Phase 5/9/13). This is the last action of this plan.
  </action>
  <verify>
    <automated>python tooling/gen_packs_page.py &amp;&amp; git diff --stat docs/packs.html &amp;&amp; python tooling/check_capability_map.py &amp;&amp; python tooling/check_release.py &amp;&amp; python -c "
import json, os
from pathlib import Path
assert len(json.load(open('catalog.json', encoding='utf-8'))['packs'])==63
mp=json.load(open('docs/capability-pack-map.json', encoding='utf-8'))
assert mp['map_version']=='1.19.1'
assert sum(len(c.get('chapters',[])) for c in mp['clusters'])==644
assert len([n for n in os.listdir('packs') if not n.startswith('.')])==65
live=['.claude-plugin/plugin.json','.cursor-plugin/plugin.json','RELEASE-INFO.txt','README.md','docs/index.html','docs/packs.html','docs/products/website/catalog.yaml','docs/products/website/01-jgs-se-knowledge-packs.yaml','docs/capability-pack-map.json']
for p in live:
    assert '1.19.0' not in Path(p).read_text(encoding='utf-8'), p
assert '## [1.19.0]' in Path('CHANGELOG.md').read_text(encoding='utf-8')
assert 'Changelog (v1.19.0)' in Path('docs/capability-pack-map.md').read_text(encoding='utf-8')
assert 'v1.19.0' in Path('docs/SOURCE-VETTING.md').read_text(encoding='utf-8')
assert Path('docs/SOURCE-VETTING.md').read_text(encoding='utf-8').count('http')==0
print('BASIS_OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - both gates PASS; catalog 63 / dirs 65
    - map_version 1.19.1; TOTAL 644
    - packs.html idempotent
    - residual 1.19.0 is whitelist-only
    - no pack/map-membership/CI/SKILLS/NOTICE/tooling/catalog.json files in the 18-01 diff
    - check_release re-run is the last action
    - MAP-20 / REL-20 boxes still unchecked
  </acceptance_criteria>
  <done>MAP-20-01 and REL-20-01 local truths hold: surfaces synchronized, no new packs, both gates PASS at 63/65, map_version 1.19.1. Ready for 18-02 tag/push/gh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → 18-01 commits | Untracked `.planning` state files must not cross into content commits |
| version trio → check_release §4 | Split trio after Task 1 is expected; Task 3 must see agreement |
| map envelope → CONTRACT example | map_version string must track the release that publishes the bump |
| honesty → consumers | Gate does not assert CHANGELOG deferral tokens; REL-20-02 still owns them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | Staging | high | mitigate | Explicit-path-only adds; STOP on surprise untracked; never `-A` |
| T-18-02 | Tampering | packs.html | medium | mitigate | RELEASE-INFO first then gen_packs_page.py; never hand-edit; idempotent re-run |
| T-18-03 | Information disclosure | CHANGELOG link policy | low | mitigate | No URLs or Unicode em dashes in the new entry; gate scans CHANGELOG |
| T-18-04 | Spoofing | map_version orphan | medium | mitigate | Bump JSON + CONTRACT example in Task 1; do not regenerate membership |
| T-18-05 | Elevation of privilege | CI validate.yml | high | mitigate | Do not add repo-Python; do not unwire the local overlap or map import |
| T-18-06 | Tampering | Invented packs | high | mitigate | catalog.json / packs/ / SKILLS.md / NOTICE out of this plan; 63/65 frozen |
| T-18-07 | Repudiation | Dishonest CHANGELOG | medium | mitigate | Task 2 named honesty tokens; Task 3 does not paper over FUT-04/AAF/IO-05/06 |
| T-18-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs this phase |
</threat_model>

## Artifacts

| Artifact | Produced by | Consumed by |
|----------|-------------|-------------|
| Version trio 1.19.1 + 11 display surfaces | Task 1 | Task 3 / check_release §4 / 18-02 tag |
| Regenerated `docs/packs.html` | Task 1 (`gen_packs_page.py`) | check_release §5c |
| `map_version` 1.19.1 + CONTRACT example | Task 1 | check_capability_map / 18-02 |
| CHANGELOG `## [1.19.1]` honest entry | Task 2 | 18-02 `gh release create --notes-file` |
| Dual-gate PASS at 63/65 | Task 3 | 18-02 precondition |
| `PRE_RELEASE_HEAD` in 18-01-SUMMARY.md | Task 1 | 18-02 soft-reset |

<verification>
1. `python tooling/check_release.py` → exit 0, RELEASE CHECK: PASS; catalog 63 / dirs 65 (REL-20-01).
2. `python tooling/check_capability_map.py` → exit 0; map_version 1.19.1; TOTAL 644 (MAP-20-01).
3. CHANGELOG `[1.19.1]` names hygiene, PACK deferred, overlap, FUT-05 residual, FUT-04/AAF/IO-05/06 deferred; no Unicode em dash; no scheme-prefix tokens.
4. Residual 1.19.0 is history-only. No tag/push in this plan (REL-20-02 public act is 18-02).
5. REQUIREMENTS MAP-20 / REL-20 boxes remain unchecked.
</verification>

<success_criteria>
- MAP-20-01: check_capability_map PASS; map_version 1.19.1; membership 644.
- REL-20-01: both gates PASS at 63 catalog / 65 dirs; 11 surfaces + trio at 1.19.1; no new packs to register; packs.html byte-identical regeneration.
- REL-20-02 content prerequisite: honest CHANGELOG `[1.19.1]` ready for GitHub Release notes. Tag/push/gh themselves are 18-02.
- Phase 16/17 fence held (no pack rebuild, no remap, no CI Python, no SKILLS/NOTICE rewrite, overlap/map wires untouched).
</success_criteria>

<output>
Create `.planning/phases/18-map-release-surface-v1-19-1/18-01-SUMMARY.md` when done. Include PRE_RELEASE_HEAD and a `## Deviations` section.
</output>
