---
phase: 18-map-release-surface-v1-19-1
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - .claude-plugin/plugin.json
  - .cursor-plugin/plugin.json
  - CHANGELOG.md
  - RELEASE-INFO.txt
  - README.md
  - docs/index.html
  - docs/packs.html
  - docs/products/website/01-jgs-se-knowledge-packs.yaml
  - docs/products/website/catalog.yaml
  - docs/capability-pack-map.json
  - docs/capability-map-CONTRACT.md
  - docs/capability-pack-map.md
  - .planning/STATE.md
  - .planning/MILESTONES.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [MAP-20-01, REL-20-01, REL-20-02]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "18-01 landed first: both gates PASS at 63 catalog / 65 dirs; trio + map_version are 1.19.1; CHANGELOG headed ## [1.19.1]; no new packs"
    - "One release commit is the last CONTENT commit on main: release(v1.19.1): hygiene + overlap tooling + deferred items visible (63 +2 signposts)"
    - "Annotated tag v1.19.1 exists (git cat-file -t $(git rev-parse v1.19.1) == tag) with colon-style message; lightweight tag is a fail"
    - "git push origin main --follow-tags succeeded; git ls-remote --tags origin | grep v1.19.1 returns the tag"
    - "gh release view v1.19.1 succeeds; title uses an em dash; notes body equals the CHANGELOG [1.19.1] entry body (hygiene + tooling + still-deferred)"
    - "Release commit staged explicit paths only; no catalog.json unless 18-01 actually edited it (it must not); no master_flow_state.json; no 18-RESEARCH/PATTERNS; no phase-dir notes tmp"
    - "Separate .planning-only follow-up commit records SHA/tag/URL in STATE + MILESTONES, ticks ROADMAP Phase 18, lists 18-01-PLAN.md and 18-02-PLAN.md"
    - "REQUIREMENTS MAP-20-01 / REL-20-01 / REL-20-02 boxes stay unchecked (phase.complete owns ticks)"
    - "No pack rebuild; no map reclassify; no CI repo-Python; no branch-protection change; no git add -A"
  artifacts:
    - path: .planning/STATE.md
      provides: "v1.19.1 shipped record (SHA, tag, GitHub Release URL) + remaining out-of-tree backlog"
    - path: .planning/MILESTONES.md
      provides: "v1.19.1 converted from in-execution to shipped format"
    - path: .planning/ROADMAP.md
      provides: "Phase 18 ticked; Plans lists 18-01 and 18-02"
  key_links:
    - from: CHANGELOG.md
      to: GitHub Release v1.19.1
      via: "gh release create --notes-file phase-dir tmp copied from [1.19.1] body"
      pattern: "## [1.19.1]"
    - from: git tag v1.19.1
      to: origin
      via: "git push origin main --follow-tags"
      pattern: "v1.19.1"
    - from: release commit
      to: annotated tag
      via: "tag points at the last content commit; .planning commit comes after"
      pattern: "release(v1.19.1)"

# Prohibitions (must-NOT). No check_kind / check_target fields.
# - must-NOT git add -A / git add docs/ / git add .
# - must-NOT write gh --notes-file under /tmp
# - must-NOT create a lightweight tag
# - must-NOT tag on a red gate or dirty surprise-untracked tree
# - must-NOT rebuild packs or reclassify the map
# - must-NOT unwire the overlap or map gate or edit validate.yml
# - must-NOT invent packs; must-NOT bump branch protection
# - must-NOT put the .planning records commit into the tagged release commit
# - must-NOT commit _v1.19.1-notes.tmp.md or master_flow_state.json
# - must-NOT tick MAP-20-01 / REL-20-01 / REL-20-02
# - must-NOT add catalog.json to the release commit
---

<objective>
Wave 2 (depends_on 18-01): publish v1.19.1. Consolidate 18-01 content into one release commit, create the annotated tag, push origin main --follow-tags, create the GitHub Release, then record shipped state in a separate .planning commit.

Purpose: REL-20-02 (v1.19.1 tagged + GitHub Release). MAP-20-01 and REL-20-01 must still hold at the tagged tree. Analog: 13-02 (executed, including the push). Admin-bypass remains in force; this plan is autonomous.
</objective>

<execution_context>
@$HOME/.zcode/gsd-core/workflows/execute-plan.md
@$HOME/.zcode/gsd-core/templates/summary.md
</execution_context>

<read_first>
.planning/phases/18-map-release-surface-v1-19-1/18-01-SUMMARY.md
.planning/phases/18-map-release-surface-v1-19-1/18-01-PLAN.md
.planning/phases/18-map-release-surface-v1-19-1/18-RESEARCH.md
CHANGELOG.md
RELEASE-INFO.txt
.planning/STATE.md
.planning/MILESTONES.md
.planning/ROADMAP.md
</read_first>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MILESTONES.md
@.planning/phases/18-map-release-surface-v1-19-1/18-RESEARCH.md
@.planning/phases/18-map-release-surface-v1-19-1/18-01-PLAN.md
@.planning/milestones/v1.19.0-phases/13-release-surface-v1-19-0/13-02-PLAN.md
@CHANGELOG.md
</context>

<plan_notes>
depends_on 18-01: if either gate is RED, or CHANGELOG has no `## [1.19.1]`, STOP. Do not tag.

TRACER_MODE: Task 1 is the production public-release slice (commit + annotated tag + push + gh). Task 2 is the planning-record follow-up after the tag exists.

REVERSIBILITY: Task 1 is costly (public tag + GitHub Release are hard to retract) but pre-approved by REL-20-02 and the Phase 13 analog. No decision checkpoint. Do not flip autonomous to false.

Stay on main. No branches / worktrees. Admin-bypass is still in force — push is expected to succeed without a PR. Do not "fix" branch protection. If push is rejected, STOP and record in Deviations; do not open a PR.

Windows: write the gh notes file under this phase directory, never `/tmp`. Delete it after `gh release view` succeeds. Never commit it.

Do not tick MAP-20 / REL-20 REQUIREMENTS boxes (phase.complete owns those ticks). Analog 13-02 ticked REL-19; this phase does not.

## Deviations ledger
Executor MUST keep a `## Deviations` section in 18-02-SUMMARY.md. Record every departure (soft-reset performed or skipped, gh notes retry, extra files in the release commit, push failure). If none: write `None.`
</plan_notes>

<claim_verification>
Live commands run this planner session (2026-08-20), cwd repo root. 18-01 will have changed the version surfaces by the time this plan runs — re-verify gates at task start. Pre-18-01 observations:

| claim | command | observed | status |
|---|---|---|---|
| Branch is main; v1.19.0 exists; v1.19.1 does not | `git branch --show-current`; `git tag -l 'v1.19*'` | `main`; `v1.19.0` only | VERIFIED |
| Phase 13 analog did push + gh | archived `13-02-PLAN.md` Task 1 | `git push origin main --follow-tags`; origin tag + `gh release view v1.19.0` succeeded | VERIFIED |
| Live tag style | `git tag -l -n3 v1.18.0 v1.19.0` | colon style; `git cat-file -t v1.19.0` == `tag` | VERIFIED |
| Live GitHub Release title style | `gh release view v1.19.0 --json name,tagName,url` | `v1.19.0 — Agent IO Depth (2 packs + VV&A chapters + DA remap)` | VERIFIED |
| gh authenticated as publisher | `gh auth status`; `gh repo view` | `systems-researcher` active (repo scope); `jgsystemsconsulting/jgs-se-knowledge-packs` default branch main | VERIFIED |
| /tmp notes failed on Windows in Phase 9 | 13-02 analog | notes file under phase dir, never `/tmp` | VERIFIED |
| Soft-reset was required in Phase 9/13 | 13-02 analog | intermediate per-task commits soft-reset into one release commit | VERIFIED |
| Surprise untracked must not ship | `git status --short` | `??` `.planning/phases/18-map-release-surface-v1-19-1/master_flow_state.json` | VERIFIED |
| Admin-bypass still in force | `.planning/STATE.md` Deviations | do not change protection; push expected to succeed | VERIFIED |
| MILESTONES v1.19.1 is still in-execution | `.planning/MILESTONES.md` | `## v1.19.1 (in execution)` | VERIFIED |
| REL/MAP boxes open | `.planning/REQUIREMENTS.md` | MAP-20-01, REL-20-01, REL-20-02 unchecked — leave them unchecked | VERIFIED |
| catalog.json not a leftover | 18-01 fence | do not add catalog.json to the release commit | VERIFIED |
</claim_verification>

<tasks>

<task type="tracer" reversibility="costly">
  <name>End-to-end public release — commit + annotated tag + push + GitHub Release</name>
  <files>.claude-plugin/plugin.json, .cursor-plugin/plugin.json, CHANGELOG.md, RELEASE-INFO.txt, README.md, docs/index.html, docs/packs.html, docs/products/website/01-jgs-se-knowledge-packs.yaml, docs/products/website/catalog.yaml, docs/capability-pack-map.json, docs/capability-map-CONTRACT.md, docs/capability-pack-map.md</files>
  <precondition>18-01 complete. python tooling/check_release.py PASS. python tooling/check_capability_map.py PASS. CHANGELOG contains exactly one ## [1.19.1]. gh auth status succeeds. No local v1.19.1 tag yet. PRE_RELEASE_HEAD recorded in 18-01-SUMMARY.md (or recoverable as the last commit before 18-01 content).</precondition>
  <read_first>.planning/phases/18-map-release-surface-v1-19-1/18-01-SUMMARY.md, CHANGELOG.md, RELEASE-INFO.txt</read_first>
  <reversibility rating="costly">Publishing an annotated tag + GitHub Release is hard to retract, but it is pre-approved by REL-20-02 and the Phase 13 analog (which did push). Everything before the tag is reversible. No decision checkpoint.</reversibility>
  <action>
Hard-stop if either gate is RED, if `git tag -l v1.19.1` is already present, or if `git status --short` shows surprise untracked outside `.planning/` that you cannot explain. Do not stage `master_flow_state.json`, `18-RESEARCH.md`, `18-PATTERNS.md`, `18-01-PLAN.md`, or `catalog.json` into the release commit.

1. Re-run immediately (OneDrive sync-lag): python tooling/check_capability_map.py ; python tooling/check_release.py ; python -c "import json;print(len(json.load(open('catalog.json', encoding='utf-8'))['packs']))" ; ls packs | wc -l . Expect PASS / PASS / 63 / 65.

2. Consolidate to ONE release commit (Phase 9/13 deviation, required). If 18-01 (or this session) created per-task content commits after PRE_RELEASE_HEAD: PRE_RELEASE_HEAD=$(grep -oE '[0-9a-f]{7,40}' .planning/phases/18-map-release-surface-v1-19-1/18-01-SUMMARY.md | head -1) then git rev-parse --verify "$PRE_RELEASE_HEAD" then git reset --soft "$PRE_RELEASE_HEAD". If HEAD is already a single untagged content tree with no extra 18-01 commits, skip the reset and just restage.

3. Stage EXPLICIT paths only. Never git add -A, never git add docs/, never git add . Run git status --short then git add -- .claude-plugin/plugin.json .cursor-plugin/plugin.json CHANGELOG.md RELEASE-INFO.txt README.md docs/index.html docs/packs.html docs/products/website/01-jgs-se-knowledge-packs.yaml docs/products/website/catalog.yaml docs/capability-pack-map.json docs/capability-map-CONTRACT.md . Add docs/capability-pack-map.md only if Task 1 of 18-01 actually edited it (git diff --name-only / git status after the soft-reset). Do not add catalog.json. Add no other files. Re-run git status --short and confirm the index contains only those paths.

4. Commit (this must be the last CONTENT commit on main) with subject release(v1.19.1): hygiene + overlap tooling + deferred items visible (63 +2 signposts) and body: map_version 1.19.1 / 644; overlap gate on release path; FUT-05 residual. No new packs. FUT-04/AAF/IO-05/06 still deferred. Catalogue 63 (+2 signposts). Use git commit --no-verify. Do not use a heredoc that writes extra files.

5. Annotated tag, colon style matching live tags (not lightweight): git tag -a v1.19.1 -m "v1.19.1: hygiene + overlap tooling + deferred items visible (63 +2 signposts)" then git cat-file -t "$(git rev-parse v1.19.1)" which must print tag.

6. Push. Phase 13 analog did this. Admin-bypass is in force. Do not open a PR. Do not change branch protection. Run git push origin main --follow-tags . If the push is rejected, STOP. Record the exact error in 18-02-SUMMARY.md Deviations. Do not retry via a PR. Do not continue to gh release create.

7. GitHub Release. Title uses an em dash (public title only; CHANGELOG body stays em-dash-free). Notes = CHANGELOG [1.19.1] entry body (everything after the heading, before ## [1.19.0]). Windows: do NOT write notes to /tmp (Phase 9 first gh attempt failed). Write .planning/phases/18-map-release-surface-v1-19-1/_v1.19.1-notes.tmp.md by splitting CHANGELOG on ## [1.19.1] then ## [1.19.0], dropping a leading colon date remainder if present. Then gh release create v1.19.1 --title "v1.19.1 — Hygiene + Tooling (deferrals visible)" --notes-file .planning/phases/18-map-release-surface-v1-19-1/_v1.19.1-notes.tmp.md

8. REL-20-02 is not done until both remote checks succeed: git ls-remote --tags origin | grep v1.19.1 and gh release view v1.19.1 --json name,tagName,url

9. Delete the notes tmp file. Confirm it is not tracked (git status --short must not show it as staged). Do not commit it.
  </action>
  <verify>
    <automated>git cat-file -t "$(git rev-parse v1.19.1)" &amp;&amp; git ls-remote --tags origin | grep v1.19.1 &amp;&amp; gh release view v1.19.1 --json name,tagName,url &amp;&amp; python -c "
import subprocess, json
t=subprocess.check_output(['git','cat-file','-t','v1.19.1'], text=True).strip()
assert t=='tag', t
rel=json.loads(subprocess.check_output(['gh','release','view','v1.19.1','--json','name,tagName,url,body'], text=True))
assert rel['tagName']=='v1.19.1'
assert '\u2014' in rel['name']
body=rel['body']
for token in ['FUT-04','AAF','PACK-20','overlap','FUT-05','IO-05','IO-06','IO-07']:
    assert token in body, token
assert 'DEFERRED' in body
print('RELEASE_OK', rel['url'])
"</automated>
  </verify>
  <acceptance_criteria>
    - last CONTENT commit subject starts with release(v1.19.1):
    - tag object type is tag (annotated), message uses colon style
    - origin has refs/tags/v1.19.1
    - GitHub Release exists; title contains an em dash; notes include hygiene/tooling/deferral tokens and do not claim new packs
    - release commit file list is the explicit version/docs set only (no catalog.json, no .planning)
    - notes tmp file deleted and untracked
  </acceptance_criteria>
  <done>REL-20-02 public act complete: annotated v1.19.1 on origin/main and GitHub Release published with CHANGELOG-derived honest notes.</done>
</task>

<task type="auto" reversibility="reversible">
  <name>Task 2: Post-release records — STATE, MILESTONES, ROADMAP</name>
  <files>.planning/STATE.md, .planning/MILESTONES.md, .planning/ROADMAP.md</files>
  <precondition>Task 1 done: git ls-remote --tags origin | grep v1.19.1 and gh release view v1.19.1 both succeed. Do not start this task before the tag exists.</precondition>
  <read_first>.planning/STATE.md, .planning/MILESTONES.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <action>
After the tag exists. These files are NOT in the release commit.

1. STATE.md — shipped record:
   - Record v1.19.1 shipped with release commit SHA, annotated tag, and GitHub Release URL (`gh release view v1.19.1 --json url -q .url`).
   - Close MAP-20-01 / REL-20-01 / REL-20-02 in the narrative only (REQUIREMENTS boxes stay unchecked; phase.complete owns ticks).
   - Carry remaining out-of-tree backlog explicitly: se-agents consumer refresh stays in the sibling repo; IO-07 accept (no pack); DoDM 5000.102 UNVERIFIED; FUT-04/AAF remain deferred.
   - Update current focus / current position to shipped v1.19.1 (this is the last v1.19.1 phase).

2. MILESTONES.md — convert `## v1.19.1 (in execution)` into a shipped record matching the v1.19.0 entry shape (prose + release commit + annotated tag + GitHub Release URL bullets). Honest scope: no new packs; hygiene + source-retry evidence + PACK deferred-with-evidence + overlap tooling + FUT-05 residual; map_version 1.19.1 / 644; gates PASS at 63/65. Do not claim Army CBA, AAF packs, or a byte-stable full-map generator.

3. ROADMAP.md:
   - Tick Phase 18 checkbox (`- [ ] **Phase 18` → `- [x]`).
   - Confirm Plans lists `18-01-PLAN.md` and `18-02-PLAN.md`.
   - Progress table: Phase 18 `2/2` Complete with today's date.

4. Do not edit REQUIREMENTS.md. MAP-20-01 / REL-20-01 / REL-20-02 stay `- [ ]`.

5. Do not edit PROJECT.md unless a one-line lie is sitting next to the shipped record — default is leave it.

6. Separate `.planning`-only commit, explicit paths: git add -- .planning/STATE.md .planning/MILESTONES.md .planning/ROADMAP.md then git status --short then git commit --no-verify -m "docs(phase-18): record v1.19.1 shipped". The release commit must remain the last CONTENT commit (this follow-up touches only `.planning/`).
  </action>
  <verify>
    <automated>python -c "
from pathlib import Path
s=Path('.planning/STATE.md').read_text(encoding='utf-8')
assert 'v1.19.1' in s
m=Path('.planning/MILESTONES.md').read_text(encoding='utf-8')
section=m.split('## v1.19.1')[1].split('##')[0]
assert 'releases/tag/v1.19.1' in section
assert 'in execution' not in section.split('\n')[0].lower()
r=Path('.planning/ROADMAP.md').read_text(encoding='utf-8')
assert '- [x] **Phase 18' in r
assert '18-01-PLAN.md' in r and '18-02-PLAN.md' in r
q=Path('.planning/REQUIREMENTS.md').read_text(encoding='utf-8')
assert '- [ ] **MAP-20-01**' in q
assert '- [ ] **REL-20-01**' in q
assert '- [ ] **REL-20-02**' in q
print('RECORDS_OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - STATE has SHA + tag + GitHub Release URL and carries se-agents / IO-07 / DoDM / FUT-04 / AAF deferral
    - MILESTONES v1.19.1 is a shipped entry, not in-execution
    - ROADMAP Phase 18 is [x] and lists both plan files
    - MAP-20-01 / REL-20-01 / REL-20-02 remain [ ]
    - follow-up commit is .planning-only; tagged tree is unchanged
  </acceptance_criteria>
  <done>Shipped records landed in a separate .planning commit. REQUIREMENTS MAP/REL boxes left open for phase.complete. Phase 18 planning close-out complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → public GitHub | Tag + GitHub Release are the public release act; errors are visible and hard to retract |
| working tree → release commit | Untracked strays and .planning research files must not cross the tag |
| CHANGELOG → release notes | Notes body ships publicly; new-pack or IO-05-built claims mislead consumers |
| release commit → .planning commit | Records must not rewrite the tagged tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-08 | Tampering | Release commit staging | high | mitigate | Explicit-path-only `git add`; `git status --short` audit; STOP on surprise untracked; no catalog.json |
| T-18-09 | Repudiation | Tag provenance | medium | mitigate | Annotated tag (`git cat-file -t` == tag); colon-style message matching v1.18/v1.19.0 |
| T-18-10 | Tampering | OneDrive / Windows notes path | medium | mitigate | Re-run check_release immediately before commit; notes file under phase dir, never `/tmp` |
| T-18-11 | DoS (of release) | Public release on red gate | high | mitigate | Precondition + step 1 hard-stop on non-PASS |
| T-18-12 | Elevation of privilege | Branch protection | low | accept | User opted to keep admin-bypass; do not change it; STOP if push rejected |
| T-18-13 | Information disclosure | Notes tmp left committed | low | mitigate | Delete `_v1.19.1-notes.tmp.md`; never stage it |
| T-18-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs this phase |
</threat_model>

## Artifacts

| Artifact | Produced by | Consumed by |
|----------|-------------|-------------|
| Single `release(v1.19.1)` content commit | Task 1 (soft-reset + explicit add) | annotated tag |
| Annotated tag `v1.19.1` | Task 1 | origin + GitHub Release |
| `git push origin main --follow-tags` | Task 1 | `git ls-remote` / REL-20-02 |
| GitHub Release v1.19.1 | Task 1 (`gh release create`) | consumers / MILESTONES URL |
| Phase-dir notes tmp (deleted) | Task 1 | `gh --notes-file` only |
| STATE / MILESTONES / ROADMAP | Task 2 | milestone close |
| Separate `.planning` commit | Task 2 | does not disturb tagged tree |

<verification>
1. `git cat-file -t v1.19.1` → `tag`; `git ls-remote --tags origin | grep v1.19.1` hits.
2. `gh release view v1.19.1` → name uses em dash; notes include FUT-04/AAF/PACK-20/overlap/FUT-05/IO-05/06 and do not claim new packs.
3. `git log --oneline -3` → release commit then .planning-only follow-up; no stray files in `git show --stat` of the release commit.
4. REQUIREMENTS MAP-20-01 / REL-20-01 / REL-20-02 remain `[ ]`; ROADMAP Phase 18 is `[x]`.
5. Both gates still PASS on the tagged tree (MAP-20-01 / REL-20-01 still true after publish).
</verification>

<success_criteria>
- REL-20-02: annotated tag v1.19.1 on origin; GitHub Release published with CHANGELOG-derived honest notes.
- MAP-20-01 / REL-20-01 still true on the tagged tree: both gates PASS at 63/65; map_version 1.19.1; no new packs.
- Release commit is the last content commit; .planning records are a follow-up.
- REQUIREMENTS MAP/REL boxes remain open for phase.complete.
</success_criteria>

<output>
Create `.planning/phases/18-map-release-surface-v1-19-1/18-02-SUMMARY.md` when done. Include the release SHA, tag, GitHub Release URL, and a `## Deviations` section.
</output>
