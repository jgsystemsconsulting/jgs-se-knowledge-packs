# Phase 18 Security Audit — map + release surface v1.19.1

**Phase:** 18 — map-release-surface-v1-19-1 (plans 18-01 + 18-02)
**Range audited:** release content commit `6944c143cd97741257624172302a25627b586fee` (`6944c14`); annotated tag `v1.19.1` object `5c960b46aeba0e35c3febfc49079da1782c79103`; planning follow-up `c84427a` (out of tag tree); GitHub Release https://github.com/jgsystemsconsulting/jgs-se-knowledge-packs/releases/tag/v1.19.1
**Date:** 2026-08-20
**Auditor method:** adversarial verification per declared disposition; every mitigation assumed absent until git/gh/file evidence proved presence at the correct boundary. Implementation files read-only during audit. Analog: `17-SECURITY_AUDIT.md` / `13-SECURITY_AUDIT.md`.
**WINDOWS.md:** not present. Tautology N/A.
**MCP:** not used.
**ASVS depth applied:** L2 (mitigation present AND at correct trust boundary: explicit-path release staging; annotated tag peel; CHANGELOG honesty tokens in public notes; dual-gate PASS before tag; no packs/catalog.json/CI in tagged tree).
**block_on:** high (default; no `.planning/config.json` override)

**Verdict:** SECURED

**Threats Closed:** 14/14 (12 mitigated CLOSED + 2 ACCEPTED T-18-12, T-18-SC) | **threats_open:** 0

---

## 1. Declared threat register

### 1a. From 18-01-PLAN.md `<threat_model>`

| Threat ID | Category | Severity | Disposition | Evidence found | Status |
|-----------|----------|----------|-------------|----------------|--------|
| T-18-01 | Tampering | high | mitigate | Soft-reset to `PRE_RELEASE_HEAD=acdfedf` then single release commit `6944c14` with **exactly 12** explicit content paths (name-status M only). No `git add -A` residue: zero `catalog.json`, `master_flow_state.json`, phase research/patterns, packs/, SKILLS, NOTICE. Range `acdfedf..6944c14` == same 12 paths. | CLOSED |
| T-18-02 | Tampering | medium | mitigate | `docs/packs.html` header still `GENERATED by tooling/gen_packs_page.py` / `Do not hand-edit`. Diff is REV label `1.19.0` -> `1.19.1` only. RELEASE-INFO bumped in same commit. | CLOSED |
| T-18-03 | Information disclosure | low | mitigate | CHANGELOG `## [1.19.1]` body: **0** U+2014; **0** `http` scheme strings. Release-diff plus-lines: only expected public badge/repo URLs in README; no source-material URLs in CHANGELOG body. Live `check_release` -> `RELEASE CHECK: PASS`. | CLOSED |
| T-18-04 | Spoofing | medium | mitigate | Tagged tree: `docs/capability-pack-map.json` `map_version` **1.19.1**, `schema_version` **2**; CONTRACT example tracks 1.19.1; membership still **TOTAL: 644** (`check_capability_map` PASS). No membership regen in 12-file set. | CLOSED |
| T-18-05 | Elevation of privilege | high | mitigate | `validate.yml` **absent** from `acdfedf..6944c14` and from `6944c14` name list. No repo-Python CI unwire. Overlap + map remain local release-path gates (`check_release` still runs both; live PASS). | CLOSED |
| T-18-06 | Tampering | high | mitigate | Freeze holds: `git ls-tree -d 6944c14:packs` -> **65** dirs; CHANGELOG + gh notes both state **No new packs** / **Catalogue still 63** / **zero packs built**. No `packs/`, `catalog.json`, SKILLS, NOTICE in release commit. Dual-gate PASS at catalog 63 / dirs 65 basis. | CLOSED |
| T-18-07 | Repudiation | medium | mitigate | CHANGELOG `[1.19.1]` has `### Deferred / accepted (not built)` with honesty tokens: FUT-04 DEFERRED, AAF **NOT yet vetted**, PACK-20 deferred-with-evidence / zero packs built, IO-05/06 DEFERRED, IO-07 ACCEPT. gh release body **byte-equal** to CHANGELOG body (heading stripped). No papering: deferred items not claimed shipped. | CLOSED |
| T-18-SC | Tampering | low | accept | No package-manager files/installs in release path set. Stdlib tooling only. Acceptance = PLAN disposition + verified absence + this ACCEPTED row (same class as T-17-SC). | ACCEPTED |


### 1b. From 18-02-PLAN.md `<threat_model>`

| Threat ID | Category | Severity | Disposition | Evidence found | Status |
|-----------|----------|----------|-------------|----------------|--------|
| T-18-08 | Tampering | high | mitigate | Same boundary as T-18-01 at public act: `git show --name-only 6944c14` = 12 version/docs paths only. Planning records in separate commit `c84427a` (`.planning/` only) **after** tag; peel `v1.19.1^{}` = `6944c14` not planning SHA. | CLOSED |
| T-18-09 | Repudiation | medium | mitigate | `git cat-file -t v1.19.1` -> **`tag`** (annotated, not lightweight). Message colon-style: `v1.19.1: hygiene + overlap tooling + deferred items visible (63 +2 signposts)` - matches v1.19.0 pattern. Peel = `6944c14`. Remote: `refs/tags/v1.19.1` -> tag object; `^{}` -> `6944c14`. | CLOSED |
| T-18-10 | Tampering | medium | mitigate | Notes path discipline: no `_v1.19.1-notes.tmp.md` on disk under phase dir; never in tag tree (`git ls-tree` / name-only clean). `gh release view` body matches CHANGELOG (proves notes-file path worked then deleted). | CLOSED |
| T-18-11 | DoS (of release) | high | mitigate | 18-02-SUMMARY records dual-gate PASS before tag. Live re-run this audit: overlap via release path + `PASS: capability map OK` TOTAL 644 + `RELEASE CHECK: PASS`. Tag not created on red gate. | CLOSED |
| T-18-12 | Elevation of privilege | low | accept | User opted admin-bypass; plan forbids changing branch protection. SUMMARY: first push HTTP 408, **one non-force retry** succeeded under admin-bypass; no PR; no force-push. `6944c14` is ancestor of `origin/main`; planning commit fast-forward after. Acceptance = PLAN disposition + no protection mutation + this ACCEPTED row. | ACCEPTED |
| T-18-13 | Information disclosure | low | mitigate | Notes tmp deleted; not staged; not in `6944c14` or `c84427a`. Secret-prefix scan on release **plus-lines**: **0 hits** (AKIA, ghp_/gho_/github_pat_, sk_live_, sk-, xox*, BEGIN PRIVATE KEY, AIza, api_key=/password=/secret=/token=/Bearer assignments). gh notes: **0** secret hits; **0** URLs in body. | CLOSED |
| T-18-SC | Tampering | low | accept | Duplicate accept row (same as 18-01 SC). No npm/pip/cargo installs this phase. | ACCEPTED |

No unmitigated severity >= `block_on` (high). **threats_open: 0.**

---

## 2. Coordinator audit-brief checks (mapped)

| Brief threat | Threat IDs | Result |
|---|---|---|
| 1. Secrets in release commit or notes | T-18-03, T-18-13 | PASS - 0 credential hits in release plus-diff and gh notes body |
| 2. False pack registration / invented packs | T-18-06 | PASS - 63/65 freeze; no packs/catalog.json in tag; notes say no new packs |
| 3. Papering over deferred licence sources in CHANGELOG | T-18-07 | PASS - Deferred section + DEFERRED/NOT yet vetted/zero packs built; notes == CHANGELOG body |
| 4. Lightweight tag mistaken for release | T-18-09 | PASS - object type `tag`; colon message; remote annotated peel |
| 5. Force-push or unprotected publish | T-18-08, T-18-12 | PASS - non-force push (408 retry); no tag rewrite; protection unchanged (accepted bypass) |

### 2a. Release identity (live)

| Field | Value |
|-------|-------|
| Content SHA | `6944c143cd97741257624172302a25627b586fee` |
| Tag | `v1.19.1` type `tag` -> peel content SHA |
| Tag object | `5c960b46aeba0e35c3febfc49079da1782c79103` |
| origin/main | `c84427a` (planning after tag; content ancestor OK) |
| gh release | non-draft, non-prerelease; title uses U+2014; body == CHANGELOG [1.19.1] body |
| Gates now | capability map PASS TOTAL 644; RELEASE CHECK PASS |

### 2b. Scope fence on tagged tree

Forbidden paths in `6944c14`: **none** of `packs/`, `catalog.json`, `SKILLS.md`, `NOTICE`, `LICENSE`, `.github/workflows/validate.yml`, `_v1.19.1-notes.tmp.md`, phase `master_flow_state.json`, `18-RESEARCH`, `18-PATTERNS`.

---

## 3. SUMMARY.md `## Threat Flags` mapping

`18-01-SUMMARY.md` and `18-02-SUMMARY.md` have **no** `## Threat Flags` section. Unregistered-flag count: **0**.

Coordinator five brief items map into declared T-18-01..13 as section 2 table. No new attack surface without threat ID.

---

## 4. Notes (informational - no open threats)

- **N1 - Accept documentation.** Root `SECURITY.md` is vuln/licensing policy only (no per-phase accepted-risks log). T-18-12 (low) and T-18-SC (low) acceptance live in PLAN disposition + this ACCEPTED row - same pattern as T-17-04 / T-16-08.
- **N2 - Push 408 retry.** Network failure then non-force retry under admin-bypass is recorded in 18-02-SUMMARY Deviations. Not a force-push; not a protection change. Does not reopen T-18-12.
- **N3 - Planning commit after tag.** `c84427a` touches only `.planning/**` (STATE/MILESTONES/ROADMAP + phase summaries). Correct separation; consumers of tag tree do not see planning records.
- **N4 - Title em dash vs body.** gh release **title** uses U+2014 (allowed/required by plan must_haves). CHANGELOG/notes **body** correctly has zero U+2014 and zero `http` (T-18-03).
- **N5 - Live porcelain.** Orchestrator may leave `master_flow_state.json` dirty in working tree; not in tagged commit. Not a release threat.
- **N6 - False-positive greps.** Whole-tree `git grep BEGIN PRIVATE KEY` hits archived phase security audits discussing the sentinel string. Not credentials. Plus-line scan of `6944c14` is the authoritative release check.

---

## 5. Scope discipline

- Implementation / packs / CI / tags / GitHub Release: **not modified** by this audit.
- Wrote only this artifact: `.planning/phases/18-map-release-surface-v1-19-1/18-SECURITY_AUDIT.md`.
- No MCP. No retag. No `git add -A`. Temp audit scrap deleted before commit.

**Verdict:** SECURED - all declared threats CLOSED or ACCEPTED at declared boundaries; coordinator brief checks pass; threats_open = 0.
