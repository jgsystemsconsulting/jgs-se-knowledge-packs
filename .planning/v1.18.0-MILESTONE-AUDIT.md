# v1.18.0 Milestone Audit — Gap-Driven Expansion + Agent Enablement

**Date:** 2026-08-14 (audit pass)
**Auditor:** ZCode (milestone audit subagent)
**Scope:** Phases 6–9 (`.planning/phases/{6,7,8,9}-*/`), REQUIREMENTS v1.18 section (VET/GP/AE/REL-1x), live tree at `177338e` (clean), tag `v1.18.0` on origin.

**Audit Status:** gaps_found

One gap, in tech-debt consolidation only (Check 4): the canonical v1.19 backlog in `STATE.md` dropped several recorded carry-forwards, and one deferral chain (7-GAP → 9-GAP → STATE) lost two items entirely. Requirements coverage, cross-phase wiring, E2E golden path, and milestone intent all PASS — the shipped v1.18.0 artifact is not affected. An adversarial pass attempting to break the consumer journey found zero breaks.

---

## Check 1 — Requirements coverage: PASS (15/15 requirements verified with evidence)

Every v1.18 requirement checkbox was verified against on-disk artifacts, not phase claims.

| Req | Checkbox | Live evidence verified on the tree |
|---|---|---|
| VET-01 | `[x]` honest | `docs/SOURCE-VETTING.md` v1.18 Vetted section: all 5 named items carry tier decisions with evidence (40051-2C T1, SP-7084 T1 evidence-only, VV&A RPG T1 chapter-wise, 881F T1, AFOTEC Excluded). 4/5 + AAF deferral annotated in 4 places (REQUIREMENTS text, STATE.md, SOURCE-VETTING DAG row, ROADMAP Phase 6 SC-1 line 88 — verify-time annotation confirmed present). 18 dated verdict stamps; `grep -c http docs/SOURCE-VETTING.md` = 0 (Link Policy held). |
| VET-02 | `[x]` | Excluded table rows for AFOTEC, DoD DAG (with "NOT yet vetted" AAF qualifier), CMU SEI — all dated "(Verified 2026-08-14.)" with rationale. |
| GP-01 | `[x]` | `packs/dod-vva-rpg/` exists (10 ch + supports); PACK.yaml carries P7-PRE-4 x2 per-chapter provenance; 10 entries in capability map. |
| GP-02 | `[x]` | `packs/faa-std-025/` (6 ch); P7-PRE-3/5 in PACK.yaml; Rev F mirror caveat in CHANGELOG. |
| GP-03 | `[x]` | `packs/dote-te-guidebook/` (8 ch); edition 8.02 recorded per P7-PRE-3. |
| GP-04 | `[x]` | `packs/dafman-63-119/` (7 ch); P7-PRE-5 in PACK.yaml; in-copy releasability line quoted. |
| GP-05 | `[x]` | `packs/mil-std-881f/` (7 ch); PACK.yaml P7-PRE-1 evidence chain (QuickSearch ident 36026, Dist Stmt A, cover visual, edition cross-check). |
| GP-06 | `[x]` honest | `packs/federal-bca/` PACK.yaml records the P7-PRE-2 dual gate verbatim: A-94 PASS in-source scan; Army CBA FAIL TO FETCH (403/503), dropped before generation. Single-source annotation in REQUIREMENTS; Army half tracked as FUT-04. |
| GP-07 | `[x]` | `packs/mil-std-40051/` (8 ch); P7-PRE-1 visual DIST-A; cluster 25 (the EMPTY cluster) now 12 entries from this pack. |
| GP-08 | `[ ]` struck | Descoped with rationale (no consolidated NASA-HDBK-2203 PDF); REQUIREMENTS Out of Scope row + SOURCE-VETTING deferral note + Out-of-Scope rescope suggestion. Tracked, not silently dropped. |
| AE-01 | `[x]` honest | Delivered as gate-not-generator: `tooling/check_capability_map.py` (249-line stdlib validator) + CONTRACT §4 agent procedure; annotation in REQUIREMENTS + FUT-05 backlog. Map envelope live: `schema_version: 2`, `map_version: "1.18.0"`, `generated_on: "2026-08-17"`. |
| AE-02 | `[x]` | Gate re-run live: C25 = 12 (≥1), C3 = 3 (≥3), C5 = 4 (≥3), C15 = 10 (≥2); TOTAL 628; exit 0. All 7 new packs mapped (58 entries = 52 chapters + 6 support files, matching the Phase 8 record). |
| AE-03 | `[x]` | `docs/capability-map-CONTRACT.md` (97 lines: schema, versioning, v1 deprecation, refresh path, threshold table) — field-by-field accurate vs live JSON; linked from `docs/capability-pack-map.md`; downstream `docs/ROLE-AGENTS-REQUIREMENTS-V2.md` FR-2.1/FR-2.3 reference the map by name. |
| REL-1x-01 | `[x]` | All 7 slugs present on all 5 registration surfaces (catalog.json 61/61 on disk, 0 dupes; SKILLS.md; docs/packs.html; NOTICE; README; cursor manifest 62). `check_release.py` PASS exit 0 at 61/63 basis. |
| REL-1x-02 | `[x]` | Tag `v1.18.0` verified on origin (`cae0145` tag / `d19be1a` deref). CHANGELOG [1.18.0] leads with the doe-o-413-3 rename note (alias `aliases: ["doe-413-3b"]` confirmed in catalog), includes the v1.17.0 wording fix (index.html), and carries all 3 Phase-7-routed caveats (vva ~2011 chapter dates, faa Rev F mirror, 40051 1168/584 counter). |

No requirement is checked without evidence; none is unchecked without tracking. The four expected honest annotations (GP-06 single-source/FUT-04, GP-08 descoped, AE-01 gate-not-generator/FUT-05, VET-01 4/5+deferral) are all present and all match reality.

## Check 2 — Cross-phase wiring: PASS (6→7→8→9 all WIRED; no unconsumed output)

- **6→7 (vetting caveats enforced at build):** P7-PRE evidence grep-verified in every PACK.yaml — P7-PRE-1 in mil-std-881f + mil-std-40051; P7-PRE-2 x2 in federal-bca; P7-PRE-3 in faa-std-025 + dote-te-guidebook; P7-PRE-4 x2 in dod-vva-rpg; P7-PRE-5 in dafman-63-119 + faa + dote. Evidence is substantive (full fetch chains, gate outcomes), not token mentions. ROADMAP Phase 6 SC-1 carries the AAF re-scope annotation (verify-time residual discharged).
- **7→8 (packs → map):** All 7 new packs mapped with 58 entries; thresholds exceeded (C25 0→12; C15 1→10; C3 2→3; C5 2→4); cluster vocabulary landed (mil-std-881f contributes 7 chapters to Technical Planning & Work Breakdown, now 13 entries). Map `.md` human summary synced (Total 628; per-cluster numbers match gate output).
- **8→9 (map v2 in release):** CHANGELOG Added section documents map v2 + gate + CONTRACT. Version reconciliation verified live: RELEASE-INFO 1.18.0 = cursor plugin 1.18.0 = CHANGELOG [1.18.0] = map_version "1.18.0" = tag v1.18.0 (IN-04 closed). OUSD(R&E) typo fixed in SOURCE-VETTING GP-01 row.
- **Unconsumed outputs:** none found. (SP-7084 is evidence-only by recorded design, not a Phase 7 output; the map gate being standalone from check_release is a documented, tracked deferral, not an unconsumed output.)

## Check 3 — E2E golden path (adversarial): PASS — could not break it

Attempted breaks and results:

1. **Consumer install:** `python install.py --dry-run` → 63 packs resolved, exit 0, nothing written. Catalog 61/61 slugs exist on disk, 0 duplicates; only the 2 intentional signpost dirs are uncataloged.
2. **Progressive disclosure (mil-std-881f):** SKILL.md loads a 7-row chapter index + topic index → all 7 `chapters/ch01..ch07-*.md` exist with matching filenames; ch01 genuinely answers its topic (100% rule, dictionary discipline, three-layer WBS spine, anti-patterns) — not a stub. Supporting files (glossary/patterns/cheatsheet) exist and are linked.
3. **Stale chapter refs:** automated extraction of every `](....md)` link across all 7 new packs' SKILL.md + chapters + root files → **0 broken links**. Every `chNN` text reference checked against each pack's actual chapter count → **0 out-of-range references** (e.g. dote 8, vva 10, 40051 8).
4. **Map → disk:** independent resolver (not the gate) mapped all **628/628** entries to existing files (`packs/<pack>/chapters/<chapter>`, support files at pack root after stripping the documented " (support file)" suffix). Cluster 17 Technical Planning: 13/13 resolve; cluster 25 Training & Documentation: 12/12 resolve.
5. **Catalog ↔ disk ↔ map:** three-way reconciliation clean; `doe-o-413-3` rename alias present in catalog (`aliases: ["doe-413-3b"]`) — CHANGELOG claim accurate.
6. **Both gates:** `check_release.py` PASS exit 0; `check_capability_map.py` PASS exit 0 (628 entries, 32 clusters). The map gate is a real validator (envelope types/shapes, duplicate cluster names, staleness in both directions, file existence, name-keyed thresholds), not a stub.
7. **Stale version surfaces:** only legitimate historical `1.17.0` references remain (CONTRACT v1-lineage note, SOURCE-VETTING historical sections, capability-pack-map.md changelog note).

## Check 4 — Tech debt aggregation: GAP FOUND (consolidation is lossy; shipped product unaffected)

The consolidation chain 7-GAP_ANALYSIS → 9-GAP_ANALYSIS → STATE.md dropped recorded items at two stages:

**Stage 1 — items in 7-GAP_ANALYSIS's v1.19 backlog but dropped from 9-GAP_ANALYSIS's carry-forward list:**
- R4 / Security Note 1: 881F/vva DIST-A in-PDF edition revisit (metadata-class evidence today)
- R6 / IN-01: topic-index polish across 4 shipped SKILL.mds (9-GAP item 9 covers only "R1/R2/R5")

**Stage 2 — items in 9-GAP_ANALYSIS's carry-forward list (items 6–8) but absent from STATE.md "v1.19 backlog (carried)" (the canonical "next: v1.19 backlog (see below)" pointer):**
- CHANGELOG hygiene: UTF-8 BOM + CRLF normalization (verified still present on the tree: BOM bytes at file head; 620 CRLF occurrences)
- `.gitattributes` pin (`*.md text eol=lf`) to prevent CRLF recurrence
- AAF guidebooks vetting before any future DAG-substitute use (carried from the Phase 6 rescope)

**Also absent from every v1.19 list:** P7-BACKLOG — external `jgs-reference-skill/tools/vet_source.py` EXCLUDED-dict sync (afotec / dod-dag / cmu-sei; adjudicated accept in 6-GAP_ANALYSIS Thread 3 with an explicit "Route: extend the existing external-repo sync backlog item" that was never taken).

Items that DID consolidate correctly: FUT-04, FUT-05, minimal committed overlap checker, thin-cluster fattening (3/5/15), map-gate wiring, PACK.yaml note additions (R1/R5), ROSAP Rev E retry, federal-bca "(c)" polish.

Severity: WARNING (process/records), not BLOCKER — all deferred items remain recoverable from phase records, and none affects the shipped v1.18.0 artifact. But STATE.md is the live pointer for v1.19 planning; items absent from it are at real risk of being silently lost.

## Check 5 — Milestone intent: PASS

Original goal (MILESTONES.md v1.18.0 + capability-gap-report): fill gaps so agents have the skills they need. Delivered state, verified live:

- **The map is genuinely consumable today.** A downstream consumer following the CONTRACT's primary read (check `schema_version == 2` → `clusters[].name → chapters[].{pack, chapter}`) succeeds on the committed JSON; my independent resolver consumed it exactly that way (628/628). Envelope, contract doc (schema + versioning + deprecation + refresh path + thresholds), and a stdlib staleness gate are all present and mutually consistent.
- **The empty cluster is filled and the worst clusters are above floors:** cluster 25 went 0 → 12 entries (8 chapters + 3 support files from mil-std-40051, the GP-07 target pack, plus faa-std-025 ch02); cluster 15 went 1 → 10. Honest residual: clusters 3/5/15 remain THIN by the gap report's own taxonomy (<8 entries or ≤2 packs) — correctly recorded as a v1.19 item, not claimed as solved.
- **The downstream repo is unblocked:** ROLE-AGENTS-REQUIREMENTS-V2 FR-2.1/FR-2.3 point at this map as the generation-time source of mapping; the CONTRACT defines what the generator must check and refuse.
- Known limitation (documented + tracked): regeneration is an agent procedure with a staleness gate, not a deterministic script (FUT-05); the gate is standalone rather than wired into check_release (backlog).

## Verdict summary

| Check | Status |
|---|---|
| 1. Requirements coverage | PASS (15/15, honest annotations verified against reality) |
| 2. Cross-phase wiring | PASS (6→7→8→9 all wired end-to-end) |
| 3. E2E golden path (adversarial) | PASS (zero breaks found across 7 attempted break classes) |
| 4. Tech debt aggregation | GAP (STATE.md backlog dropped 6+ recorded carry-forwards; 2 items lost at 9-GAP stage) |
| 5. Milestone intent | PASS (map v2 consumable today; empty cluster filled; downstream unblocked) |

**Consolidated v1.19 backlog (superset — what STATE.md should carry):** FUT-04 (Army CBA retry); FUT-05 (deterministic map generator); minimal committed overlap checker (7-CODE IN-02); thin-cluster fattening 3/5/15; map-gate wiring into check_release; CHANGELOG BOM strip + CRLF→LF; `.gitattributes` pin; AAF guidebooks vetting before any use; 881F/vva DIST-A in-PDF edition revisit (R4); topic-index polish (R6); optional PACK.yaml notes (R1/R5); ROSAP Rev E retry; external vet_source.py EXCLUDED-dict sync (afotec/dod-dag/cmu-sei).

Recommended remediation (one small docs commit, no product change): sync STATE.md "v1.19 backlog (carried)" to the superset above and add R4/R6 to 9-GAP_ANALYSIS item 9 (or note where they live), restoring 1:1 traceability of every deferred item.
