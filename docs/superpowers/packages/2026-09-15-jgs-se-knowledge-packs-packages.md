---
date: 2026-09-15
project: jgs-se-knowledge-packs
mode: light
rounds: 1
input_digest: 1199c7861833f155362b1ef98ed7d702d213ab0048c619e2bc29baab5b683158
open_objections: []
---

# Work packages - jgs-se-knowledge-packs (2026-09-15, light, visual website cut)

Context: user-triggered package-loop on **website visual improvements with images**, using taste/design direction and image creation (jgs-image-gen). Prior cut 2026-09-14 closed P1-P7 on tooling/CI; P8 (phase21-planning-ledger-close) stays **in-flight / frozen** and is not part of this visual cut. New work is P9-P14 on the static GH Pages surface (`docs/index.html`, generated `docs/packs.html`, self-hosted assets under `docs/`).

Carry-forward status from [2026-09-14 packages](2026-09-14-jgs-se-knowledge-packs-packages.md):

| id | name | status |
|----|------|--------|
| P1 | align-website-yaml-versions | done |
| P2 | gate-website-yaml-versions | done |
| P3 | close-phase21-ship-surface | done |
| P4 | ci-link-policy-parity | done |
| P5 | ci-local-gate-coverage-gap | done |
| P6 | validate-pack-signpost-parity | done |
| P7 | map-tooling-residual-harden | done |
| P8 | phase21-planning-ledger-close | in-flight (frozen this cut) |

## P9 - og-share-cards

- **id**: P9
- **name**: og-share-cards
- **size**: M
- **status**: done
- **promoted_ids**: []
- **corroboration**: 3 (value, risk, cohesion)
- **deps**: P12, P13

**problem**: Public pages ship text-only Open Graph and Twitter cards. `og:title` / `og:description` / `og:url` exist, but there is no `og:image` or `twitter:image`, and `twitter:card` is `summary` rather than `summary_large_image`. There is no self-hosted image tree under `docs/` (fonts and HTML only). `docs/packs.html` is generated by `tooling/gen_packs_page.py`, so an index-only meta fix drifts on the next regen.

**evidence**:
- docs/index.html:16-22 OG/Twitter meta without images; `twitter:card` = `summary`
- docs/packs.html:16-22 same pattern
- docs/packs.html:5 `GENERATED by tooling/gen_packs_page.py ... Do not hand-edit (RR-B-00).`
- tooling/gen_packs_page.py:85-91 head template OG block, summary only
- docs/index.html:5-6 self-contained, no third-party CDN/tracking

**in_scope**:
- Create on-brand self-hosted OG/share card image(s) under `docs/assets/` (or equivalent) via taste-aligned prompts and jgs-image-gen; dark drafting tokens (ink/paper CSS vars); no CDN
- Wire `og:image` and `twitter:image` with absolute `https://jgsystemsconsulting.github.io/jgs-se-knowledge-packs/...` URLs; set `twitter:card` to `summary_large_image` once a real image exists
- Mirror meta in `tooling/gen_packs_page.py`, regenerate `docs/packs.html`
- Optional favicon file only if it reuses the same asset kit

**out_scope**: landing hero layout; labelled body stills; packs table UX; visual-regression CI; product YAMLs; P8; stock/CDN hotlinks

**why_now**: Highest-payoff missing visual for a public catalogue; every external link currently previews text-only. Asset path unblocks later on-page stills.

**first_prompt**: `/superpowers-process full add self-hosted OG share cards and wire og:image on index + gen_packs_page (taste + jgs-image-gen)`

## P10 - landing-hero-still

- **id**: P10
- **name**: landing-hero-still
- **size**: M
- **status**: done
- **promoted_ids**: []
- **corroboration**: 2 (value, cohesion)
- **deps**: P9, P13

**problem**: The landing hero is typography and CTAs only (label, h1, lede, two buttons). No figure, still, or brand mark beyond a data-URI favicon. First paint reads as a long drafting memo rather than a product surface, under-using the dark drafting-document system the page already claims.

**evidence**:
- docs/index.html:113-124 hero markup, text + CTA only
- docs/index.html:56-59 hero CSS, text layout only
- docs/index.html:15 data-URI SVG favicon only

**in_scope**:
- Taste/design pass limited to hero composition on `docs/index.html`
- One primary self-hosted hero still under the docs asset path; meaningful alt text; `prefers-reduced-motion` safe
- Minimal inline CSS for hero media (no framework, no CDN)

**out_scope**: OG/Twitter meta (P9); §01-§07 copy rewrite; packs.html body; light theme; P8; install.py / pack Markdown

**why_now**: On-site payoff for visitors who already opened the URL; reuses the brand kit after tokens and share assets exist.

**first_prompt**: `/superpowers-process full add on-brand hero still to docs/index.html (taste + jgs-image-gen, self-hosted)`

## P11 - labelled-site-stills

- **id**: P11
- **name**: labelled-site-stills
- **size**: S
- **status**: ready
- **promoted_ids**: []
- **corroboration**: 1 (value)
- **deps**: P9, P10
- **triage notes**: single-lens; kept because one clear one-run flow and direct line to stated "labelled site stills" intent

**problem**: Below the hero, the landing page stays text and code only (§03 install is a `<pre>` block; §06 catalogue is text chips). User intent asked for labelled site stills; none exist as published assets under `docs/`.

**evidence**:
- docs/index.html:153-165 §03 Using a pack, pre/code only
- docs/index.html:193-209 §06 catalogue, text chips
- docs/index.html:5-6 self-contained dark drafting aesthetic

**in_scope**:
- 1-2 labelled self-hosted stills on `docs/index.html` (e.g. pack-reference filter surface and/or install/invoke moment), captioned in drafting-sheet voice
- On-brand generation or restyle into the asset path from P9; no CDN
- Tight figure + label CSS only

**out_scope**: publishing raw `.playwright-mcp` dumps; using `sources/*/covers` as marketing art; packs.html chrome redesign; screenshot automation / visual CI; OG meta

**why_now**: Pays off only after asset path and hero language exist; otherwise stills become orphan style.

**first_prompt**: `/superpowers-process full add 1-2 labelled on-brand site stills to docs/index.html body sections`

## P12 - html-self-contain-scan

- **id**: P12
- **name**: html-self-contain-scan
- **size**: S
- **status**: done
- **promoted_ids**: []
- **corroboration**: 1 (risk)
- **deps**: none

**problem**: The public brand rule is self-contained HTML with no third-party asset/CDN/tracking, but the local release gate and CI link scans never include `docs/*.html`. `check_release` text collection and the CI host scan skip `.html` suffixes. The first image PR can hotlink a remote `og:image`, font, or script and stay green.

**evidence**:
- docs/index.html:5-6 self-contained, no third-party asset/CDN/tracking
- tooling/check_release.py:125-127 text_files suffixes omit `.html`
- .github/workflows/validate.yml:116-120 CI scan skips `.html`
- docs/packs.html:5-6 self-contained inline CSS/JS claim

**in_scope**:
- Fail-closed scan of `docs/*.html` (and generator template path if needed) for external `http(s)` asset refs (`img`/`src`, stylesheet/font/script hosts, remote `og:image` / `twitter:image`)
- Allowlist intentional first-party hosts (e.g. github.com blob links, `jgsystemsconsulting.github.io` absolute OG URLs, `data:` favicon)
- Document the self-host image path convention under `docs/` for GH Pages
- CI twin within the existing trust boundary, or an explicit residual note if CI twin is out of budget

**out_scope**: creating artwork; full visual-regression suite; adopting a CDN; P8; non-HTML tooling backlog

**why_now**: Images are about to land; without this gate the no-CDN contract is unenforceable on the only files that will change.

**first_prompt**: `/superpowers-process full add fail-closed docs HTML self-containment scan (no remote asset hosts)`

## P13 - brand-token-sync

- **id**: P13
- **name**: brand-token-sync
- **size**: M
- **status**: done
- **promoted_ids**: []
- **corroboration**: 2 (risk, cohesion)
- **deps**: none

**problem**: Brand lives as a duplicated inline `:root` block in hand-edited `docs/index.html` and the f-string template inside `tooling/gen_packs_page.py` that emits `docs/packs.html`. Packs is marked GENERATED / do not hand-edit. Visual token or chrome edits applied only to the landing page leave the catalogue on the old skin; regen wipes hand edits to packs. No gate asserts the shared token block still matches.

**evidence**:
- docs/index.html:31-40 `:root` token block
- tooling/gen_packs_page.py:97-106 duplicated `:root` in template
- docs/packs.html:5-6 GENERATED, do not hand-edit
- docs/packs.html:28-37 same `:root` on disk
- .planning/codebase/TESTING.md:85-91 packs.html freshness local-only; no token parity

**in_scope**:
- Taste / design-style direction pass on the existing dark drafting brand (not a new design system)
- Update shared `:root` tokens and shared mast/base chrome once in `docs/index.html`
- Mirror the same block in `tooling/gen_packs_page.py` and regenerate `docs/packs.html`
- Minimal lock so shared `:root` cannot diverge unnoticed (shared constant, extracted snippet, or assert)
- Keep self-hosted fonts under `docs/fonts/`; no CDN

**out_scope**: OG raster assets and meta (P9); landing hero stills (P10); packs table-only polish (P14); large redesign / external CSS framework; visual-regression suite; P8; install.py / pack content

**why_now**: Shared token/chrome is the common parent of both public pages. Image gen and page polish must follow one direction or drift immediately.

**first_prompt**: `/superpowers-process full sync brand tokens across index.html and gen_packs_page with taste direction + parity lock`

## P14 - packs-table-polish

- **id**: P14
- **name**: packs-table-polish
- **size**: S
- **status**: ready
- **promoted_ids**: []
- **corroboration**: 1 (cohesion)
- **deps**: P13
- **triage notes**: single-lens; kept as bounded generator-scoped polish, one-run clear

**problem**: `docs/packs.html` is a separate UI subsystem (filter tools, sticky table, mobile stacked rows) generated by `tooling/gen_packs_page.py`. Table chrome polish must live in the generator template so hand-edits do not violate RR-B-00 and so work does not mix with landing hero packages.

**evidence**:
- docs/packs.html:5-6 GENERATED, do not hand-edit
- docs/packs.html:50-65 table/tools CSS
- docs/packs.html:89-105 header, tools, table markup

**in_scope**:
- Table/header/tools visual polish inside `tooling/gen_packs_page.py` CSS template only
- Regenerate `docs/packs.html` via the generator
- Optional small catalogue header still if packs-local and self-hosted

**out_scope**: index.html hero; SKILLS.md row content; shared `:root` redesign (P13); OG asset creation (P9); hand-editing the generated table body; P8

**why_now**: Catalogue UI is a separate file+generator surface from the landing hero; polish after shared tokens so chrome matches brand without an oversized site redesign.

**first_prompt**: `/superpowers-process full polish packs.html table chrome via gen_packs_page.py template only`

## Dependency order (triage, light round 1)

P12 → P13 → P9 → P10 → P11 → P14

(P12 and P13 are independent roots; either may start first. P14 only needs P13.)

## Recommended skill stack per package

| Package | Skills inside the SP run |
|---------|--------------------------|
| P13 | `taste-skill`, `design-style-select` (direction), then token edit |
| P9, P10, P11 | `taste-skill` (prompt constraints), `jgs-image-gen` (assets), wire HTML |
| P12 | tooling only |
| P14 | taste light + generator CSS |

## Human forks

None. Conflicts empty this round. P8 remains frozen outside this cut (ledger + ship), not a visual dependency.

## Backlog

See [backlog.md](../backlog.md). Delta this run: new rows b-18..b-25 for rejected visual anti-patterns and deferred infra pets; existing tooling rows b-01..b-17 unchanged (no duplicate re-routes).
